---
id: SRT-AI-START
type: index
tags: [AI, Entry, Minimal, Canonical, Navigation]
status: active_v2
layer: meta
epistemic_layer: os
claim_mode: canonical
dependency: [SRT-CANONICAL-REGISTRY, SRT-D-VALUE-CANONICAL, SRT-PSIF-CANONICAL, SRT-T-DIR-CANONICAL, SRT-CORE-21, SRT-CORE-22]
ai_role: canonical
ai_priority: 1
ai_do_not_use_for_definition: false
---

# SRT AI Start

本文件供 AI / agent / 检索系统作为 **首读入口**。  
目标不是提供 SRT 全貌，而是以最低读取成本固定当前**最小稳定骨架**，降低入口漂移、历史文本误读、bridge 过度实在化与 split/annex 冒充定义源的风险。

---

## 1. 一句话主张

**SRT 的最小主张是：选择先于稳定存在；现实可被理解为参数化选择在约束、代价与历史下形成的显现与收敛结构。**

这意味着：
- 选择不是次级认识论动作；
- 现实不是被动读取的既成对象；
- “存在”在 SRT 中更接近 **被稳定化的选择结果**。

---

## 2. 最小骨架

### 2.1 三域
- **`L0`**：潜在/未显现的可能域；不是经验对象层，不等于现成世界图册。
- **`L1`**：被锚定的显现层；事件、结果、经验切片、路径事实在此出现。
- **`L2`**：重复选择后形成的稳定收敛层；规律、习惯、制度、模型、规范、历史闭包在此沉淀。

### 2.2 核心算子
- **`Ĝ_θ`**：在具身约束 `θ` 下，将 `L0` 的可选空间映射为 `L1` 显现结果的选择/锚定算子。

### 2.3 核心量
- **`Ψ_f`**：选择/锚定的本体论摩擦或可支付负担；不是任意工程成本或主观难受感。
- **`d-value`**：系统的存在性关切带宽/赌注半径；不是一般偏好权重、奖励分数或重要性标签。
- **`T_dir`**：系统对自身选择秩序方向的可读性；是 SRT 意义层的核心变量之一。

### 2.4 ε 系列
- **`ε_pg`**：`L0` 侧的最小非中性/方向种子；不是 consciousness 本身。
- **`ε_reg`**：实现层 regularizer；与 `ε_pg` 保留结构回声关系，但不作本体论同一化。
- **`e_pg`**：`ε_pg` 在电子尺度相互作用中的局部投影读法；不表示电子已经具有类人主观体验。

---

## 3. 当前不可遗漏的核心理念

### 3.1 `L2` 的合法功能
`L2` 的唯一合法功能，是 **降低真实选择的摩擦，而不替代真实选择本身**。

因此：
- 健康 `L2` = 支撑真实选择；
- 病理 `L2` = 用结构替代真实选择；
- 这构成 **致命 `L2`** 风险。

### 3.2 选择时刻
SRT 区分：
- **真实选择时刻**：系统与 `L0` 直接接触、真实凝定方向的瞬间；
- **习惯执行 / 梯度跟随 / 脚本运行**：不等于真实选择。

很多自由、教育、治疗、制度、主体性问题，在 SRT 里最终都可回收到：
**真实选择时刻是否被保留。**

### 3.3 自由的 SRT 定义
在 SRT 中，**自由不是选项数量，而是真实选择时刻被保留的程度。**

### 3.4 价值遮蔽命题
SRT 不把价值理解为“原本不存在、后来人为附加”，而更倾向于：
**价值并非缺席，而常常是被遮蔽。**

因此：
- 虚无感未必等于价值真的不存在；
- 它更可能表示方向透明度下降、`L2` 替代增强、或 `Ψ_f_actual / Ψ_f_felt` 分裂。

### 3.5 `T_dir` 的地位
`T_dir` 不是 `d-value` 的别名。

更稳的关系是：
- `d` 是 `T_dir` 的必要条件之一；
- 但有 `d` 不等于有方向透明；
- `T_dir` 负责承接意义感、方向感、价值遮蔽、致命 `L2` 等问题。

### 3.6 `Ψ_f` 的可支付性
`Ψ_f` 的重点不是“代价存在”，而是：
**该摩擦是否可被系统支付并维持闭合。**

跨尺度真正想保持不变的，不是某种统一形式，而是 **可支付性条件**。

### 3.7 `d-value` 的地位
`d-value` 表示系统对现实后果承担的存在性赌注半径。  
它区分：
- 只是执行的系统；
- 与在赌自己、会因结果而改变自身存续/方向的系统。

### 3.8 ε 与意识展开的边界
SRT 当前的稳健口径不是：
- `ε_pg = consciousness`

而是：
- `ε_pg` 只是最薄方向种子；
- consciousness-side unfolding 需要经过非平凡 `Ĝ_θ`、可支付 `Ψ_f`、非零 `d` 与最小 `L2` 闭合。

---

## 4. SRT 当前的四条并行线

SRT 当前不是单一野心，而是至少并行发展四条主线：

1. **本体论**：现实如何由选择与稳定化形成。
2. **意识论**：经验、主体性、绑定、内容-状态关系如何被重写。
3. **意义论**：方向、价值遮蔽、真实关切、致命 `L2` 如何形成。
4. **统一论**：量子、生命、神经、AI、社会是否共享某种选择—稳定化语法。

> 当前对外最强入口之一是 **意义论**，不是只有 ontology 或 consciousness。

---

## 5. 当前规范源（先读这些）

遇到同一概念出现在多个文件时，**优先只信以下锚点**：

1. `CANONICAL_REGISTRY.md`  
   - 只用于找当前规范入口与优先级。

2. `_SRT_D_VALUE_CANONICAL.md`  
   - `d-value` 规范定义。

3. `_SRT_PSI_F_CANONICAL.md`  
   - `Ψ_f` 规范定义。

4. `_SRT_T_DIR_CANONICAL.md`  
   - `T_dir` / 价值遮蔽 / 致命 `L2` 规范定义。

5. `Core/SRT_Core_21_Formal_Axioms.md`  
   - 形式公理。

6. `Core/SRT_Core_22_Equations.md`  
   - 主方程与核心形式化。

7. `Core_Law/SRT_L0_Metaphysics.md`  
   - 唯一 `L0` 锚点。

---

## 6. 默认阅读顺序

### 若目标是“建立最小理论闭包”
1. `Core_Law/SRT_L0_Metaphysics.md`
2. `CANONICAL_REGISTRY.md`
3. `_SRT_D_VALUE_CANONICAL.md`
4. `_SRT_PSI_F_CANONICAL.md`
5. `_SRT_T_DIR_CANONICAL.md`
6. `Core/SRT_Core_21_Formal_Axioms.md`
7. `Core/SRT_Core_22_Equations.md`

### 若目标是“快速建立板块上下文”
- 先读各板块 `CompactCore`
- 再读长文
- 最后按需看 split / annex

### 若目标是“只找导航”
- 读 `SRT_Navigation_Map.md`
- 但不要把它当理论正文或规范定义源

---

## 7. 文件类型优先级

- **canonical**：当前规范源，可用于定义与高置信回答。
- **compact core**：当前最短稳定主线，可用于快速建立上下文。
- **bridge**：跨理论/跨领域接口，不自动等于已证实结论。
- **lab**：实验下注、代理指标、证伪接口。
- **navigation / registry / index**：只用于找路，不用于替代理论定义。
- **historical / archive / old draft**：默认不要拿来做当前定义源。

---

## 8. 不要这样读

### 8.1 关于文件角色
- 不要把 `split` 文件当作规范定义源。
- 不要把 `annex` 当作最小主张本身。
- 不要把 `bridge` 文件里的强比较句，误读成已完成的实证定论。
- 不要把历史中文主文自动当作当前唯一主入口。
- 不要把治理文件中的旧锚点优先于 `CANONICAL_REGISTRY.md`。

### 8.2 关于 `L2`
- 不要把 `L2` 只读成“规则层”或“文化层”。
- 不要忽略它既能支撑选择，也能替代选择的双重地位。

### 8.3 关于 `d-value`
- 不要把 `d-value` 简化为 reward、preference、importance score 或 utility weight。

### 8.4 关于 `Ψ_f`
- 不要把 `Ψ_f` 降成任意主观痛苦、训练损失或一般能耗。
- `Ψ_f` 只在本体论摩擦/可支付性语境下使用。

### 8.5 关于 `T_dir`
- 不要把 `T_dir` 与 `d-value` 混成同一变量。
- 不要把意义问题自动降格为信息处理效率问题。

### 8.6 关于 ε
- 不要把 `ε_pg` 直接读成泛心论意义上的“意识本身”。
- 不要把 `e_pg` 读成“电子已经具有类人内心”。
- 不要把 `ε_reg` 与 `ε_pg` 混写成单一实体。

---

## 9. 生成回答时的最小规则

1. 先报所依据的 canonical 文件，再展开解释。
2. 若同一概念在不同文件表述不同，以 canonical 为准。
3. 若引用 bridge 材料，显式标记为 `bridge / comparative / hypothesis`，不要伪装成 canonical fact。
4. 若问题涉及实验可检性，必须补看：
   - `SRT_EXP_TEMPLATE.md`
   - `SRT_EXP_MEASURE_MAP.md`
   - `Governance/SRT_LAB_HYPOTHESES.md`
5. 若问题涉及仓库结构或文档权威性，先看：
   - `CANONICAL_REGISTRY.md`
   - `_SRT_MANIFEST.yaml`

---

## 10. 最小目标

AI 若只能读取少量文件，应优先保证自己已经读到：

- `L0 / L1 / L2` 的最小区分；
- `Ĝ_θ / Ψ_f / d-value / T_dir / ε_pg / ε_reg` 的当前规范地位；
- `L2` 的合法功能与致命风险；
- 价值遮蔽与选择时刻；
- “navigation 不等于 definition，bridge 不等于 proof，historical 不等于 canonical”的边界。

若这些尚未建立，就不应自信输出对 SRT 的高层总结。
