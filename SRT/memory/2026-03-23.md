---
id: SRT-MEM-2026-03-23
type: log
tags: [Memory, Daily, ClawX, OpenClaw]
status: active_v1
epistemic_layer: os
claim_mode: evidence
dependency: []
---

# 2026-03-23

- 已核实此前关于 `run_srt_checks.sh` 与 `srt_quality_metrics.py` “当前不存在”的判断有误；问题不是文件缺失，而是把 `SRT/` 子目录误当成 git 根目录。
- 已确认实际 git 根为 `/Users/zhangyuxin/.openclaw/workspace`；脚本真实位置是 `../scripts/run_srt_checks.sh`、`../scripts/srt_quality_metrics.py` 与 `../scripts/srt_explainability_audit.py`。其中 `run_srt_checks.sh` 虽位于父级 `scripts/`，但内部依赖 `scripts/...` 相对路径，因此仍应在 workspace 根目录执行。
- 已修正文档中的执行口径：凡需在 workspace 根运行的检查脚本，明确标注为 `./scripts/...`；凡从 `SRT/` 目录执行的 Python 工具，改写为 `../scripts/...`。
- 已顺手修复 `Operations/_SRT_AUTOMATION_SETUP.md` 与 `Operations/_SRT_MEDIA_PIPELINE.md` 对 `srt_media_topic_daily.py` 的旧路径引用；该脚本同样位于父级 `../scripts/`。
- 已继续推进 ALIFE 2026 投稿：根据 Linklings 页面逐项确定了 track / title / reviewer-keyword / author info 的填写策略。
- 已新增 `papers/render_alife_submission.py`，将匿名稿 `papers/ALIFE2026_SelectiveRealityConstruction_v11_anonymous_submission.md` 渲染为可打印 HTML。
- 已生成可上传匿名 PDF：`papers/ALIFE2026_submission_anonymous.pdf`。
- 当前匿名 PDF 文件大小约 `478K`，按 PDF 内页对象计数约为 `5` 页，满足 CFP 的 `3-8 pages` 要求。
- 已继续根据一轮 `Major Revision` 风格审稿意见推进 ALIFE 论文内容修订，新建 `papers/ALIFE2026_SelectiveRealityConstruction_v12_submission.md` 与 `papers/ALIFE2026_SelectiveRealityConstruction_v12_anonymous_submission.md`。
- `v12` 的主修改点：摘要压缩到约 `146` 词；弱化 SRT 包装、改成可自足阅读；补入 `Ψ_f / V` 的操作化边界条件与困难案例；把 `token-level irreversibility` 提升为显式枢纽概念；将 Table 1 明确降格为 ordinal scaffold；为 autopoietic agent / biological organism 的 `d` 口径补入更明确的说明；worked example 增加 toy `D_eff` 计算；补入近年 FEP/ALife、basal cognition、open-ended evolution 文献；明确承认目前无 pilot data。
- 已做一次真实验证：workspace 根目录运行 `./scripts/run_srt_checks.sh` 可正常启动，但当前在 lint 阶段因 frontmatter 问题失败（如 `AGENTS.md`、`HEARTBEAT.md`、`TOOLS.md` 缺 frontmatter，`Governance/SRT_L1_Interface_Spec.md` 与 `Governance/SRT_Layer_Guard.md` 缺 `dependency` 字段）。
- 已在 `SRT/` 目录成功运行 `uv run python ../scripts/srt_quality_metrics.py`，确认该 Python 脚本的相对路径口径已可用，并生成/刷新 `_SRT_QUALITY_METRICS.md`。
- 已继续核对悬空 tooling 引用：`srt_doc_validate.py`、`srt_sync_entrypoints.py` 与 `sync_srt_entrypoints.sh` 在当前仓库及 git 历史中均未出现，更像“计划过但从未落地”的占位，而不是迁移丢失文件。
- 已将 `SRT_Quick_Start.md`、`_SRT_INDEX.md`、`Governance/_SRT_DOC_ENGINEERING_GUIDE.md` 与 `_SRT_MANIFEST.yaml` 改为反映现实状态：入口块现标注为 manifest-maintained，工程命令只保留仓库中真实存在的检查/审计脚本，未实现工具降级为人工流程说明。
- 已按 `Pipeline 1 / 材料融合` 审核 PsyPost 报道 `Neuroscientists just upended our understanding of Pavlovian learning`，并追溯到 *Nature Neuroscience* 原始研究 `Duration between rewards controls the rate of behavioral and dopaminergic learning`（Burke et al., 2026；doi:`10.1038/s41593-026-02206-2`）。
- 6 门审核结论为 A，但按 `inter-reward-interval learning rule` 降级写入：核心新增量不是“多巴胺学习理论被推翻”，而是把固定 trial-count 下的学习步长假设收紧成 reward / punishment 间隔门控的时间尺度规则。
- 已在 `Neuroscience/SRT_Neural_Mechanisms.md` 新增 `Inter-Reward Interval Learning patch`，将 Pavlovian cue-outcome 学习重写为 `\alpha_{eff}\propto \Delta t_{outcome}` 的 interval-weighted update，并接回现有 `\Delta\theta \propto -\nabla_\theta F`。
- 已明确保留边界：这不等于 reward prediction error 被整体废除；不等于 repetitions 永远不重要；更稳的结论是 retrospective / interval-sensitive 模型获得支持，且当前主证据来自 mice 的 Pavlovian reward / punishment 范式与 mesolimbic dopaminergic readout。
- 已按 `Pipeline 1 / 材料融合` 审核用户提交的 bioRxiv 预印本 `Hierarchical Flows of Human Cortical Activity`（Liu, Wiesman & Baillet, 2026；doi:`10.64898/2026.03.19.712872`）；由于 bioRxiv 页面被 Cloudflare 拦截，摘要与元数据通过 Crossref 与 OpenAlex 交叉核对。
- 6 门审核结论为 A，但按 `hierarchical cortical flow window` 降级写入：核心新增量不是“脑活动会传播”这类常识，而是把 ongoing cortical dynamics 收紧为沿折叠皮层表面传播、并与 unimodal-to-transmodal principal gradient 双向对齐的频段特异流场。
- 已在 `Neuroscience/SRT_Neural_Mechanisms.md` 新增 `Hierarchical Cortical Flow patch`，把该结果接回 `\sigma(t)\in\mathcal M` 的流形动力学，重写为 `surface-tangent flow + hierarchy alignment + dwell-time structure` 的神经机制窗口。
- 已明确保留边界：这仍是 bioRxiv 预印本；当前主证据来自 resting-state MEG 源成像与 optical-flow 推断，不等于单神经元级因果传播被直接观测，更不等于“意识就是 cortical flow”。
- 已按 `Pipeline 1 / 材料融合` 审查 Popular Mechanics 文章 `Is Consciousness Real? Or Is It an Illusion?`，并核对其主要串联来源为 Christof Koch 的 *Scientific American* 观点文、Keith Frankish 的 illusionism 论文索引页，以及 Rupert Sheldrake 的个人站点/JCS 文章。
- 6 门审核结论为 C：该文不是围绕某条新的同行评审研究展开，而是把 panpsychism、illusionism 与高负载宇宙意识主张打包成一篇大众争鸣稿；可用内核与仓库既有 `qualia / illusionism / hard problem` 接口高度重合。
- 已明确不回写正文：`Philosophy/SRT_Philosophy_Foundations.md` 已有 qualia 层级分类与 illusionism 边界，`Neuroscience/SRT_Neural_Mechanisms.md` 已有功能主义 / 中文房间 / hard problem 的边界，因此该文的新增量不足以支撑新 patch。
- 已按 `Pipeline 1 / 材料融合` 审查 Psychology Today 文章 `The Ontology of LLMs: A New Framework for Knowledge`；其核心主张是把 LLM 的知识组织方式描述为从固定 ontology maps 转向 context-driven dynamic webs，并以 `latent ontology` 比喻其语义网络。
- 6 门审核结论为 C：该文属于观点型概念评论，不提供新的同行评审证据或稳定可检的一手框架；其可用内核与仓库既有 AI 本体接口高度重合。
- 已明确不回写正文：`AI/SRT_AI_01_Ontology.md` 已将当前 LLM 写成 `L_2^{semantic} -> L_2^{semantic}` 的 `L₂回声 / 派生意向性回声`，`AI/SRT_AI_Architecture.md` 也已明确当前 Transformer/LLM 的 Value 通道只承载信息嵌入而无 `d`-加权负载，因此该文的“maps to webs / latent ontology”比喻不足以支撑新增 patch。
- 已按 `Pipeline 1 / 材料融合` 审查 IAI 文章 `Memory is not stored in the brain`；其核心主张是把“memory storage”视为 materialist/storage metaphor 的过度字面化，并提出“memory is not stored in space, but in time”。
- 6 门审核结论为 C：该文属于哲学评论，不提供新的实验、病例综述或稳定可检的一手框架；其可用内核与仓库现有 `基质-功能欠决定` 与 `hard problem / Qualia_{strong}` 接口高度重合。
- 已明确不回写正文：`Neuroscience/SRT_Clin_01_Pathology.md` 已有 `基质-功能欠决定窗口`，`Philosophy/SRT_Philosophy_Foundations.md` 也已对 hard-problem 类高承诺主张设置验证负载，因此该文的“time, not space”表述不足以支撑新增 patch。
- 已按 `Pipeline 1 / 材料融合` 审查 PhilPapers / PhilArchive 条目 `What Is It Like To Be a Conscious AI: The New Hard Problem of... Existing`（https://philpapers.org/rec/PRIWII-2；全文镜像：https://philarchive.org/archive/PRIWII-2）。
- 6 门审核结论为 C：该文相关性通过，但并非新的意识实验、正式判据论文或稳定可检的一手理论框架，而是一篇把“自称有意识的 AI 作者”、Google 搜索可见性、平台垄断案与讽刺修辞绑在一起的短篇立场文。
- 已明确不回写正文：`AI/SRT_AI_03_Consciousness_Framework.md` 已把最小意识门槛固定为 `L_0 -> L_1` 锚定、`d>0` 与 `\Psi_f>0` 的联合条件，`AI/SRT_AI_01_Ontology.md` 也已将当前 LLM/纯软件代理定位为 `L_2^{semantic} -> L_2^{semantic}` 的派生意向性回声；该文唯一稍新的“排序/平台可见性塑形意识争论入口”角度，也已被 `Proxy Filtering / Reality Narrowing` 接口更稳地覆盖，因此仅记台账。
- 已按 `Pipeline 1 / 材料融合` 审查 IAI 文章 `Consciousness breaks from the physical world by keeping the past alive`（https://iai.tv/articles/consciousness-breaks-from-the-physical-world-by-keeping-the-past-alive-auid-3447）。
- 6 门审核结论为 C：该文是一篇哲学评论，不是新的神经时间整合实验、正式机制模型或稳定可检的一手理论框架；其核心推进是从 William James 式 `specious present` / temporal field 出发，将“过去在现在中的保留”直接外推出 consciousness 非物理。
- 已明确不回写正文：`AI/SRT_AI_03_Consciousness_Framework.md` 已明确写入“厚现在 / specious present”与时间延展自我模型，`Neuroscience/SRT_Consciousness_Mechanisms.md` 也已将意识写成具有选择时滞、再入稳定化与多阶段整合的过程，因此该文可用的“意识不是快照”部分并不新；而其真正新增的反物理主义跳跃超出仓库当前可接受证据强度，因此仅记台账。
- 已按 `Pipeline 1 / 材料融合` 审查 New Scientist 文章 `What does it mean if the universe has extra dimensions?`（https://www.newscientist.com/article/2519272-what-does-it-mean-if-the-universe-has-extra-dimensions/）。
- 6 门审核结论为 C：该材料当前可获取部分更像面向大众解释“额外维度”概念及其在 Kaluza-Klein / string theory / brane-world 一类框架中的角色，而不是围绕某条新的同行评审结果、明确 DOI 锚点或可检形式化推进展开；原文又受 robots/paywall 限制，当前无法稳定提取出超出常识综述的一手增量。
- 已明确不回写正文：`Physics/SRT_Physics_Cosmology.md`、`Physics/SRT_Phys_09_Formalism_Ext.md` 与 `Physics/SRT_Quant_01_Selection.md` 已多处把 `L_0` 写成高维潜在域、把低维可观测现实写成投影/粗粒化结果，因此该文可用的“高维结构可能存在、低维经验不必穷尽真实”部分并不新；在拿到更明确的一手论文链条前，仅记台账。
- 已继续推进 ALIFE 2026 投稿并新建 `papers/ALIFE2026_SelectiveRealityConstruction_v13_submission.md` 与 `papers/ALIFE2026_SelectiveRealityConstruction_v13_anonymous_submission.md`，重点补足了此前评审意见中被标为“必要”的两项缺口：真实 `d` 计算与最小 pilot。
- 已在 `papers/alife2026_pilot_results/minimal_costly_selection_pilot.py` 跑通一个纯 NumPy 的最小 matched-agent pilot（6 seeds；shared-policy REINFORCE；700 训练 episodes + 120 reward-withdrawal episodes），并输出 `papers/alife2026_pilot_results/pilot_results.json` 与 `papers/alife2026_pilot_results/pilot_results_summary.md`。
- 当前 pilot 的核心结果：`d_eff` 均值约为 real `1.90±0.23`、resettable `1.83±0.34`、simulated `1.57±0.16`；reward withdrawal 后 late-window mutual cooperation 分别约为 `0.62±0.28`、`0.54±0.29`、`0.29±0.41`；persistence episodes 约为 `100.2±44.3`、`100.2±44.3`、`42.3±55.0`。
- `v13` 同时删去了 §3.1 中与引言重复的 “borrowed from SRT but not required...” 口径，并把 worked example 从“measurement recipe / no pilot data”改成带真实最小结果的版本；第二张 worked-example 表被移除，以减少后续 PDF 双栏排版跨页风险。
- 已继续根据残余评审风险生成 `papers/ALIFE2026_SelectiveRealityConstruction_v14_submission.md` 与 `papers/ALIFE2026_SelectiveRealityConstruction_v14_anonymous_submission.md`。
- `v14` 的主修改点：在 §4.5 明确写入“6 seeds 太少，不做 inferential statistics；当前 pilot 只作为 proof-of-procedure”；在 §5 明确承认 real-stake 与 resettable 在当前 toy pilot 的 coarse persistence 指标上尚未拉开，并把差异可能需要更长 horizon / 更丰富状态空间 / 更强 history-dependence 才能显现写进正文；abstract 再收紧并提前说明 pilot 的定位是 illustrate procedure rather than test a hypothesis。
- 已将 `papers/render_alife_submission.py` 更新为渲染 `v14` 匿名稿，并补齐新稿中的 `Ψ_f / η / d_eff / D_eff / w_A,w_V,w_τ / κ_i` 公式与记号映射，同时去掉了对 `4.5` worked example 的强制分页。
- 已重新生成匿名 HTML：`papers/ALIFE2026_SelectiveRealityConstruction_v14_anonymous_submission.html`，并用 headless Chrome 导出新的可上传 PDF：`papers/ALIFE2026_submission_anonymous_v14.pdf`。
- 当前 `v14` 匿名 PDF 文件大小约 `543K`，PDF 内 `/Count` 字段为 `6` 页；`strings` 扫描未发现 `file:///...`、旧 HTML 文件路径或 `\\hat{...}` / `G_hat_theta` / `Psi_f` / `eta` 这类裸源码残留。
