## 2026-04-15

- 用户明确了 GitHub 侧的最终目标：只保留 `main`，且 `main` 的内容要和当前 SRT 目录一一对应，原 GitHub 内容不要。
- 已执行分支收口：本地分支改名为 `main`，`origin/main` 已强制对齐到当前内容提交 `f5c9c4b`，并删除了远端 `master`。
- 现在远端只剩 `main`，本地工作区干净，后续同步只围绕 `main` 进行。
