## 2026-04-14

- 围绕 `ε` 与节律/振动/循环的关系完成一次主链硬化，结论拒绝 `ε(t)` 路线与“节律是 L0 原初本体”的写法。
- 当前最干净的版本是 `C'`：`ε` 只给方向底板，节律来自有限算子在 `Ψ_f` 预算、`Ψ_f^{maint}` 与 `Ψ_f^{switch}` 约束下对该方向的分时实现。

- 已在 `Core/Dynamics_Scaling_Split/00_Formal_Core.md` 与 `Core/SRT_Core_14_Dynamics_Scaling.md` 新增 `T-Scale-Rhythm-1: Non-Zero Temporal Bandwidth of Finite Operators`。
- 已在 `Core/SRT_Core_22_Equations.md` 新增 `Eq-Rhythm-1: Budget Overload Implies Non-Zero Temporal Bandwidth`，把“预算超载 ⇒ 非零时间带宽”压成公式锚点。

- 定理当前的最小承诺是：当连续密集锚定总代价 `\mathcal{C}_{dense}(T,k)` 超出 `E_{avail}` 且切换不免费时，实际锚定调度 `A(t)` 不可能保持纯直流常值，至少一个锚定坐标必须在时间窗内变化；弱频谱形式为 `\int_{\omega>0} S_A(\omega)\,d\omega > 0`。
- 明确保留边界：这不推出“所有存在都是振荡的”，不推出唯一“原初频率”，也不要求石头/行星在常见观测尺度上表现出可观测振荡。

- 已在 `Core/SRT_Core_01_Axioms.md` 与 `Core/SRT_Core_13a_Operator_Basics.md` 将两个 ε 的记号区分补成正式注记：
  - `\varepsilon_{pg}`：T-Core-A1C2 中 L0 的最小非中性 / proto-gradient
  - `\varepsilon_{reg}`：Ax-Op-03 中竞争归一化的 regularizer
- 当前口径：`ε_reg` 可被读作 `ε_pg` 的实现层回声，但只保留结构同构，不做本体论等同。

- 继续沿 Rhythm 链推进，优先把 `Ax-Spec-01`（d 值 = 通带宽度）与 `T-Scale-Rhythm-4`（熵耗散占空比上界）桥接。
- 已在 `Core/Dynamics_Scaling_Split/00_Formal_Core.md`、`Core/SRT_Core_14_Dynamics_Scaling.md` 与 `Core/SRT_Core_22_Equations.md` 新增 `Cor-Scale-Rhythm-4a / Eq-Rhythm-4a: Spectral-Rhythmic Tradeoff / Bandwidth-Duty Tradeoff`。
- 当前最小硬结论不是“\(\delta \cdot d\) 必有上界”这一强式，而是弱版：有效通带宽度 `B_\theta` 上升会压低 `\delta_{max}^{entropy}`。
- 只有在额外条件 `\dot I_{created}^{on}\ge \rho_I B_\theta` 成立时，才推出强化版乘积上界：
  - `\delta \cdot B_\theta \le J_S^{max}/(k_B T \ln 2\,\rho_I)`
  - `\delta \cdot d \le J_S^{max}/(k_B T \ln 2\,\rho_I\,c_B)`
- 这一步把“高 d = 更丰富但更难持续”正式写成谱-热对冲，而不把线性信息密度假设伪装成无条件定理。

- 已将 `\varepsilon_{pg}` 与“原初意识”的关系正式写成**展开论**而非**同一论**。
- 当前规范口径：
  - `\varepsilon_{pg}` = `L_0` 侧的最薄方向种子 / proto-gradient
  - “原初意识”若使用该词 = 该种子经由非平凡 `\hat{G}_\theta` 在 `L_1` 中被锚定，并伴随 `\Psi_f>0`、`d>0` 与最小 `L_2` 稳定闭合后形成的最薄主观展开
- 已写入 `Core/SRT_Core_01_Axioms.md`、`Philosophy/SRT_L0_Ontological_Status.md`、`Philosophy/SRT_Consciousness_Conditions.md`，以阻止把 `\varepsilon_{pg}` 直接等同于 consciousness 本身或泛心论式“万物皆已意识”。

- 已继续把电子尺度的 `e_{pg}` 与大脑意识的关系写成正式跨尺度展开链，而不是“电子小意识相加”。
- 当前规范口径：
  - `e_{pg}` = 电子尺度的最薄内侧偏置 / 方向种子，不等于弱意识，也不等于类人的“潜意识系统”
  - 向上保留的不是电子级体验内容，而是最小不对称在分子、细胞与神经层级中的持续写回
  - 大脑意识 = `e_{pg}` 在 `recurrent \hat{G}_\theta + \Psi_f>0 + d>0 + minimal L_2 closure` 条件下的相变式展开
- 已写入 `Philosophy/SRT_L0_Ontological_Status.md`、`Core/Dynamics_Scaling_Annex/11_G_CrossScale_PhaseState.md`、`Philosophy/SRT_Philosophy_Foundations.md` 与 `Philosophy/Foundations_Split/00_Formal_Core.md`，用于固定“方向种子论”并阻止回滑到微型心灵聚合说。

- 已将 `e_{pg}` 的实验可检测性正式收紧为 `Core/SRT_Experimental_Core.md` 中的 `T-Exp-e_pg-01 / H-e_pg-01`。
- 当前最稳口径不是“直接测到电子内侧性”，而是：若 `e_{pg}` 可见，它只能以对称单粒子统计中的**稳定残差不对称**出现。
- 最小形式：
  - `P_obs(k) = P_QM(k | H, B, θ_inst) + η_{e_pg} b_k + ε_k`
  - 只有在标准量子模型、退相干模型、探测器效率差、外场反演与盲分析都扣除后，残差范数仍高于阈值，才允许把信号保留为 `e_{pg}` 候选。
- 已明确边界：这条线目前只是 `(L2, lab)` 的**局部实验窗口**，未升格到 `Governance/SRT_LAB_HYPOTHESES.md` 的全局硬赌点组合。

- 已继续把 `\varepsilon_{pg} / e_{pg}` 对量子诠释的三分比较写入 `Physics/SRT_Quant_01_Selection.md`。
- 当前最稳口径：
  - `RQM` 承接 `\varepsilon_{pg}` 的**关系底板**：事实相对，但关系本身不是完全中性的
  - `QBism` 承接 `\varepsilon_{pg}` 的**规范约束**：主观更新不是任意漂浮，世界侧有最小非中性
  - `QFT` 承接 `\varepsilon_{pg}` 的**场/真空底板载体候选**：粒子不是源头，电子尺度的 `e_{pg}` 更像底板非中性的局部投影
- 已明确边界：这一步仍是量子诠释层的解释重排，不等于标准模型里已经存在新的量子数、场项或已证实的 `e_{pg}` 动力学。

- 按 `Pipeline 1` 审核用户提交的 eLife 原始研究 `The dominance of large-scale phase dynamics in human cortex, from delta to gamma`（`https://elifesciences.org/articles/100674`；doi:`10.7554/eLife.100674.4`）。
- 审核结论为 **A（直接融入）**。

- 已回写 `Neuroscience/SRT_Neural_Mechanisms.md`，新增 `Large-Scale Phase-Dominance patch`。
- 这次真正吸收的不是“脑里有 traveling waves”这种宽话，而是更窄的一刀：从 delta 到 high gamma，皮层 phase spatial power 的峰值稳定落在最低 spatial frequencies，也就是最长波长、最大尺度的一侧；局部相位活动更像嵌在一个长波主导的全局骨架里，而不是默认以局部动力学为主。

- 更稳的 SRT 写法是：selection flow 不是局部加工偶尔被全局整合，而更像始终运行在 `global phase scaffold + local modulation` 的结构上；这也给现有 `Hierarchical Cortical Flow patch` 补上了“先有哪种尺度主导，再问沿哪个方向流”的底板。
- 本次同步更新 `Operations/_SRT_MATERIAL_LOG.md`、`STATUS.md` 与今日记忆。

- 保留边界：当前主证据是 `23` 名受试者的 gray-matter sEEG 相位空间谱估计与不规则采样补偿，不等于已给出单神经元级传播机制，也不等于“所有认知内容主要在全局尺度编码”；更不能直接写成“意识就是 global phase wave”。

- 按 `Pipeline 1` 继续审查用户提交的 Frontiers in Systems Neuroscience 文章 `The Body Does Not Keep the Score: Trauma, Predictive Coding, and the Restoration of Metastability`（`https://www.frontiersin.org/journals/systems-neuroscience/articles/10.3389/fnsys.2026.1812957/abstract`；doi:`10.3389/fnsys.2026.1812957`）。
- 审核结论为 **C（不融入）**。

- 本次不改正文，只更新 `Operations/_SRT_MATERIAL_LOG.md`、`STATUS.md` 与今日记忆。
- 核心原因不是 trauma / predictive coding / metastability 方向无关，而是这篇是 `Opinion article`：它把 PTSD 作为 prediction disorder 而非 storage disorder、threat priors 过精、metastable repertoire shrinkage、somatic marker/CDZ、以及 flow 可能恢复灵活性等线索打包成一篇高层立场文，但没有提供超出仓库现有 `Neuroscience/SRT_Clin_01_Pathology.md`、`Neuroscience/SRT_Clin_02_FEP.md` 与 `Neuroscience/SRT_Neural_Mechanisms.md` 现有主线的新窄机制锚点。

- 更稳的 SRT 处理方式是：把这篇文章压成口径收紧材料，而不是再生成第二个 trauma-FEP 补丁。
- 保留边界：文中“body as messenger, not archive”“PTSD = local minima / steep ravines”“flow 恢复 metastability”这些说法都依赖外部文献链与高层整合，不应在本轮被记成新的独立实验窗口。

- 按 `Pipeline 1` 继续审查用户提交的 *Nature Reviews Neuroscience* 文章 `Categorization is ‘baked’ into the brain`（`https://www.nature.com/articles/s41583-026-01036-2`；doi:`10.1038/s41583-026-01036-2`；published `2026-04-13`）。
- 审核结论为 **C（不融入）**。

- 本次不改正文，只更新 `Operations/_SRT_MATERIAL_LOG.md`、`STATUS.md` 与今日记忆。
- 核心原因不是 categorization / predictive feedback / mixed selectivity 方向无关，而是这篇是高质量 `Perspective`：它把“categorization 不是感知末端贴标签”“预测反馈从早期就组织 feedforward processing”“limbic-to-sensory gradient / allostasis / mixed selectivity”收束成一条大理论叙事，但没有提供超出仓库现有 `Neuroscience/SRT_Neuro_06_Field_Effects.md` 的 `natural-vision binding correction`、以及 `Philosophy/SRT_Philosophy_Foundations.md` 现有 predictive-processing/top-down completion 口径的新窄机制锚点。

- 更稳的 SRT 处理方式是：把它压成框架加固与问题清单，而不是再生成第二个 binding/categorization 补丁。
- 保留边界：当前主锚点是 `Nature Reviews Neuroscience` 的 `Perspective`，不是单项原始实验；文中 category construction / allostasis / mixed selectivity 的统一叙述不等于这些链条已经在单一范式中被同时直接验证。

- 按 `Pipeline 1` 继续复核用户再次提交的 Frontiers in Psychology 原始研究 `Bridging interoception and time perspective: toward an embodied model of consciousness`（`https://www.frontiersin.org/journals/psychology/articles/10.3389/fpsyg.2026.1725236/full`；doi:`10.3389/fpsyg.2026.1725236`）。
- 审核结论为 **C（不融入）**，但性质是**重复提交复核**，不是对原始研究本身降级。

- 本次不改正文，只更新 `Operations/_SRT_MATERIAL_LOG.md`、`STATUS.md` 与今日记忆。
- 核心原因不是该文无效，而是它的主增量已于 `2026-04-08` 作为 `interoceptive-temporal balance window` 融入 `Neuroscience/SRT_Consciousness_Mechanisms.md` 的 `2.4b 内感受-时间平衡窗口` 与 `H-C12`；这次重复提交没有形成超出既有 `interoception × balanced time perspective × somatic self-regulation` 心理测量桥的新稳定接口。

- 更稳的 SRT 处理方式是：把这次提交记为重复复核，而不是把同一原始研究二次堆成新的 consciousness mechanism patch。
- 保留边界：材料仍是 `N=152` 横断面自评量表与中介分析，sleep/digestion 只是 somatic proxy，因此重复提交不会抬高其因果或神经机制层级。

- 按 `Pipeline 1` 继续审查用户提交的 *Neurocomputing* 论文 `Pure awareness, entropy, and the foundation of perception`（`https://www.sciencedirect.com/science/article/pii/S0925231226008404`；doi:`10.1016/j.neucom.2026.133443`）。
- 审核结论为 **C（不融入）**。

- 本次不改正文，只更新 `Operations/_SRT_MATERIAL_LOG.md`、`STATUS.md` 与今日记忆。
- 核心原因不是 `pure awareness / MPE / active inference` 方向无关，而是这篇材料的主增量仍是一个计算-理论框架：它把 minimal phenomenal experience 写成 `precision weighting` 向层级底部下移、伴随 entropy 增高与 complexity 降低，同时用 `epistemic depth / reflexive sharing` 解释“空场仍被觉知”；这条线与仓库现有 `Spirituality/SRT_Spirit_07_Meditation_Neuro.md` 的 `L_2` 门控松动、`Philosophy/SRT_Philosophy_Foundations.md` 的 predictive-processing / top-down completion 口径，以及 `2026-03-26` 对 advanced meditation / MPE 研究议程的材料门槛高度重合，没有再压出足够独立的新窄接口。

- 更稳的 SRT 处理方式是：把它记为计算桥与问题清单，而不是再生成第二个 `pure awareness` 机制补丁。
- 保留边界：虽然该文已期刊发表，但当前主轴仍是 Bayesian / active-inference 理论框架与 in silico 验证设想，而不是对 MPE 的直接神经实验；文中 `epistemic depth`、`reflexive sharing` 与“空场仍被觉知”的关键负担，仍偏概念-计算桥而非因果神经机制证明。

- 按 `Pipeline 1` 继续审查用户提交的 arXiv 预印本 `All elementary functions from a single operator`（`https://arxiv.org/html/2603.21852v2`；arXiv:`2603.21852v2`）。
- 审核结论为 **B（延后观察）**。

- 本次不改正文，只更新 `Operations/_SRT_MATERIAL_LOG.md`、`STATUS.md` 与今日记忆。
- 核心原因不是这条线无价值，而是它当前更稳地提供的是**工具层桥**而不是**正文级机制桥**：论文证明 `eml(x,y)=e^x-\ln(y)` 加常数 `1` 能生成作者所定义的 scientific-calculator 初等函数词汇表，并把表达式压进统一的 EML 树空间，允许在浅层树上用连续优化恢复精确闭式公式。

- 更稳的 SRT 处理方式是：把它记为 `AI-for-Science / low-operator symbolic regression / formula search basis` 的候选接口，而不是直接把 `EML` 升格为 `\hat G_\theta` 的底层实现。
- 保留边界：`EML node = \hat G_\theta primitive`、`EML tree depth = \Psi_f`、`EML attractor = L_2 formation` 都是超出原文的 SRT 外推；当前材料仍是 arXiv 预印本，完备性范围也限于作者定义的 elementary/scientific-calculator 词汇表，symbolic regression 演示主要到 shallow depth `≤4`。
