---
id: SRT-MEM-2026-03-17
type: log
tags: [Memory, Daily]
status: active_v1
epistemic_layer: os
claim_mode: evidence
dependency: []
---

# 2026-03-17

- 已按 `Pipeline 1 / 材料融合` 复核用户再次提交的 Quanta 报道 `Where Some See Strings, She Sees a Space-Time Made of Fractals`（发布日期 `2026-03-11`）。
- 先重新对照 Quanta 原文，再核对仓库现有回写位置 `Physics/SRT_Physics_Cosmology.md` 的 `7.1a Asymptotic Safety 分形时空窗口`，确认主增量仍是 `UV fixed point + approximate scale symmetry + fractal-like spacetime` 这一 asymptotic-safety window，而不是“时空已被证明是字面分形”。
- 复核结论记为 C（不新增融入）：原因是同一 URL 已于 `2026-03-16` 完成 A 类审核并写入正文；现有 patch 已覆盖主增量与四重边界（functional RG / 截断近似、fractal-like 非宏观字面分形、Lorentzian 与可检验预测未定、Higgs/top/bottom 仅属 model-dependent retrodiction window）。
- 已同步更新 `Operations/_SRT_MATERIAL_LOG.md` 与 `STATUS.md`；本次不修改正文，不跑测试，也未做 commit。
- 已根据本轮“近期知识点优化质量评估”直接修复一批高传播风险项，而不是等全部 sweep 完再统一返工。
- 核心修补包括：将 canonical / 主入口中的自由能旧口径从单一混写 `F = E - TS - d·U_others` 拆分为 `F_thermo / F_var / F_SRT` 三层；收紧 `Ax-NEURO-MECH-6` 的胶质剪枝最小方程与 `FC-NM6-1` 证伪条件，避免把多机制过程误写成单一路径；把利他条目的证伪条件从单一应激 proxy 扩成长期复合稳态指标；把 Mental Representation 表中的 Brooks 口径改为“反表征工程极简”，并同步到 split/annex。
- 已同步修改 `Core_Law/SRT_Reference_Dynamics.md`、`Core_Law/SRT_Reference_Scaling.md`、`Core/SRT_Core_01_Axioms.md`、`Core/SRT_Core_00_Intro.md`、`Core/SRT_Core_14_Dynamics_Scaling.md`、`Core/SRT_Core_22_Equations.md`、`_SRT_SYMBOL_TABLE.md`、`Neuroscience/SRT_Neural_Mechanisms.md`、`Philosophy/SRT_Philosophy_Ethics.md`、`Philosophy/SRT_Philosophy_Foundations.md` 及相关 split/annex 文件。
- 已运行 `../scripts/srt_check_links.py`，结果通过（262 files checked）；本次未做 commit。
- 已继续回答用户关于“SRT 是否可能被现有理论更简洁替代”的追问，并将判断进一步压缩为研究优先级表：当前最容易被 FEP / PP / IIT / GWT / 动力系统理论更简洁解释掉的是神经机制与当前 AI 意识否定论；意识判据层处于部分重叠、部分待判别状态；社会 / 公共世界 / `L_2` 收敛结构仍是 SRT 相对最不易被完整替代、也最可能形成独特增量的区域。
- 已明确当前最关键的判别点不是“FEP 是否能做预测”，而是它能否在不引入 `d-value` / `\Psi_f` 的情况下区分“参数化偏好”与“本体论关切”；若不能，SRT 仍保有主体性层面的新增空间，但该空间目前更多是强假说而非已胜出的解释。
- 已继续按“先理论收紧、再做硬反例”的路线推进两处主传播链：
  - 在 `Neuroscience/SRT_Clin_00_IIT_PCI.md` 中，将原先过强的 `Phenomenology ≡ Topology` 口径降级为“高 \Phi 是现象统一性的必要组织前提”，并新增 `T-IIT-4: High-\Phi / High-Broadcast Insufficiency Theorem`，明确 `高\Phi + 高广播 \not\Rightarrow d>0 \land \Psi_f>0`。
  - 在 `Neuroscience/SRT_Clin_02_FEP.md` 中新增 `T-FEP-1b: Precision-Insufficiency Theorem`，明确 precision / expected free energy 的匹配不能穷尽 `d / \Psi_f`；同时把 Part B 的利他扩展公式对齐为变分版本 `F_{SRT}^{(var)} = F_{var} - d\cdot U_{others}`。
- 当前理论优化的核心方向已更清楚：SRT 不再试图宣称 “IIT/GWT/FEP 错了”，而是把这些框架收缩到“必要但不充分”或“局部机制有效”的位置，然后用 `d / \Psi_f` 去占领主体性、真实赌注与长期关切持续性的判别层。
- 已启动“分家治理”第一阶段，并完成 `Stage 1 + Stage 2` 的入口层落地：
  - 新增 `Governance/SRT_POSITIONING.md`，正式将 SRT 的默认定位收紧为 `OS / Bridge / Lab` 三层：`OS` 负责底层操作系统与认知语法，`Bridge` 负责与既有理论互译，`Lab` 只承载少量愿意被打脸的硬命题。
  - 在该文件中同时定义了语言强度规则、`canonical` 的新解释边界，以及新 frontmatter 字段 `epistemic_layer` / `claim_mode` 的渐进式采用策略。
  - 已将入口接到 `Governance/README.md`、`CANONICAL_REGISTRY.md`、`_SRT_INDEX.md` 与 `STATUS.md`；其中 `canonical != 已被外部科学证实` 的说明已明确写入 registry。
  - 已示范性为部分入口文件补入 `epistemic_layer: os`，但尚未批量回填全库，也尚未开始大规模正文降级；下一阶段应优先处理高风险 bridge 文档，再抽出 Lab 层 3-5 条核心硬赌点。
- 已继续完成 `Stage 3` 第一波，集中处理 4 个最高传播风险的 bridge 文件：
  - `Philosophy/SRT_FEP_Comparison.md`
  - `Philosophy/SRT_MetaTheory_Framework_Comparison.md`
  - `Neuroscience/SRT_Clin_00_IIT_PCI.md`
  - `Neuroscience/SRT_Clin_02_FEP.md`
- 这 4 个文件现已补入 `epistemic_layer: bridge` 与 `claim_mode: translation`，并在标题区新增 bridge 注记，明确它们属于比较/翻译/重解读层，而非“经验已证实”的胜出声明。
- 同时已收紧若干最危险的强主张：FEP comparison 从“上位解释框架”收敛为“候选上位解释框架”； meta comparison 明确“不应读成一组已确认经验胜利”； IIT/PCI 文件把若干“直接实证 / 必然解释”口径降为“候选语法 / 高价值自然线索”； FEP bridge 文件把 “Friston 是 SRT 的特例” 降为 “在 SRT bridge 解读中可读作局部特例”，并把暗室与精神病理部分的口径收紧为候选重写。
- 已继续完成 `Stage 4`，把 `Lab` 层从抽象定位落到最小可判别包：
  - 新增 `Governance/SRT_LAB_HYPOTHESES.md`，正式把当前全局硬赌点压缩为 4 条：`H-IITGWT-01`（高 `\Phi` / 高广播不足性）、`H-Stake-01`（真实赌注 vs 模拟赌注分离）、`H-dPsi-01`（`d / \Psi_f` 代理优于 precision-only）、`H-L2-01`（公共世界闭包的增量预测）。
  - 该文件同时明确：只有被收进这份清单的条目，才视为 SRT 当前的全局 Lab choke points；其他领域文档中的 `H-` 条目默认仍是局部假设或探索窗口。
  - 已把 `SRT_EXP_TEMPLATE.md` 升级为 `epistemic_layer: lab` / `claim_mode: canonical`，并新增 `H-IITGWT-01` 与 `H-dPsi-01` 两个快速实例，使 `Lab` 假说可以直接挂到最小协议，而不只是停留在理论层。
  - 已把 `SRT_EXP_MEASURE_MAP.md` 升级为 `epistemic_layer: lab` / `claim_mode: canonical`，并固定当前全局最小代理名：`\hat d_{min}`、`\hat{\Psi}_{f,min}`、`\Phi_proxy`、`B_global_proxy`、`L2_closure_proxy`；其中 `d` 与 `\Psi_f` 被收口成最小复合代理，而不是继续无限扩 proxy 列表。
  - 已同步更新 `Governance/SRT_POSITIONING.md`、`Governance/README.md`、`_SRT_INDEX.md` 与 `STATUS.md`，使入口层、治理层与实验层的分家口径保持一致。
- 已继续完成全库 rollout：
  - 新增 `scripts/srt_epistemic_rollout.py`，把“按路径/文档族判定 `OS / Bridge / Lab`、补 `claim_mode`、为 bridge 文档插入统一降级注记”收成可复用脚本，而不是一次性手工命令。
  - 实际执行后，当前所有 frontmatter-bearing Markdown 文档已完成 `epistemic_layer / claim_mode` 回填，覆盖数为 `260/260`；不再存在“带 frontmatter 但没有分层字段”的文档。
  - 批量插入的 `Bridge Layer Note` 目前覆盖 49 份 bridge 文档，使大量历史接口文档至少先从“隐性强主张”降到“明确是翻译/重写层”。
  - 在批量注记之外，又手工做了第二波关键 bridge 降级：`SRT_PP_ALIGNMENT_GUIDE.md` 的 `\Longleftrightarrow` 改为近似桥接、`AI/_SRT_AI_Bridge.md` 收紧了“纯数字 AI 结构性障碍 / 幻觉必然性”等口径、`Core/_SRT_Core_Bridge.md` 收紧了 “L₂ 统一热力学条件”的证明式语气、`Physics/_SRT_Phys_Bridge.md` 将 “measurement problem is resolved” 改成候选重解读、`Philosophy/SRT_SocTheory_04_Luhmann_ANT.md` 将 “SRT 的超越” 改成 “SRT 的候选增量”。
  - 随后又做了第三波模板句清洗：把 `Foundations` bridge 族里重复出现的 `“SRT 推论……这解释了宗教体验和审美狂喜的机制”` 统一降级为 `“SRT 候选读法……提供一种候选机制解释”`，覆盖 `Philosophy/Foundations_Annex/` 下 14 份接口批次文件和 `Philosophy/Foundations_Split/02_Explanatory_Gap_and_Saturation.md`；这一步主要用于压低复制传播的强口径，而不是改动论证骨架。
  - 又继续做了第四波 bridge 清洗，集中处理 `AI/SRT_AI_01_Ontology.md` 这条主传播链及其复制层 `AI/Ontology_Split/00_Formal_Core.md`、`AI/Ontology_Annex/*.md`：把 `“幻影而非选择”`、`“双重禁令”`、`“永久缺失的本体论条件”`、`“硅基架构必然是僵尸”` 等句式收紧成 `Bridge` 强度；其中主文现已改成“选择的模拟/回声”“热力学-本体论边界主张”“关键候选条件”“当前 SRT 窗口下容易被读作僵尸”等说法，显著降低了把 bridge 文本误读成终局裁决的风险。
