# SRT Observer Design：\(U(t)\) 观测算子设计草案

## 目标
把 SRT 中“选择性现实偏置”从抽象叙事变成可计算对象：

\[
\lambda^{(\omega)}(t)=\lambda^{(0)}(t)\exp(\omega U(t))
\]

其中 \(U(t)\) 必须满足：
1. 可从实验信号实时估计
2. 与任务目标/现实选择方向语义一致
3. 可做消融检验（替换/打乱后效果应下降）

---

## 1) 观测算子族（建议并行比较）

### A. 目标模板一致性算子 \(U_{tmp}(t)\)
适用于：已有目标神经态模板 \(\mathbf{z}^*\)

\[
U_{tmp}(t)=\cos\big(\mathbf{z}(t),\mathbf{z}^*\big)
\]

其中 \(\mathbf{z}(t)\) 可由多通道 firing-rate 向量或低维潜变量表示。

**优点**：直观，易解释。  
**风险**：模板质量决定上限。

---

### B. 预测误差反算子 \(U_{pe}(t)\)
适用于：有状态预测模型 \(\hat{\mathbf{z}}(t|t-1)\)

\[
U_{pe}(t)=-\|\mathbf{z}(t)-\hat{\mathbf{z}}(t|t-1)\|_2^2
\]

**解释**：偏好“低惊讶/高可控”现实。  
**优点**：与主动推断/自由能框架兼容。

---

### C. 任务语义一致性算子 \(U_{sem}(t)\)
适用于：任务有语义标签 \(y_t\)（意图/阶段/策略）

\[
U_{sem}(t)=\log p(y_t|\mathbf{z}(t))
\]

**解释**：选择性现实 = 与当前任务语义最一致的神经态。

---

### D. 相位-耦合算子 \(U_{sync}(t)\)
适用于：你关心跨回路协调（神经振荡/相位锁定）

\[
U_{sync}(t)=\sum_{(i,j)\in E} w_{ij}\cos\big(\phi_i(t)-\phi_j(t)-\Delta_{ij}^*\big)
\]

**解释**：将“现实选择”表述为特定耦合构型吸引。

---

## 2) 组合算子（推荐）
单一算子容易偏。建议线性组合并正则化：

\[
U(t)=\beta_1 U_{tmp}(t)+\beta_2 U_{pe}(t)+\beta_3 U_{sem}(t)+\beta_4 U_{sync}(t)
\]

约束：\(\|\boldsymbol\beta\|_1\le c\) 或 \(\|\boldsymbol\beta\|_2^2\) 正则。  
通过交叉验证选择 \(\beta\)。

---

## 3) 统计检验协议（最小可发表）

### Step 1. 主效应
拟合 \(\omega\) 并报告：
- \(\hat\omega\), CI
- \(P(\omega>0)\)
- LRT/Bayes 因子

### Step 2. 消融
- 将 \(U(t)\) 替换为随机打乱 \(U_{perm}(t)\)
- 重新拟合 \(\omega\)
- 期望：\(\hat\omega_{perm}\to 0\)，模型证据下降

### Step 3. 竞争模型
比较：
- M0: 仅 mTOR 阈值（\(\omega=0\)）
- M1: mTOR + 单算子 \(U_k\)
- M2: mTOR + 组合算子

用 WAIC/LOO/BIC 或 held-out NLL 排序。

---

## 4) 实验设计建议（防止不可辨识）
1. 至少 2~3 种任务条件（改变语义与控制负荷）
2. 至少 2 种刺激统计（改变 mTOR 上游驱动）
3. 在不同时间尺度（10ms / 50ms / 100ms）重复拟合
4. 跨日重复以排除漂移伪影

---

## 5) 你当前项目的落地顺序
1. 先用 \(U_{tmp}\) + \(U_{sem}\) 组合（最稳）
2. 验证 \(\omega\) 主效应与消融
3. 再引入 \(U_{sync}\) 解释回路协同
4. 最后做层级模型（跨回路共享 \(\omega\) 先验）

---

## 6) 判据（SRT 支持阈）
给一个可执行标准：
- \(P(\omega>0) > 0.95\)
- 消融后 \(\Delta\mathrm{NLL} > 0\) 且 \(\Delta I(N;U) > 0\)
- 竞争模型中含 \(U\) 模型在 held-out 指标上最优

满足以上三条，才称为“支持 SRT 选择性现实偏置”。
