---
id: SRT-MEM-2026-04-05
type: log
tags: [Memory, Daily, Materials, Consciousness]
status: active_v1
epistemic_layer: os
claim_mode: evidence
dependency: []
---

# 2026-04-05

- 已按 `Pipeline 1 / 材料融合` 复核用户再次提交的 IAI 文章 `Studies on animal minds suggest consciousness is not computation`（`https://iai.tv/articles/studies-on-animal-minds-suggest-consciousness-is-not-computation-auid-3535?_auid=2020`）。
- 本次重新对照了 IAI 原文可见部分、其显式引用的 `biological naturalism` 线索，以及仓库内 `2026-04-01` 的既有 C 类裁决。结论没有翻盘：该文仍是一篇把 `biological naturalism`、跨动物振荡现象和较早的果蝇注意/`beta` 振荡线索打包成强标题的理论评论，而不是新增比较神经科学证据或稳定机制论文。
- 审核结论维持为 C（不融入）。不融入的核心原因依旧不是主题无关，而是增量门与证据门都不足以支撑新增正文 patch；现有 `Biological Naturalism Interface`、比较动物意识接口与振荡/波场边界已足以承接其主要可用部分。
- 已将本次处理作为“重复提交复核”写入 `Operations/_SRT_MATERIAL_LOG.md`，并把统计更新为 `124` 条总提交、`84` 条 A、`6` 条 B、`34` 条 C。这样后续再遇到同一 URL 时，不会把“再次出现”误判为“新增材料”。
- 已同步更新 `STATUS.md`。本次只改台账、状态与今日日志，不改正文。
- 已按 `Pipeline 1 / 材料融合` 复核用户再次提交的 Quanta 文章 `In Expanding de Sitter Space, Quantum Mechanics Gets Even More Elusive`（`https://www.quantamagazine.org/in-expanding-de-sitter-space-quantum-mechanics-gets-even-more-elusive-20260330/`）。
- 本次重新对照 Quanta 原文与仓库内 `2026-04-02` 的既有 B 类裁决，确认这篇材料的主要价值仍是暴露 de Sitter 中 `patch-relative / observer-bound observability`、无单一边界、以及 `AdS/CFT` 直觉不可硬搬的压力；但这些内容目前更像 Bridge 级观察窗口，而不是足以直接改写 SRT 正文的稳定接口。
- 因此，本次没有把该材料改判成新的 A 或新的独立 B 项，而是将这次提交作为“重复提交复核”记账：活跃处置仍维持 `2026-04-02` 的 B（延后观察），本次提交本身不产生新的正文动作。
- 已将此条写入 `Operations/_SRT_MATERIAL_LOG.md`，并把统计更新为 `125` 条总提交、`84` 条 A、`6` 条 B、`35` 条 C。这样后续再遇到同一 Quanta URL 时，能区分“已有观察项仍有效”与“本次提交没有新增条件”。
