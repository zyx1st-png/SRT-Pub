## 2026-04-13

- 按 `Pipeline 1` 审核用户提交的 eLife 原始研究 `Functional connectivity-based attractor dynamics of the human brain in rest, task, and disease`（`https://elifesciences.org/articles/98725`；doi:`10.7554/eLife.98725.3`）。
- 审核结论为 **A（直接融入）**。

- 已回写 `Neuroscience/SRT_Neural_Mechanisms.md`，新增 `Functional-Connectivity Attractor Geometry patch`。
- 本次真正吸收的不是“脑像吸引子网络”这种宽泛旧话，而是更窄的一刀：resting-state functional connectivity 本身已经足以约束少数宏观 attractor basins，使真实脑活动更像在既有低能盆地之间受限游走；task 与 disease 更像是对这些 basin 的偏移、流向重加权与占据概率改变。

- 更稳的 SRT 写法是：momentary activation pattern 不是每次都从零生成，而更像在 connectome 给出的 basin geometry 上游走；因此任务与病理不必先写成“新增一套动力学”，也可以先写成原有 attractor 地形被系统性重配。
- 这条接口可接到 `Neuroscience/SRT_Neural_Mechanisms.md` 的 `Neural Manifold / Selection Flow` 主线，把 \(\sigma(t)\) 从一般连续轨迹进一步收紧为 connectome-constrained basin-to-basin traversal。

- 已同步更新 `Operations/_SRT_MATERIAL_LOG.md`、`STATUS.md` 与今日记忆。
- 保留边界：当前模型主要是 coarse-grained fMRI connectome + Hopfield 类抽象动力学，不等于单神经元级真实实现；`approximately orthogonal attractors` 与 free-energy-minimizing attractor prediction 一致，但不应被写成对 FEP / active inference 的终局证明。

- 按 `Pipeline 1` 继续审查用户提交的 *Cortex* 综述 `The tenacious brain: How the anterior mid-cingulate contributes to achieving goals`（`https://www.sciencedirect.com/science/article/abs/pii/S0010945219303326`；doi:`10.1016/j.cortex.2019.09.011`）。
- 审核结论为 **C（不融入）**。

- 本次不改正文，只更新 `Operations/_SRT_MATERIAL_LOG.md`、`STATUS.md` 与今日记忆。
- 核心原因不是 aMCC / effort / persistence 方向无关，而是该文是 2020 年 broad review：它把 aMCC 写成 cost/benefit integration、attention allocation、memory / affect / motor control 汇流的 tenacity hub，但没有提供超出仓库现有 `vmPFC-前扣带回-腹侧纹状体` 努力回路、`高 β 成本敏感` 和 `astroglial effort transition` 窗口的新窄机制锚点。

- 更稳的 SRT 处理方式是：把它保留为问题清单，而不是生成第二层 ACC 补丁。
- 若后续出现更窄的一手结果，再优先判断 aMCC 是否能在 `energy forecasting / effort persistence / action initiation` 之间形成独立 formal window。

- 按 `Pipeline 1` 继续审查用户提交的 Essentia Foundation 文章 `Can AI be conscious?`（`https://www.essentiafoundation.org/can-ai-be-conscious/reading/`；Christof Koch，2026-04-03）。
- 审核结论为 **C（不融入）**。

- 本次不改正文，只更新 `Operations/_SRT_MATERIAL_LOG.md`、`STATUS.md` 与今日记忆。
- 核心原因不是 AI consciousness / IIT / cerebellum analogy 方向无关，而是这篇文章属于立场型 op-ed：它把 computational functionalism、IIT 的 Φ 读法、小脑类比、zombie / uploading 论证重新打包成一篇高传播性论战稿，但没有提供超出仓库现有 `AI/SRT_AI_01_Ontology.md` 的 `L₁ 封闭 / V=0` 边界、`AI/SRT_AI_03_Consciousness_Framework.md` 对 `Φ测量` 局限的说明、以及 `Neuroscience/SRT_Neuro_09_Integ_Eq.md` 的 `Φ-d 正交 / pseudo-experience` 的新窄命题。

- 更稳的 SRT 处理方式是：把它继续压成“行为相似≠体验成立、内部组织比输入输出更关键、IIT 需和 d/Ψ_f 并读”的问题清单，而不是再生成第二个“LLM 不会有意识”的正文补丁。
- 其中 Koch 文中援引的“四元神经回路 vs 可编程计算机 Φ 差异”还指向未同行评审 arXiv 稿，因此当前证据负担也不足以支撑新增 formal window。

- 按 `Pipeline 1` 继续审查用户提交的短链 `https://t.co/8Q68aoIUm7`，已解析到 *Science* 论文 `A shared code for perceiving and imagining objects in human ventral temporal cortex`（doi:`10.1126/science.adt8343`）。
- 审核结论为 **A（直接融入）**。

- 已回写 `Neuroscience/SRT_Neural_Mechanisms.md`，新增 `Ventral-Temporal Shared Imagery Code patch`。
- 本次真正吸收的不是“想象和知觉共享回路”这种宽话，而是更窄的一刀：human VTC 中的 imagery 不是另起一套对象表征，而是对 perception 所用 distributed axis code 的部分重激活；因此内在模拟更像对既有 perceptual object code 的低锚定重放。

- 更稳的 SRT 写法是：visual imagery 不是 free-floating symbol play，而是对 perceptual code 的代价性借用；这条接口可接到 `Core/SRT_Core_13a_Operator_Basics.md` 的 `T-Op-HFL`，把“无锚点想象的高摩擦”从抽象律进一步压到 human VTC 的单神经元窗口。
- 已同步更新 `Operations/_SRT_MATERIAL_LOG.md`、`STATUS.md` 与今日记忆。

- 保留边界：本轮主锚点主要来自 DOI 与可访问 abstract metadata，*Science* 正文页被 Cloudflare 挡住，因此任何超出摘要的实验细节都需等后续直接核正文后再加满。
- 当前结果也只稳覆盖 human VTC object imagery，不自动推广到全部想象类型、空间想象或跨模态 imagination。

- 按 `Pipeline 1` 继续审查用户提交的 OUP 官方期刊页 `Continual Learning of Multiple Cognitive Functions with Brain-inspired Temporal Development Mechanism`（`https://academic.oup.com/nsr/advance-article/doi/10.1093/nsr/nwag066/8450195`；doi:`10.1093/nsr/nwag066`）。
- 审核结论为 **C（不融入）**。

- 本次不改正文窗口，只更新 `Operations/_SRT_MATERIAL_LOG.md`、`STATUS.md`、`memory/2026-04-12.md` 与今日记忆。
- 核心原因不是材料无效，而是这次提交带来的主要增量是“来源升级”而不是“理论增量”：它独立确认了 `National Science Review` 的 OUP 官方 accepted-manuscript 页面、正式 DOI 与摘要可核信息，关闭了 `2026-04-12` 留下的“`NSR` DOI 待独立核验”缺口，但没有形成超出既有 `发展式持续学习窗口` 的第二个稳定接口。

- 更稳的 SRT 处理方式是：把这次提交压成“证据闭环 / 来源复核”，而不是再生成一层持续学习剪枝补丁。
- 保留边界：当前可公开读取的仍主要是期刊页摘要与 accepted-manuscript 元数据，所以任何超出摘要可见范围的实验细节都不继续外推，也不因期刊收录就自动上调到通用 AGI 路线。

- 按 `Pipeline 1` 继续审查用户提交的 New Scientist 文章 `The simple questions cracking the hard problem of consciousness`（`https://www.newscientist.com/article/2519288-the-simple-questions-cracking-the-hard-problem-of-consciousness/`）。
- 审核结论为 **A（直接融入）**。

- 已回写 `Philosophy/SRT_Philosophy_Foundations.md`，新增 `Qualia Relational-Geometry Bridge`。
- 这次真正吸收的不是“hard problem 快被攻克了”这种标题层宽话，而是更窄的一刀：qualia 不必只作为不可言说的命名单点处理，也可以先作为 `L_1` 内部的相似性、邻接与扭曲关系来研究；意识理论因此不该只回答“有没有报告/有没有点亮”，还应承担对体验几何的预测负担。

- 更稳的 SRT 写法是：解释鸿沟仍然成立，但它的一部分负担可以先被改写为 `L_1` 关系几何约束问题；`L_gap > 0` 不再只是静态边界声明，还可以接出一个操作化桥，要求理论对 experience neighborhood geometry 给出约束。
- 本次同步更新 `Operations/_SRT_MATERIAL_LOG.md`、`STATUS.md` 与今日记忆。

- 保留边界：本轮主锚点主要是 Fleming & Shea 2024 的方法综述、Hirao et al. 2025 的颜色 qualia/no-report 数据描述，以及 Ellia & Tsuchiya 2025 的结构转向评论；它们不等于已经回答“为什么红色就是这种感觉”，也不构成统一机制定论。
