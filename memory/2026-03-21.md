---
id: SRT-MEM-2026-03-21
type: log
tags: [Memory, Daily]
status: active_v1
epistemic_layer: os
claim_mode: evidence
dependency: []
---

# 2026-03-21

- 已按 `Pipeline 1 / 材料融合` 审核用户提交的 Veritas / *New Scientist* 报道 `A single-celled organism without a brain is capable of Pavlovian learning`。
- 已追溯到主锚点：Gershman 团队 *bioRxiv* 预印本 `Associative learning in the protozoan Stentor coeruleus`（doi:`10.1101/2026.02.27.640732`，`2026-02-27` posted）。
- 6 门审核结论为 A，但按 `non-neural associative-learning window` 降级写入：核心新增量不是“单细胞已经有意识”，而是把无神经系统的生理计算窗口从 habituation 推进到一个更窄的 `weak tap -> strong tap` 短时联结预测层。
- 已在 `Neuroscience/SRT_Neuro_07_Evo_Devo.md` 新增 `3.2.5 单细胞联结学习窗口`，并把这条材料接回 `\theta_{physio}` 与 `deep symmetries between evolution and learning` 的主线。
- 已明确保留四重边界：当前主证据仍是预印本；范式主要是约 `1` 秒量级、每 `45` 秒重复一次的短时配对；报道 readout 主要是培养皿中固定 `Stentor` 的群体响应；该结果不等于任意长 trace conditioning，也不等于现象意识已获证明。
- 已同步更新 `Operations/_SRT_MATERIAL_LOG.md` 与 `STATUS.md`；本轮未做 git commit，且仓库中未发现 `run_srt_checks.sh` / `scripts/srt_quality_metrics.py` 可直接执行版本。
- 已继续按 `Pipeline 1 / 材料融合` 审核 *Trends in Cognitive Sciences* 综述 `Beyond binding: from modular to natural vision`（Scholte & de Haan, 2025；doi:`10.1016/j.tics.2025.03.002`；PMCID:`PMC12135946`）。
- 6 门审核结论为 A，但按 `natural-vision binding correction` 降级写入：核心新增量不是某个新实验结果，而是对仓库里较旧的强模块化绑定口径做纠偏，明确经典 binding problem 可能部分是“先模块化、再重绑定”的理论预设产物。
- 已在 `Neuroscience/SRT_Neuro_06_Field_Effects.md` 新增 `1.1a 自然视觉绑定收紧补丁`，将自然视觉重写为更倾向直接编码 `co-occurring, behaviorally relevant patterns`，并把 binding-style 问题收缩为人工去相关、注意竞争与异常组合条件下被放大的子问题。
- 已明确保留边界：该文是同行评审综述而非单项新实验；不等于绑定问题彻底消失；也不自动替 ephaptic 场论完成排他性证明。
- 已继续按 `Pipeline 1 / 材料融合` 审核 *Communications Psychology* 原始研究 `Interoceptive ability is uncorrelated across respiratory and cardiac axes in a large scale psychophysical study`（Banellis et al., 2026；doi:`10.1038/s44271-026-00404-z`）。
- 6 门审核结论为 A，但按 `interoceptive fractionation window` 降级写入：核心新增量不是再说“内感受很重要”，而是用一致的心理物理框架与 `N=241` 样本把“单一 interoceptive ability”收紧成器官/模态特异的能力束。
- 已在 `Neuroscience/SRT_Neuro_08_Immune_Dist.md` 新增 `3.3b Interoceptive Axes Fractionation`，把现有 `Vagus Multiplex Interface` 进一步推进为 `\mathcal I_{cardiac} / \mathcal I_{respiratory} / ...` 的多轴 manifold，并明确 confidence 可能比 sensitivity / precision / metacognition 更接近高层共享 readout。
- 已明确保留边界：当前研究只覆盖心脏与呼吸两轴；“不相关”不等于永远独立；confidence 的 modest association 先记为候选高层偏置，而不是稳定的 domain-general core。
- 已继续按 `Pipeline 1 / 材料融合` 审核 Popular Mechanics 报道 `You Owe Your Intelligence to These Tiny Life Forms, Research Suggests—And They’re Still Shaping Our Brains`，并追溯到 PNAS 原始研究 `Primate gut microbiota induce evolutionarily salient changes in mouse neurodevelopment`（DeCasien et al., 2026；doi:`10.1073/pnas.2426232122`）。
- 6 门审核结论为 A，但按 `gut-microbiome encephalization window` 降级写入：核心新增量不是“肠菌决定智力”，而是不同灵长类来源的微生物组可将无菌小鼠脑基因表达推向不同的能量代谢与神经发育轨道。
- 已在 `Neuroscience/SRT_Neuro_08_Immune_Dist.md` 新增 `3.2.3a Gut-Microbiome Encephalization Window`，把现有 `L_2^{neural} \leftarrow L_2^{gut}` 推进一步，写成 `L_2^{gut} -> \theta_{metabolic}^{neurodevelopment} -> L_1^{brain}` 的代谢先验注入链。
- 已明确保留边界：当前仍是 germ-free mice + 少量 primate species 的 proof-of-principle；主要是 pathway / gene-expression 层结果，不等于 cognition 被跨物种复制；也不能直接锁定神经发育障碍病因。
