# 2026-02-28

- 用户明确了长期工作流：用户提供文本/链接后，助手需从 SRT 角度输出**文件级修改建议 + unified diff**，并持续追加到 `diff.md`。
- 用户要求并确认：`SRT 相关度/理论映射/理论引入/实验引入` 作为背景，不应主导输出结构；主结构应是 Target Files + Patch。
- 用户反复强化规范：
  - 引入外部内容时必须提取**具体分类/层级**并映射到 d-value 区间、能流态、\(\Psi_f\) 状态。
  - 若出现新实证尺度且跨尺度表缺定义，需补 `SRT/Core_Law/SRT_Reference_Scaling.md`（\(\hat{G}_\theta\), \(L_0\), \(L_1\), \(L_2\)）。
  - 新数学术语必须附 `[Lineage/Source]`。
  - 必须写 `## 【理论边界/防误用声明】`，且作为目标 Markdown 文件中的正式 Header（不是仅备注）。
  - 外部公式中全状态/可能性空间符号（如 \(\Omega\), \(S\)）在 SRT 写入中统一映射为 \(L_0\)。
- 已创建并持续迭代 skill：`skills/collab-brief-zh/`，并反复打包 `skills/dist/collab-brief-zh.skill`；该 skill 现用于规范上述输出。
- 已完成 SRT 文档体系一轮主线建设：
  - `SRT/OPTIMIZATION_BACKLOG.md`
  - `SRT/D_VALUE_ALIGNMENT.md`
  - `SRT/EXPERIMENT_PRIORITY_MATRIX.md`
  - `SRT/EXPERIMENT_TEMPLATE.md`
  - `SRT/MEASURE_MAP.md`
  - `SRT/START_HERE_1H.md`
  - `SRT/STYLE_GUIDE.md`
  - `SRT/FAQ_CRITICAL.md`
  - 并补写 `SRT/Core/SRT_Core_01_Axioms.md`（A7-A12 Part B 首轮）。
- `diff.md` 已成为当日核心增量工作文件，已写入多条材料的 SRT 文件级补丁（Quanta、Popular Mechanics、Neuroscience News、用户提供 Astera 长文等）。

- 晚间发生重大策略变更：用户明确计划 **1 和 3 不再走 `diff.md`**，改为“**先审核必要性（A/B/C）→ 通过后直接修改 SRT 正文**”。
- 已落地执行总控与五条流水线文档体系（并接入 index/manifest）：
  - `_SRT_EXECUTION_PLAN.md`
  - `_SRT_PAPER_PIPELINE.md` + candidates/draft/checklist
  - `_SRT_SIGNAL_PIPELINE.md` + `_SRT_SIGNAL_LOG.md`
  - `_SRT_GOVERNANCE_PIPELINE.md`
  - `_SRT_MEDIA_PIPELINE.md` + `_SRT_MEDIA_QUEUE.md`
  - `_SRT_OPERATIONS_SCHEDULE.md`
- 已将论文候选池与媒体发布队列填入首批 active 条目（论文3题、媒体3题）。
- 文档治理在今晚完成“大规模工程化 + 减肥”：
  - 建立并跑通检查脚本：frontmatter/link/symbol/quality/explainability。
  - 完成边界声明覆盖与解释链审计流程搭建；后进入“减肥模式”并清理注入式 explainability 块、重复边界块、重复分隔与空行。
  - 新增减肥日志：`_SRT_SLIMMING_CHANGELOG_2026-02.md`。
- 用户确认减肥策略组合：`B + C + A`（去注入+去重复、全量瘦身、小步提交）。
- 关键内容融入（按新规则先审后改正文）已执行：
  1) Essentia “controlled hallucination” 链接：审核 A，融入 `Neuroscience/SRT_Neuro_Experiments.md`（新增 Ax-EXP-16 / T-EXP-4），并记录 signal log。
  2) Quanta fully quantum complexity 链接：审核 A，融入 `Physics/SRT_Phys_09_Formalism_Ext.md`（新增 Def-FQC-1 / Ax-FQC-1 / T-FQC-1），并记录 signal log。
  3) 用户附件 PDF（Fisher metric manuscript）：审核 A，融入 `Physics/SRT_Phys_09_Formalism_Ext.md`（§12.8, Ax-IG-1b / T-IG-5 / Def-IG-2）与 `SRT_EXP_MEASURE_MAP.md`（\Psi_f 信息几何 proxy），并记录 signal log。
- 持续偏好确认：用户喜欢“继续直接做”，无需频繁确认；每轮完成后要 git commit。
