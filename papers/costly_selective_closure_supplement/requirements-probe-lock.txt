# Exact locked environment for the COMMITTED common-state probe
# (results/common_state_probe.json and figures/figure4_*).
#
# The probe retrains policies with REINFORCE; its per-seed trajectories are
# deterministic only WITHIN a fixed software environment. Reproducing the
# committed probe numbers bit-for-bit requires this exact stack:
#
#   Python   3.14.0   (CPython, arm64 / Apple Silicon — macOS-15.7.4-arm64)
#   platform macOS-15.7.4-arm64-arm-64bit-Mach-O
#
# Different Python/NumPy versions or a different CPU architecture (e.g. x86_64)
# may cause trajectory-level divergence even with identical seeds. For that,
# use requirements.txt (the broad minimum: numpy + matplotlib) and expect
# qualitative — not bit-identical — reproduction.
#
# Recreate:
#   python3.14 -m venv venv && source venv/bin/activate
#   pip install -r requirements-probe-lock.txt
#   python run_common_state_probe.py

numpy==2.5.1
matplotlib==3.11.0

# transitive (matplotlib) — pinned for completeness
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
packaging==26.2
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
