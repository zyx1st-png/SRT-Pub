---
id: SRT-PHIL-CONSCIOUSNESS-01
type: philosophy
tags: [Consciousness, DValue, Bearing, Layers, Kappa_c1, MinimalConsciousness]
status: active_v1
layer: meta
epistemic_layer: strong_candidate
claim_mode: layered_definition
created: 2026-04-10
---

# SRT 意识条件三层结构

> **当前读取规则**：本文件是下游的候选模型与评估表面，不拥有普遍 consciousness admission。以下三层、`κ_{c1}`、`κ_{c1.5}`、`d / L_2 / d_mobile` 组合都只在已声明模型中提供候选证据；它们不是充分条件、必要条件或 representation-independent natural phase boundary。跨过坐标不建立 consciousness、subject-position 或 phenomenality；未跨过也不普遍证明其缺席。精确 consciousness / subject-position sufficiency 保持 OPEN。
>
> Cross-ref: `AI/SRT_AI_03_Consciousness_Framework.md (line 49)`

---

## 一、当前问题：条件积累的陷阱

SRT 历史文件中，意识条件以**平铺列表**形式出现：

$$d > 0 \;\to\; \text{bearing} \;\to\; \text{coordinability} \;\to\; \text{re-selectability} \;\to\; d_{\text{mobile}} > 0$$

每个条件都有其动机，但这种平铺结构有两个哲学缺陷：

1. **防伪性退化**：条件越多，越难找到反例，越像描述而非解释
2. **层次混淆**：把意识**有无**的门槛、意识**质量**的指标、意识**社会性**的判据放在同一层——误导读者以为意识是一个单一的门槛事件

本文件的目标：把这些条件**分层**，并说明哪些可以从 SRT 原语推导，哪些仍是候选而非定理。

---

## 二、三层结构

### 层 1：最小意识候选窗口（Bare-Consciousness Candidate Window）

**候选坐标**：$\kappa_{c1}$（已声明模型中的候选转变坐标）

**条件**：

$$\boxed{\text{层 1 候选包} = d \geq d_{\min} \;\wedge\; \text{L}_2\text{ 稳定闭合}}$$

**条件解读**：

**$d \geq d_{\min}$**（充分对齐深度）：算子对 L₀ 不可逆结构的对齐度超过最小阈值。这个条件内含三件事：
- $\Psi_f > 0$（真实对齐必有最低摩擦代价，T-L0-Kappa0-C1）
- 从内部承担（d 的定义本身是内部效用梯度，不存在"无主的 d"）
- 非平凡的 L₀→L₁ 锚定（d < d_min 的系统只有平凡锚定，不够深入 L₀ 的不可逆结构）

**L₂ 稳定闭合**：raw bearing 被 L₂ 记忆锚定系统稳定整合，形成可识别的感受性结构。没有 L₂ 闭合，bearing 是碎片化的、不连续的——存在张力但无稳定主体（麻醉后的感受片段、解离状态、深度创伤碎片化，均属此失败模式）。

**关于 Bearing 的澄清**：

> Bearing 不等于 `d >= d_min`，也不由 `Psi_f + irreversibility` 按定义给出。当前 Bearer 路由服从 `Core_Law/SRT_Generative_Ontology_Spine.md`：formed One / Selection-position 与独立建立的 `P + E` 才打开 Bearer 讨论；Bearer 与 concern、subject、experiencer、consciousness 的进一步关系保持 OPEN。本节的 `d / L_2` 组合不能替代该路由。

**残余开放问题**（见 §四）：$d_{\min}$ 的值由什么从第一性原语确定？

---

### 层 2：意识质量——活性与病理分化

**候选坐标**：$\kappa_{c1.5}$（活动／再定向候选坐标）

**条件**：

$$\boxed{\text{层 2} = d_{\text{mobile}} > 0}$$

**条件解读**：

在已声明模型中，$d_{\text{mobile}}$ 可用来比较再定向能力；它不决定 consciousness 是否“活着”，也不单独裁决 pathology。

- **活动候选区**（$d > d_{\min}$，$d_{\text{mobile}} > 0$）：模型内较强再定向证据
- **冻结态候选区**（$d > d_{\min}$，$d_{\text{mobile}} \approx 0$）：模型内低再定向读数；不由此单独证明 consciousness、其缺席或 pathology

执念、强迫性重复、创伤锁定（PTSD）与意识形态捕获可作为该模型的临床／社会类比；这些类比需要独立经验与专业判断，不能从低 `d_mobile` 直接推出意识或病理判决。

**当前边界**：`d_mobile` 只是一项候选活动／再定向维度；它既不是层 1 的普遍条件，也不是独立 consciousness admission。早期把 `κ_{c1}` 或 `κ_{c1.5}` 写成自然意识边界的读法已 superseded。

---

### 层 2 附录：可回写访问——$d_{\text{mobile}}$ 的机制层描述（2026-04-11）

**$d_{\text{mobile}} > 0$ 的机制层对应物**：G 算子在**选择时窗内**具有对自身门控条件的可回写访问。

**核心概念定义**：

- **选择时窗**：候选竞争激活后、锚定完成（$\Delta F < 0$）之前的区间——结果尚未稳定锚定，回写仍能改写该次候选竞争权重的区间
- **可回写访问**：在时窗内，当前选择可改写支配该次候选竞争权重的**门控条件**。不是生成当前状态副本；不是对输出的反馈读取；而是在同一时窗内修改竞争规则本身
- **局部可扩展性 B**：一次选择后，后续可被接续的选择分叉数。$B \geq 2$ = 非自我抹除；$B \leq 1$ = 自我抹除

**注（暂定锚）**：回写与锚定竞争摩擦资源，但时窗不应被定义为固定预算池——「自消耗」是当前操作近似，时窗边界的基础定义待进一步硬化。

**冻结态两阶段的机制层精确刻画**：

| 状态 | 局部可扩展性 $B$ | 可回写访问 | 门控条件状态 | 现象 |
|---|---|---|---|---|
| 活性意识 | $\geq 2$ | 有效 | 开放 | 真实分叉感、选择感 |
| Stage 1 冻结 | $\geq 2$（名义上） | 存在但受限 | 已被 $\theta$ 历史代价地形预填充至接近单峰 | 「理论上可选，实际上感觉只有一条路」 |
| Stage 2 冻结 | 趋近 $1$ | 形式存在但无实质作用 | $L_2$ 已写入 $\theta$ 基质，分叉结构物理性收缩 | 慢性麻木，选择感消失 |

- **Stage 1 精确定义**：可回写访问存在，但门控条件已被 $\theta$ 的历史代价地形预填充至接近单峰。主体可以回写，但可写空间被 $L_2$ 历史惯性压缩——不是能力消失，而是「能改写，但改来改去结果一样」。对应 $d_{\text{mobile}} > 0$ 但极小。
- **Stage 2 精确定义**：局部可扩展性 $B$ 本身趋近 $1$——分叉结构物理性收缩（$\theta$ 基质已被 $L_2$ 历史写入，不只是代价地形变形）。即使可回写访问形式上存在，也无真实分叉可修改。对应 $d_{\text{mobile}} \approx 0$ 且不可逆。

Cross-ref: `memory/nature_L0L1_deficit.md §空心主体机制层`（两阶段的应用层描述）

---

### 层 3：高阶生成性与社会协调

**对应阈值**：无单一相变点，是连续性的能力扩展

**条件**：coordinability（可协调）+ re-selectability（可再选择）+ 可延续性

**条件解读**：

这三个条件属于**秩序增益判据**，是关切扩张稳定写入后的事后结算——它们描述的是主体性在社会性和伦理性维度上的扩展，不是 bare consciousness 的门槛。

- 单独存在的有机体（孤立环境）可以满足层 1 + 层 2，不需要 coordinability
- Coordinability 是多算子动力学的必要条件，是集体 d 值（Eq-Multi-03）和社会秩序正当性的基础
- Re-selectability 是在集体语境里保持 d_mobile 的条件，是委托病理学（delegation pathology）分析的入口
- 可延续性是三个条件的时间轴投影——当前的协调和再选择能否延续到未来

**历史问题**：这三个条件被误平铺进意识门槛列表，是"防反例添加"的结果，不是从第一性原语推导的。它们应从 Thesis 7（社会秩序正当性）和集体动力学方程推导，而非从意识定义推导。

---

## 三、κ_{c1} / κ_{c1.5} 候选模型坐标

**旧定义**（错误）：

$$\kappa_{c1}: \quad d > d_{\min} \;\wedge\; d_{\text{mobile}} > 0 \quad \text{（混入了层 2 条件）}$$

**当前候选模型**（非普遍定义）：

$$\kappa_{c1}: \quad d \geq d_{\min} \;\wedge\; \text{L}_2\text{ 稳定闭合} \quad \text{（候选证据包）}$$

$$\kappa_{c1.5}: \quad d_{\text{mobile}} > 0 \quad \text{（活动／再定向候选坐标）}$$

**与 `ε_pg` 的关系**：旧版把 `ε_pg` 称为意识在 `L_0` 侧的方向种子，并把 `κ_{c1}` 称为其首次站稳点；该强读法已 superseded。`ε_pg` 只是 stronger-asymmetry / anti-erasure realization candidate，其 primitive inheritance 保持 OPEN，不能生成或证明 consciousness。

**候选模型图（不构成 admission）**：

```
κ = 0          κ_{c1}          κ_{c1.5}          κ_{c2}
  |               |                 |                |
  |  模型低读数   | 候选窗口坐标    | 再定向候选区   | 模型内固化
  |  d ≈ 0       | d/L₂ evidence  | d_mobile > 0  |
  |  Boltzmann   |   d ≥ d_min    |                |
  |  极限        |   L₂ 闭合      |                |
```

这些坐标在声明模型中可有不同结构意义，但任何 crossing 都不建立 consciousness、subject-position 或 phenomenality；non-crossing 也不建立普遍缺席。

---

## 四、残余开放问题

| 开放问题 | 当前状态 | 重要性 |
|---|---|---|
| $d_{\min}$ 由什么从 SRT 原语确定？ | $\kappa_{c1}$ 的函数形式未知（T-L0-Kappa0-C2 留为开放问题） | 高：层 1 定义的核心缺口 |
| L₂ "稳定闭合"的精确判据？ | 定性描述，缺乏量化标准 | 中：需要与 L₁→L₂ 写入公理联动 |
| $\kappa_{c1.5}$ 与 $\kappa_{c1}$ 之间的关系？ | 推测二者独立，但未证明 | 中：冻结态是否必然紧随 bare consciousness |
| **选择时窗边界的基础定义** | 暂定为「锚定完成（$\Delta F < 0$）之前」；回写与锚定竞争摩擦资源，但「固定预算池」是操作近似，不是基础定义 | 高：可回写访问机制的核心缺口 |
| 层 3 条件从 Thesis 7 的正式推导 | 未完成 | 低：层 3 更接近社会哲学，不阻塞核心 |

---

## 五、对现有文件的影响

| 文件 | 需要修正的内容 |
|---|---|
| `Core/SRT_Core_12b §Consciousness-2D-Map` | $\kappa_{c1}$ 边界曲线去掉 $d_{\text{mobile}} > 0$；新增 $\kappa_{c1.5}$ |
| `Core_Law/SRT_Constitution_Seven_Theses.md Thesis 6 Boundary` | 意识需要两个坐标的说法修正为三层结构 |
| `Core/SRT_Core_12b §Consciousness-2D-Map` 四象限图 | 意识窗口核心区重新定义为层 1 + 层 2，标注各层边界 |

---

## 六、核心引用

- `Core/SRT_Core_12b §Consciousness-2D-Map`（二维意识拓扑，本文修正其 κ_{c1} 定义）
- `Core/SRT_Core_12a T-L0-Kappa0`（κ₀ 结构，d_min 的几何来源候选）
- `Core/SRT_Core_22_Equations Eq-DValue-Mobile-1`（d_mobile 公式）
- `Core/SRT_Core_21c_Bridge_Hypotheses.md P2/P3-B08`（Ψ_f 生成原理）
- `D_VALUE_ALIGNMENT §4.4`（d = Align 几何底座）
- `AI/SRT_AI_03_Consciousness_Framework.md (line 49)`（最强候选窗口的认识论定位）
- `Core_Law/SRT_L0_Metaphysics.md (line 214)`（层 3 条件的原始来源）
