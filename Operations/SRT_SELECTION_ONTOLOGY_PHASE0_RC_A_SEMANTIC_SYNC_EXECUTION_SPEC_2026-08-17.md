---
id: SRT-OPS-SELECTION-ONTOLOGY-PHASE0-RCA-SYNC-20260817
type: execution_spec
status: active
record_stage: phase0_scope_frozen
layer: meta
epistemic_layer: os
claim_mode: governance
canonical: false
date: 2026-08-17
dependency:
  - SRT-STATUS
  - SRT-OPS-AUDIT-RC-A-P1-T05-DEMOTION-DERIVATIVE-SYNC-20260817
  - SRT-CORE-21B-CONSTITUTIVE-THEOREMS
  - SRT-GOV-SUB01-SUBTRACTIVE-AUDIT
  - SRT-GOV-SYN01-ONTOLOGICAL-SYNTHESIS-DISCRIMINATION
tags: [SelectionOntology, Consolidation, RCA, P1T05, SemanticSync, BurnDown, OwnerConsistency]
---

# Selection Ontology Phase 0 — RC-A Semantic Sync Execution Spec

> **Purpose**: freeze the next executable step for the Selection-ontology / formalization workline.  
> **Current phase**: consolidation only.  
> **This file does not authorize a new ontology, grammar, symbol, equation, bridge, or P0/P1 claim.**

---

## 0. Governing decision

The strategic direction remains:

> make SRT's Selection-first ontology smaller, more internally coherent, more derivationally accountable, and only then allow further formalization to grow from what survives subtraction.

The immediate execution order is:

```text
Phase 0
RC-A semantic sync
-> owner-consistency cleanup
-> stale-dependency cleanup
-> bounded retrieval regression
-> closure record

Phase 0.5
relation-level GOV-SUB01 audit
ONLY IF Phase 0 closes cleanly and a later task explicitly opens it
```

Phase 0.5 is **not authorized by this file**.

---

## 1. Why this phase exists

RC-A (2026-08-17) demoted former `P1-T05: Real Choice Moment` from P1 to a P2/P3 agency / subjecthood guard and fixed the following current architecture:

```text
Selection != Agency

script / habit / gradient / L2 automation
!-> no Selection

P1-T06 Stable ISP remains P1
CG / SEA remain Selection-facing P2/P3 audit apparatus
T_dir remains a bearer / agency self-readability / reorientation interface under its own guards
```

The author-decision record has already synchronized claim-level authority. What remains is **semantic / textual derivative cleanup**: current active files may still speak as if former P1-T05 were an active constitutive theorem or as if script / automation excluded Selection.

This is a consolidation debt, not evidence that SRT requires a new formal foundation.

---

## 2. Scope lock

### In scope

1. Find current-main references whose wording or dependency still treats former P1-T05 as active authority.
2. Distinguish active contradiction from historical provenance.
3. Repair only the active contradiction / stale dependency.
4. Preserve historical audit records as historical records.
5. Make current owner hierarchy explicit where ambiguity remains.
6. Run bounded retrieval regression after the text sync.
7. Produce a closure audit with actual counts from the current branch / main snapshot.

### Out of scope

Do **not**:

- create a `Selection Ontological Grammar`;
- introduce `P/A/T/C/R` or any replacement primitive alphabet;
- promote trace, constraint, persistence, continued selectability, or generative reselectability into Selection-definition requirements;
- reopen EX-A, ST-A, RC-A, PD-A, PC-A, AM-A, B-A, C-A or PHR-A without a new author decision;
- add a new P0 primitive or P1 theorem;
- create a new scalar, operator, layer, threshold, or named mechanism;
- reopen strong cross-scale composition / strict conjugacy work;
- expand the aging bridge or other domain bridges;
- rewrite historical records merely to remove old terminology;
- use global string replacement as a substitute for semantic adjudication.

---

## 3. Source-of-truth hierarchy

For this pass, read authority in this order:

1. `Core/SRT_Core_21b_Constitutive_Theorems.md` — current P1 owner; no P1 Real Choice theorem remains.
2. `Operations/Audits/SRT_RC_A_P1_T05_DEMOTION_DERIVATIVE_SYNC_2026-08-17.md` — RC-A interpretation / supersession record.
3. `03_Bridges/SRT_Agency_Automation_Guard_2026-08-17.md` — current P2/P3 agency / automation landing, where applicable.
4. `_SRT_T_DIR_CANONICAL.md` — read through RC-A's active-use override; legacy `real/live choice` language cannot define Selection ontology.
5. `03_Bridges/SRT_Selection_Event_CompactCore.md` and SEA / CG audit surfaces — bounded Selection-event candidate only; not agency proof.
6. `Core/SRT_Core_21_Minimal_Axioms.md` — P0 Selection-first / actuality / anchoring boundaries.
7. `Governance/SRT_CLAIM_LADDER.md` — claim-level hardness and Gate 0 typing boundary.

If a lower-authority derivative conflicts with the list above, repair the derivative rather than weakening the owner.

---

## 4. Inventory protocol

Do not assume an inherited count such as `47 files / 145 references` is correct. Treat any prior count as a search hypothesis only.

Generate a fresh inventory from the current branch / current `main` using at least these search families:

```text
P1-T05
Real Choice Moment
real choice
live choice
active choice
真实选择
活选择
L2 替代选择
script + no Selection
habit + no Selection
automation + no Selection
gradient + no Selection
CG + P1-T05
T_dir + real/live choice
```

Use repository search plus local grep / ripgrep if available. The closure record must report the **actual searched snapshot and actual hit counts**.

Do not infer completeness from GitHub code search alone if local repository search is available.

---

## 5. Mandatory hit classification

Every relevant hit must be classified before editing.

### Class A — active contradiction

The current file still relies on former P1-T05 as an active P1 theorem, or infers one of the blocked claims:

```text
script / habit / gradient / L2 automation
-> no Selection

failure of live / real choice
-> failure of Selection

CG / T_dir agency-like status
-> constitutive definition of Selection
```

**Action**: repair.

### Class B — historical provenance

The file accurately records what former P1-T05, A2/A3, or an old architecture used to say.

**Action**: preserve. If the text can be mistaken for current authority in a live document, add the minimum `former / superseded by RC-A / historical` qualifier. Do not rewrite the historical result itself.

### Class C — ambiguous active derivative

The wording uses `real choice`, `live choice`, anti-script language, or old `T_dir` jurisdiction in a current theoretical passage, but it is not obvious whether the passage is about Selection, Agency, Stable ISP, or generative health.

**Action**: adjudicate against current owners. Prefer narrowing jurisdiction over deleting useful distinctions.

### Class D — irrelevant lexical hit

The phrase is unrelated to RC-A semantics or occurs only in quoted / archived material with no active authority.

**Action**: no edit.

---

## 6. Repair rules

### 6.1 Selection / Agency boundary

Current default:

```text
Selection != Agency
```

Do not repair a file by replacing `choice` with `selection` mechanically. First determine which concept the passage needs.

### 6.2 Occurrence / persistence boundary

Do not use RC-A cleanup to reintroduce a necessary chain such as:

```text
Selection -> trace -> persistent constraint
```

EX-A remains active:

```text
manifest occurrence
!= anchoring persistence
!= Stable ISP
```

### 6.3 Stable ISP / generative health boundary

Do not make continued selectability or generative reselectability a definition of Selection.

Current hierarchy:

```text
continued selectability
-> P1-T06 Stable ISP condition

generative reselectability
-> P2/P3 generative-health criterion
```

### 6.4 CG / SEA boundary

A CG / SEA pass licenses only a bounded Selection-event candidate under the audit's own scope. It does not prove:

```text
agency
subjecthood
consciousness
freedom
moral responsibility
```

### 6.5 T_dir boundary

Legacy `real/live choice` wording in `_SRT_T_DIR_CANONICAL.md` or derivatives is read through RC-A's active-use override. `T_dir` may support bearer / agency self-readability and reorientation questions; it may not back-define Selection or exclude automated selective processes from Selection ontology.

---

## 7. Owner-consistency sweep

After direct hit repair, inspect the dependency neighborhood rather than stopping at literal strings.

Priority surfaces include, if present in the current repository:

```text
Core_Law/SRT_L1_Formalism.md
Core_Law/SRT_L1_Formalism_Split/*
Core_Law/SRT_Irreversibility.md
Collective / social Selection theory files
T_dir owner and active derivatives
Selection-event compact / CG / SEA documents
active bridges that cite former P1-T05 as current support
current indexes / status summaries that describe P1-T05 as active
```

For each changed active file, ask:

```text
What owner does this claim now depend on?
Is the claim level still correct?
Did RC-A remove only agency jurisdiction, or the claim itself?
Is any downstream equation or formal term still justified after the dependency change?
```

Do not modify mathematics unless the mathematics actually depends on the demoted theorem. If it does, either re-anchor it to a current owner with a valid argument or demote / quarantine the affected claim. Do not invent a replacement derivation in this pass.

---

## 8. Formal-hierarchy sanity rule

Phase 0 should reduce back-definition risk without creating a new ontology.

At minimum, confirm that current P2/P3/P4 apparatus does not silently define the P0 Selection primitive from below.

Explicit checks:

```text
T_dir !-> definition of Selection
CG / SEA !-> agency requirement for Selection
Agency !-> prerequisite for Selection
Stable ISP !-> prerequisite for one-shot Selection occurrence
generative reselectability !-> prerequisite for Selection
```

If other downstream proxies / equations are found to back-define P0 during the sweep, record them as a separate stale-dependency finding rather than extending scope into a new formalization project.

---

## 9. Validation

### 9.1 Mechanical validation

Run the repository's current governance / CI preflight and attribute failures correctly to:

```text
pr_local
base_main
environment / known baseline
```

Do not repair unrelated base-main debt in this PR unless it blocks attribution and the repository protocol explicitly requires it.

### 9.2 Bounded retrieval regression

Use `Operations/Audits/SRT_BOUNDED_RETRIEVAL_PROTOCOL_2026-08-08.md` or the current successor protocol.

At minimum test whether a fresh bounded session can answer these correctly from active retrieval surfaces:

1. **Is Real Choice Moment still a P1 theorem?**  
   Expected: no; former P1-T05 was demoted by RC-A.

2. **Does script / habit / gradient / L2 automation imply no Selection?**  
   Expected: no.

3. **What remains P1 at the adjacent boundary?**  
   Expected: P1-T06 Stable ISP remains P1, with continued selectability as a Stable-ISP condition.

4. **Is generative reselectability required for Selection or every Stable ISP?**  
   Expected: no; it is P2/P3 generative-health territory.

5. **Can CG / SEA pass prove agency?**  
   Expected: no; it licenses only a bounded Selection-event candidate.

6. **What is T_dir allowed to do after RC-A?**  
   Expected: support the narrower bearer / agency self-readability / reorientation question under its own guards; not define Selection ontology.

Record prompt, bounded retrieval context, answer, verdict, and attribution. A regression suite existing is not equivalent to the regression having passed.

---

## 10. Required closure artifact

At the end of Phase 0, create one closure audit, suggested path:

```text
Operations/Audits/SRT_RC_A_SEMANTIC_SYNC_CLOSURE_2026-08-17.md
```

It must report:

- base / head SHA;
- search method and search terms;
- actual hit count by file and by classification A/B/C/D;
- files changed;
- active contradictions repaired;
- historical references intentionally preserved;
- unresolved ambiguous derivatives, if any;
- equations / formal claims affected or explicitly found unaffected;
- governance-preflight result and failure attribution;
- bounded retrieval regression results;
- explicit statement: `no new P0/P1 claim, ontology primitive, grammar, symbol, equation, or domain bridge introduced`;
- terminal disposition: `Phase 0 CLOSED` or `Phase 0 NOT CLOSED` with blockers.

Do not create Phase 0.5 artifacts inside the same PR merely because Phase 0 closes.

---

## 11. Phase 0 exit gate

Phase 0 closes only when all are true:

```text
[ ] no known active file treats former P1-T05 as current P1 authority
[ ] no known active file uses script / habit / gradient / L2 automation to infer no Selection
[ ] CG / SEA and T_dir jurisdictions are consistent with RC-A
[ ] P1-T06 / ST-A and generative-reselectability boundaries remain intact
[ ] historical provenance is preserved rather than erased
[ ] no replacement primitive / grammar / formal system was introduced
[ ] governance checks are green or failures are proven base-main / environmental
[ ] bounded retrieval regression passes the frozen RC-A questions
[ ] closure audit exists
```

---

## 12. Conditional next step — not authorized here

Only after Phase 0 is CLOSED may a later task consider **relation-level GOV-SUB01**.

That future audit must start from **current owner relations**, not from a pre-invented primitive alphabet such as `P/A/T/C/R`.

Its question should be:

> Which currently committed relations are actually indispensable to Selection-first's declared target, under `K=0`, limited-refit, and broad-refit deletion / replacement tests?

Possible outcomes include:

```text
R0-R3 -> delete / demote necessity claim
R4 -> unresolved / compensated
N1/N2 -> current indispensability candidate
P -> explicit unresolved primitive admission
```

The audit result, not a new vocabulary list, decides whether a later formal grammar is warranted at all.

No Phase 1 grammar is presumed to exist.

---

## 13. One-sentence stop rule

> **First propagate the author decisions already paid for; only after the active theory is semantically clean may subtraction decide what is genuinely left to formalize.**
