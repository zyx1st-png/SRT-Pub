---
id: SRT-SELECTION-TOTALITY-WAVE-D1-PATCH-SPEC-20260915
type: proposal
status: active
layer: operations
epistemic_layer: os
claim_mode: proposal
canonical: false
date: 2026-09-15
---

# Wave D1 patch spec — authority / registry truth-up

## 0. Execution contract

This specification is the single semantic source for D1 mechanical landing.

Authorized semantic files are exactly:

```text
_SRT_SYMBOL_TABLE.md
_SRT_CROSS_DOMAIN_MATRIX.md
CANONICAL_REGISTRY.md
```

Do not edit D2/D3 consumers in the same semantic commit.

Prefer local Codex / a normal full working tree. Do not modify workflows or lower governance checks.

## 1. Fixed upstream authority

D1 must summarize, not replace, the already-landed architecture:

```text
Selection = reality's one generative mode;
Selection-monist != Selection-flat;
primitive Selection is normatively contentless;
existence = Selection under determinate actuality;
history = prior Selection remaining materially effective where retained efficacy is established;
primitive Selection != formed G-hat;
L0/L1/L2 are analytic/model-facing aspects, not three ontic substances;
kappa_0 / epsilon_pg / B-count do not inherit primitive status from O0;
T_dir requires an independently typed, declared downstream direction signal;
T_dir > 0 does not establish good / legitimacy / generative health / O2-M;
O2-M remains OPEN.
```

## 2. `_SRT_SYMBOL_TABLE.md`

### D1-S1 — L0 row

Replace the warehouse/set reading.

Target meaning:

```text
L0
= model-facing notation for Selection's open / non-preclosed aspect
  before determinate actuality in the representation at hand.
```

Required guard:

```text
L0 != pregiven completed set / inventory / warehouse of all possibilities.
```

Scope note must route metaphysical authority to `Core_Law/SRT_L0_Metaphysics.md` and `Core_Law/SRT_Generative_Ontology_Spine.md`.

Do not invent a new symbol or new positive metaphysical object.

### D1-S2 — L1 row

Retype as the determinate-actuality / manifest aspect of Selection.

Do not say L1 is a product selected by a prior chooser.

Preserve event/persistence distinction:

```text
manifest actuality != anchoring persistence.
```

### D1-S3 — L2 row

Retype the universal summary away from an always-existing convergence-domain substance.

Target meaning:

```text
L2
= model-facing notation for retained / sedimented historical efficacy
  and stabilized constraints where such retention is established.
```

Guard:

```text
Selection occurrence -/-> L2-grade retained history automatically.
```

### D1-S4 — G-hat row

Retype `G-hat` as a formal / formed / model-level role-carrier for a Selection realization, not primitive Selection itself.

Required guard:

```text
primitive Selection != G-hat;
G-hat is not a prior chooser / cause of actualisation.
```

If an `L0 -> L1` mapping is retained, state that it is a declared formal/model representation, not the metaphysical definition of primitive Selection.

### D1-S5 — T_dir row

Atomic definition target:

```text
T_dir
= v0 operational proxy / constrained readability-reorientation functional
  for a formed system's access to an independently typed,
  declared direction signal.
```

Scope guards:

```text
no declared direction -> T_dir not admitted / undefined for that claim;
T_dir > 0 -/-> good / legitimacy / generative health / O2-M;
not valence / reward / confidence / semantic coherence.
```

### D1-S6 — kappa_0 row

Do not delete the symbol. Retype its registry status.

Suggested name:

```text
Kappa-0 Curvature / Anisotropy Floor Candidate
```

Target meaning:

```text
conditional model / realization candidate for a minimum curvature,
anisotropy floor or related non-flat realization when explicitly declared.
```

Required guards:

```text
kappa_0 is not primitive by inheritance from O0;
necessity / exact relation to primitive non-flatness = OPEN;
kappa_0 does not supply a universal value/order direction;
kappa_0 does not cause first Selection.
```

Delete/retype universal claims such as:

```text
structural prerequisite for all Selection;
provides directionality bias for all selection operators;
Psi_f^min = f(kappa_0) as universal core identity.
```

A model-local formula may be referenced only as a conditional realization.

### D1-S7 — kappa(t) row

If retained, scope it explicitly to declared curvature/co-evolution models.

It must not imply a universal evolving L0 curvature field.

### D1-S8 — epsilon_pg row

Keep namespace registration but remove primitive admission by registration.

Suggested name may preserve lineage, e.g.:

```text
Proto-Gradient / Minimum-Non-Neutrality Realization Candidate (legacy namespace)
```

Target meaning:

```text
registered symbol for a stronger asymmetry / anti-erasure realization candidate;
its exact relation to primitive Selection / O0 remains OPEN unless separately re-established.
```

Required guards:

```text
registration != primitive admission;
epsilon_pg does not define O0;
B>=2 > B<=1 is not a primitive Selection preference;
epsilon_pg does not supply universal T_dir direction or value.
```

### D1-S9 — Usage Rule 15

Update the kappa namespace rule without changing the separation between:

```text
kappa_0 / kappa(t)
vs
kappa_c1 / kappa_c1.5.
```

But remove the statement that kappa_0's structural-prerequisite role is retained.

New rule must defer admission to current L0 owner + Gate 0 and state Class-B / model-realization status at current strength.

### D1-S10 — Usage Rule 16

Preserve its pointer-only role. Add only enough truth-up to make explicit that current Gate 0 places `kappa_0`, `epsilon_pg`, B-count orderings and model gradients in Class B / admission-gap unless independently established.

Do not turn Rule 16 into a second copy of Claim Ladder §0A.

### D1-S11 — Usage Rule 17

Retain the no-Shoshin-alias governance rule, but remove:

```text
epsilon_pg remains the registered L0 minimum-non-neutrality postulate
```

Replace with:

```text
epsilon_pg remains a registered namespace / lineage symbol;
registration does not establish primitive admission or a Shoshin precursor.
```

## 3. `_SRT_CROSS_DOMAIN_MATRIX.md`

### D1-M1 — status guard

Strengthen the existing file-role guard minimally:

```text
This matrix summarizes current owner-approved usage;
it cannot upgrade a symbol / proxy / bridge to primitive status.
```

### D1-M2 — d-value §1.1 explanatory coordinate

Remove the derivational phrase:

```text
minimum non-neutrality unfolds into concern / stake ...
```

Target:

```text
d is an explanatory coordinate for stake-coupled concern,
consequence return and future-selectability effects in formed systems;
this matrix does not derive d from O0 / epsilon_pg / primitive Selection.
```

### D1-M3 — T_dir table

Update every domain row so that T_dir presupposes a declared direction source.

Core row target:

```text
formed-system self-readability / reorientation relative to an independently typed declared direction signal.
```

Physics:
- generally not a physics variable;
- physical directionality does not automatically supply the direction.

Neuroscience:
- model-scoped access/reorientation proxies only;
- reportability/action revision may proxy the functional, but direction must be independently typed.

Philosophy/Political:
- readability of declared relational / normative criteria;
- `T_dir` does not validate the criterion or establish legitimacy.

Spirituality:
- declared practice / relational direction readability;
- strong experience does not establish direction truth/value.

AI:
- architecture-state-marked self-monitoring/reorientation relative to declared objective/criterion;
- self-report does not establish T_dir.

### D1-M4 — epsilon table

Core row target:

```text
epsilon_pg = registered stronger-asymmetry / anti-erasure realization candidate;
exact inheritance from primitive non-flatness is OPEN;
ISP anti-closure remains conditional P2/P3 candidate;
epsilon_reg and epsilon_s keep their existing local meanings.
```

Forbidden shortcut must include:

```text
epsilon_pg / B-count preference as primitive Selection direction;
epsilon grounds legitimacy;
all epsilon symbols collapsed together.
```

Domain rows may keep local regularizer / threshold / bridge uses, but cannot back-prove primitive epsilon.

## 4. `CANONICAL_REGISTRY.md`

### D1-R1 — Generative Ontology Spine summary

Replace the stale trunk summary with a compact current routing summary. Suggested form:

```text
primitive Selection = reality's one generative mode
  -> determinate actuality / manifestation
  -> retained historical efficacy where established
  -> recurrent self-conditioning / One formation
  -> formed Selection-position
  -> stronger recurrent standing / Stable ISP
  -> prospective self-exposure / Bearer under P+E
  -> reflexive condition-rewriting / Agency
  -> separately typed normativity routes.
```

Guard:

```text
This is routing compression, not a replacement definition source.
```

Do not turn the registry into a second Spine.

### D1-R2 — T_dir entry

Update the summary to:

```text
T_dir is a v0 operational self-readability / reorientation proxy
relative to an independently typed, declared direction signal
in a formed system / relation / model.
```

Remove the old 'selection order direction' language.

Explicitly state:

```text
no declared direction -> T_dir undefined/not admitted for that claim;
T_dir > 0 does not establish good / legitimacy / generative health / O2-M.
```

### D1-R3 — d / T_dir relationship

Delete the universal statement:

```text
d is necessary for T_dir.
```

Replace with:

```text
exact d/T_dir necessity or sufficiency is model/bridge scoped unless independently established by the owners;
D2 consumer truth-up remains required where older formulas assume the relation universally.
```

### D1-R4 — Cross-Domain Matrix entry

Keep its role as governance usage matrix, but state that it summarizes current owner-approved use and cannot promote `epsilon_pg`, `kappa_0`, T_dir directions or domain proxies to primitive/theory-canonical status by repetition.

### D1-R5 — Core Equations entry

Do not edit Core22 in D1. Add a bounded routing warning only if needed:

```text
local equation semantics remain subordinate to current symbol / owner typing;
Wave D2 will independently review stale kappa/T_dir consumers.
```

Do not pre-judge a D2 equation beyond already-fixed Wave A-C boundaries.

### D1-R6 — Claim Ladder entry

Keep Gate 0 as single authority. It may summarize current result narrowly:

```text
kappa_0 / epsilon_pg / B-count ordering are not primitive by registration/inheritance;
exact stronger-realization relation remains OPEN.
```

Do not copy the whole Class A/B/C table.

## 5. D1 acceptance gates

Return PASS / FAIL with evidence for all gates.

```text
D1-01 L0 registry no longer defines a possibility warehouse.
D1-02 primitive Selection != G-hat in registry language.
D1-03 L2 registry does not imply every Selection produces durable history.
D1-04 kappa_0 registration no longer gives primitive prerequisite/direction authority.
D1-05 epsilon_pg registration no longer establishes B>=2 primitive preference.
D1-06 symbol registration is explicitly separated from primitive admission.
D1-07 T_dir requires independently typed declared direction in Symbol Table.
D1-08 Cross-Domain Matrix no longer derives d from primitive minimum non-neutrality.
D1-09 all T_dir domain rows preserve declared-direction + non-legitimacy boundary.
D1-10 epsilon cross-domain row cannot restore primitive direction or legitimacy.
D1-11 Canonical Registry Spine summary matches Selection-totality without becoming a duplicate owner.
D1-12 Canonical Registry removes universal d -> T_dir necessity.
D1-13 Gate 0 remains the single admission/type authority.
D1-14 kappa/epsilon necessity/inheritance remains OPEN rather than forbidden in kind.
D1-15 no new primitive symbol/layer/claim introduced.
D1-16 only the 3 authorized semantic files changed before derivative closure.
```

Any FAIL => STOP before derivative regeneration and before D2.

## 6. Targeted residual search

After editing, search at least:

```text
Set of all unselected possibilities
Primordial Curvature
structural prerequisite
Provides directionality bias
Psi_f^min
B >= 2
B <= 1
minimum non-neutrality postulate
current selection direction
value / legitimacy direction
d is T_dir
necessary condition
proto-gradient
registered L0
```

Classify every residual as:

```text
current bounded statement
OPEN
model / realization / bridge scoped
historical / lineage
FAIL
```

Do not chase non-authoritative historical files in D1.

## 7. Derivative closure

After D1-01..D1-16 PASS:

1. determine from the live repo whether any of the three files has a registered reading split;
2. if yes, rebuild shard bodies with the official split generator;
3. regenerate official context bundles;
4. run bundle `--check`;
5. run normal Governance Preflight;
6. inspect `git diff --stat` and `git diff`.

No metadata-only fake freshness.

## 8. Stop condition

After D1 lands and CI is green:

```text
STOP
D2 not automatically authorized by a green D1.
```

Return the landed diff and D1-01..D1-16 evidence for independent review.

`MERGE #976: NO`.
