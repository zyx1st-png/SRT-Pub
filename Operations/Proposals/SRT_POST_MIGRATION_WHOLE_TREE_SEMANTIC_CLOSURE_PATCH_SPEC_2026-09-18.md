---
id: SRT-POST-MIGRATION-WHOLE-TREE-SEMANTIC-CLOSURE-PATCH-SPEC-2026-09-18
type: proposal
status: active
layer: governance
epistemic_layer: proposal
claim_mode: exact_patch_spec
canonical: false
date: 2026-09-18
---

# Post-migration whole-tree semantic closure — exact patch specification

## Binding scope

This specification implements the bounded closure identified in:

`Operations/Audits/SRT_POST_MIGRATION_WHOLE_TREE_SEMANTIC_CLOSURE_PRELANDING_2026-09-18.md`.

No new author theory adjudication is required.

Exactly 13 semantic files are authorized:

```text
Core/SRT_Core_12a_Ontology_L0L1.md
Core/SRT_Core_12b_Ontology_L2.md
Core_Law/SRT_Selection_Argument.md
Core_Law/SRT_Constitution_Seven_Theses.md
Philosophy/SRT_Consciousness_Conditions.md
Philosophy/SRT_Subjecthood_Threshold_Interface.md
Philosophy/SRT_Philosophy_Foundations_CompactCore.md
Philosophy/03_Selection_Realism_Layered_Realism_CompactPatch.md
Philosophy/SRT_Soc_03_Institutions.md
AI/SRT_AI_03_Consciousness_Framework.md
AI/SRT_AI_03_Consciousness_Framework_CompactCore.md
_SRT_SYMBOL_TABLE.md
CANONICAL_REGISTRY.md
```

If execution appears to require any additional semantic owner, STOP and report.

---

# Global invariants

## WTC-01 — ontology hierarchy

Preserve:

```text
primitive Selection = one generative mode
Selection-totality != Selection-flatness

formal model / operator / state space / manifold / curvature
  != primitive Selection
G_hat_theta != primitive Selection
kappa0 != O0
kappa0 != hidden cause of primitive Selection
```

## WTC-02 — standing hierarchy

Preserve:

```text
finite positionality -/> One / Bearer / subject
One -/> Stable ISP / Bearer / subject
Stable ISP -/> Bearer / subject / consciousness
Bearer -/> subject / consciousness / phenomenality
d > 0 -/> Bearer / subject
sigma_sr threshold crossing -/> subject admission

exact subject-position sufficiency = OPEN
subject-position <-> Bearer = OPEN
phenomenality = OPEN
```

## WTC-03 — agency boundary

Preserve:

```text
Selection != Agency
Selection -/> Agency automatically
agency sufficiency remains downstream / separately gated
```

Do not reopen the agency family unless a concrete contradiction is found.

## WTC-04 — normativity boundary

Preserve:

```text
primitive Selection != ought
Selection occurrence -/> moral legitimacy
high d -/> goodness / legitimacy / rights / authority
Psi_f != pain / suffering / moral cost
L2 stability -/> legitimacy
occlusion / pathology -/> O2-M verdict
O2-C != O2-A != O2-M
O2-M = OPEN / HOLD
```

## WTC-05 — anti-replacement rule

Do not replace old `d / meta-selection / Psi_f` moral solvers with:

```text
continuability
coordinability
non-export
reselectability
future-selectability
correction / exit / rollback
```

as a new universal N&S moral or political solver.

These may appear only as scoped comparison axes / candidate diagnostics under declared positions, populations, horizons, interfaces and consequence channels.

---

# Semantic Commit A — consciousness / subject family

Exact files:

```text
Philosophy/SRT_Consciousness_Conditions.md
Philosophy/SRT_Subjecthood_Threshold_Interface.md
AI/SRT_AI_03_Consciousness_Framework.md
AI/SRT_AI_03_Consciousness_Framework_CompactCore.md
Core_Law/SRT_Constitution_Seven_Theses.md
_SRT_SYMBOL_TABLE.md
```

Suggested message:

```text
Truth-up post-migration consciousness and subject interfaces
```

## WTC-06 — Consciousness Conditions status

The file must explicitly state that it is a downstream candidate-model / evaluation surface, not the owner of universal consciousness admission.

## WTC-07 — kappa_c1

Replace active readings equivalent to:

```text
kappa_c1 = bare consciousness threshold / phase boundary
d >= d_min + stable L2 closure = bare consciousness
```

with:

```text
kappa_c1 = model-local candidate coordinate / candidate evidence window
the associated d / L2 package is a hypothesis to test
crossing -/> consciousness / subject-position / phenomenality
failure -/> absence of consciousness universally
```

Do not create a new exact sufficiency condition.

## WTC-08 — kappa_c1.5

Replace active readings equivalent to:

```text
kappa_c1.5 = consciousness activity threshold
d_mobile > 0 separates active consciousness from frozen real consciousness
```

with:

```text
kappa_c1.5 = candidate activity / reorientation coordinate
d_mobile may be an operational discriminator in declared models
crossing -/> consciousness proof
non-crossing -/> consciousness absence or pathology by itself
```

## WTC-09 — frozen-state claims

Statements that obsession, trauma, fanaticism or low `d_mobile` are ontologically "real consciousness but frozen" must become candidate model interpretations / clinical-social analogies, not theory-wide verdicts.

## WTC-10 — Bearing

Remove or truth-up any statement that:

```text
Bearing is merely d >= d_min
Bearing is Psi_f + irreversibility by definition
```

Current Bearer routing must defer to the Generative Ontology Spine / formed One + P + E route.

## WTC-11 — epsilon_pg / old L0 consciousness seed

Do not preserve an active claim that `epsilon_pg` is a direction seed of consciousness or that `kappa_c1` marks when that primitive seed becomes subject-level consciousness.

Historical language may remain only if explicitly marked superseded / historical.

## WTC-12 — Subjecthood Threshold Interface

Keep the S0-S6 discrimination ladder.

Retype:

```text
S4 subjecthood package
S5 agency package
S6 responsibility package
```

as conservative candidate evidence / evaluation packages.

Replace universal "requires" / "at minimum" language where it reads as theory-wide N&S admission with:

```text
current evaluation package
candidate evidence burden
do not infer stronger standing without independent establishment
```

## WTC-13 — responsibility

Do not define moral responsibility by a fixed universal conjunction of:

```text
agency + alternatives + norm access + consequence understanding + payable Psi_f + repair
```

The package may remain as a high-threshold evaluative rubric / governance lens.

## WTC-14 — AI consciousness owner

Keep architecture-state rules and anti-overattribution guards.

Retype `Ax-CONSC-*`, `Cor-CONSC-1`, observer thresholds and `d * Psi_f` formulas as AI-domain hypotheses / candidate evaluation constructs where they exceed current theory authority.

In particular:

```text
nontrivial L0->L1 anchoring + d>0 + Psi_f>0
!= iff consciousness window
!= universal necessary condition
!= universal sufficient condition
```

## WTC-15 — AI CompactCore mirror

Synchronize the CompactCore with the repaired owner.

Phrases like:

```text
minimum consciousness threshold
necessary threshold
minimum subject anchoring boundary
```

must become candidate / evaluation language.

## WTC-16 — Seven Theses

Respect the existing top status:

```text
superseded_as_active_constitution
L1 interface / commentary
```

Truth-up Thesis 6 so it no longer states:

```text
subjectivity requires d + bearing as a settled definition
kappa_c1/c1.5 are independent consciousness phase transitions
frozen state is definitely conscious
order increment has a universal four-criterion solver
healthy/pathological delegation follows directly from d/order-gain
```

It may preserve these as historical/candidate explanatory lines with current guards.

## WTC-17 — Symbol Table kappa rows

Only two rows may change in this file:

```text
kappa_c1
kappa_c1.5
```

Retype them from theory-wide consciousness thresholds to candidate model coordinates / candidate evaluation handles consistent with the repaired consciousness owner.

Recommended role labels:

```text
kappa_c1
  Minimal-Consciousness Candidate Window Coordinate

kappa_c1.5
  Consciousness-Activity Candidate Coordinate
```

Exact wording may vary, but the rows must explicitly block:

```text
crossing -> consciousness proof
crossing -> subject-position
non-crossing -> no consciousness
universal natural phase boundary
```

Do not touch `sigma_sr^sub/self` or any other symbol row.

---

# Semantic Commit B — Core12a / Core12b realization closure

Exact files:

```text
Core/SRT_Core_12a_Ontology_L0L1.md
Core/SRT_Core_12b_Ontology_L2.md
```

Suggested message:

```text
Truth-up Core12 realization and L2 legacy backflow
```

## WTC-18 — Core12a top override remains controlling

Do not weaken the existing Selection-totality override.

## WTC-19 — L0 formal realization

Legacy body statements equivalent to:

```text
L0 true = moduli space
L0 = differential manifold
L0_abs = absolute potential source / full possibility warehouse
L0 formal = gauge x Ruliad as best ontology
```

must be explicitly retyped as:

```text
domain / bridge realization
historical strong formulation
conditional formal projection
```

not primitive ontology.

## WTC-20 — proto-G

"proto-G in L0" must not read as a stage prior to primitive Selection.

Keep only:

```text
low-order realization candidate downstream of primitive actualisation
```

## WTC-21 — kappa phase language

`kappa / kappa_c1 / kappa_c2` may remain as declared model stabilization coordinates.

They must not be universal births of:

```text
L1
L2
subjecthood
consciousness
order creation
```

## WTC-22 — kappa0

Every active Core12a/12b occurrence must respect:

```text
kappa0 = conditional anisotropy / curvature-floor candidate
kappa0 != O0
kappa0 != primitive direction itself
kappa0 != cause of first Selection
kappa0 necessity = OPEN
```

## WTC-23 — swampland / Platonic / pre-anchored / matter-knot

These may remain as historical / bridge constructs only.

Do not allow:

```text
Matter = Knot(L0)
Platonic attractor = universal L0 ontology
pre-anchored state = universal premanifest ontology
swampland = universal ontological region
```

as active theory definitions.

## WTC-24 — Core12b L2 accumulation

Replace universal readings equivalent to:

```text
all selection contributes to L2
every real selection leaves an L2 trace
Ax-L2-01 = universal trace law
```

with a typed ladder:

```text
Selection may have irreversible consequence / retained efficacy;
model-local trace requires an explicit retention / writeback mapping;
closure requires recurrent consequence-sensitive conditioning;
L2-grade scaffold requires stronger inheritance / recurrence / backgrounding.
```

Do not make ordinary irreversibility sufficient for L2.

## WTC-25 — T-L2-Scaffold

Preserve it as a useful model where its assumptions are declared.

Do not let its `rho`, `Psi_f` gate, or stable-ISP language become a universal ontology theorem.

## WTC-26 — rights / auth layer

All Core12b rights / authorization / legitimacy sections must be retyped as:

```text
declared-framework candidate institutional model
P2-P4 diagnostic / bridge
```

Block:

```text
rights = L2 structural fact
d -> rights
L2 recognition -> legitimacy
authorization layer -> normative authority automatically
```

## WTC-27 — political comparison signals

`P_blocked`, `d_accessible`, `Delta R_future`, challenge signals, generative-health criteria and related quantities may remain comparative diagnostics only.

No `iff` legitimacy or O2-M verdict.

## WTC-28 — algorithm identity

Statements equivalent to:

```text
Algorithm = extreme / bloodless L2 state
```

must be historical / metaphorical / model-class readings, not ontology identities.

## WTC-29 — consciousness map inside Core12b

Any `kappa_c1/c1.5` consciousness map must align with Semantic Commit A: candidate coordinates only.

---

# Semantic Commit C — normativity / legitimacy closure

Exact files:

```text
Core_Law/SRT_Selection_Argument.md
Philosophy/SRT_Philosophy_Foundations_CompactCore.md
Philosophy/03_Selection_Realism_Layered_Realism_CompactPatch.md
```

Suggested message:

```text
Remove residual universal legitimacy solvers
```

## WTC-30 — four axes survive, universal solver does not

In `SRT_Selection_Argument.md §7b` retain:

```text
continuability
coordinability
non-export
reselectability
```

as scoped structural comparison axes.

Required scope declaration:

```text
position / population
time horizon
interfaces
consequence channels
comparison domain
declared objective where applicable
```

## WTC-31 — no universal order-gain conjunction

Remove / truth-up:

```text
all four simultaneously = complete order-gain test
missing one = order loss universally
```

A declared model may use a conjunction, but SRT does not elevate it to a universe-wide theorem.

## WTC-32 — legitimacy section

`§7d.3` must not state:

```text
legitimacy = order gain
legitimacy iff the four criteria
```

Replace with a candidate delegation / institutional-accountability lens.

Explicitly preserve:

```text
O2-M remains OPEN
structural comparison != moral legitimacy theorem
```

## WTC-33 — concern language

"true / false concern" may remain as an explanatory contrast only if:

```text
not a moral verdict
not canonical d definition
not a fitness-function identity theorem
```

Canonical `d` ownership stays outside this file.

## WTC-34 — Foundations purpose

Replace:

```text
purpose is generated by high-d differences
purpose = high-d attractor
```

with a candidate philosophy-domain interpretation.

High `d` does not establish purpose, goodness or legitimacy.

## WTC-35 — Foundations normativity

Replace the numbered "legitimacy tests" battery with:

```text
candidate scoped questions / declared-framework diagnostics
```

Explicitly state they are neither jointly necessary nor sufficient for legitimacy.

## WTC-36 — morality summary

Replace / qualify:

```text
Morality = high-d social conflict stabilized into legitimacy-seeking norms
```

as a historical/candidate social-philosophy model, not SRT-wide definition.

## WTC-37 — consciousness references in Foundations

Where these files repeat an old consciousness-threshold package, synchronize with Semantic Commit A.

Do not create a second consciousness owner.

---

# Semantic Commit D — social institutions + registry sync

Exact files:

```text
Philosophy/SRT_Soc_03_Institutions.md
CANONICAL_REGISTRY.md
```

Suggested message:

```text
Truth-up social institutions and registry summaries
```

## WTC-38 — institutions owner status

Preserve the top claim-status note.

Add a local current-reading guard before legacy formal axioms if needed:

```text
Part A labels Ax/T are retained historical/model labels;
they do not create theory-canonical political or moral definitions.
```

## WTC-39 — rights

Replace active definition-strength language:

```text
Right(i) = partial Omega_i subset L2
rights are protection of d / choice channels
rights protocols forbid removal ...
```

with declared-framework candidate institutional models.

## WTC-40 — inclusive / extractive and d

Do not identify:

```text
inclusive = high d
extractive = low d
social power = d distribution
```

as ontology definitions.

These may remain empirical / modeling hypotheses with explicit proxy and falsification requirements.

## WTC-41 — value

```text
Value = expected stabilization probability
```

must not appear as a theory-wide value definition.

Retype as historical/candidate institutional-economics model.

## WTC-42 — institutional engineering / policy

Sections on:

```text
three SRT institutional-engineering principles
public regulation requirements
anti-monopoly prescriptions
optimal entropy
```

must be framed as candidate design implications under declared objectives, not prescriptions derived directly from ontology.

## WTC-43 — democracy thresholds

Numerical / structural thresholds for democracy must be explicit toy / historical empirical hypotheses.

Do not call them necessary conditions of democracy in SRT.

## WTC-44 — Definition Summary

The end-of-file Definition Summary / Formalization Summary must be rewritten to reflect candidate mappings, not definitions of rights, value, d-based agency inequality or political legitimacy.

## WTC-45 — Registry individuation summary

Update stale summary language:

```text
sigma_sr^sub = subject-position entry
sigma_sr^self = self-consciousness condensation threshold
Individuation owns ISP entry
```

to:

```text
candidate transition coordinates
downstream transition model
not Stable-ISP entry owner
not subject admission
exact subject-position sufficiency OPEN
```

## WTC-46 — Registry consciousness routes

Where Registry summaries present the repaired AI / consciousness owners as definition authority for universal consciousness thresholds, synchronize them to bridge/candidate status.

Do not restructure unrelated Registry entries.

---

# Derivative Commit E — Core split synchronization

Expected directories:

```text
Core/Ontology_L0L1_Split/**
Core/Ontology_L2_Split/**
```

Suggested message:

```text
Regenerate Core ontology reading splits after closure
```

## WTC-47 — temporary generator preflight

Before writing official split directories, run `scripts/create_reading_split.py` into temporary directories.

Use existing source-owner IDs:

```text
SRT-CORE-12A-SPLIT-INDEX
SRT-CORE-12B-SPLIT-INDEX
```

Preserve current topology expectation:

```text
Core12a: 2 shards + README
Core12b: 3 shards + README
```

If the current official splitter unexpectedly changes topology beyond ordinary boundary movement caused by owner edits, STOP.

Do not force a split-topology migration into this closure.

## WTC-48 — official generation

If temporary preflight is compatible, regenerate the two registered Core split families from their repaired owners using the official splitter.

Require source-owner bytes/SHA binding and strict freshness.

---

# Derivative Commit F — AI legacy consciousness split

Directory:

```text
AI/Consciousness_Framework_Split/**
```

Suggested message:

```text
Sync AI consciousness legacy split after closure
```

## WTC-49 — preserve semantic shard topology

Keep the existing five semantic shards + README.

Do not replace them with generic numbered Part shards merely for automation.

## WTC-50 — bounded derivative truth-up

Review all five shards.

Modify only shards that mirror changed owner passages.

No independent theory edits are allowed in split files.

## WTC-51 — metadata

Run official split metadata tooling.

README owner bytes/SHA must match the final owner.

Strict split metadata/freshness must pass.

---

# Generated Commit G — Context Bundles

Expected:

```text
Operations/Context_Bundles/**
```

Suggested message:

```text
Regenerate context bundles after whole-tree closure
```

## WTC-52

After all semantic + split commits:

```bash
uv run python scripts/build_srt_context_bundles.py --check
```

Expected: NON-NOOP.

Then:

```bash
uv run python scripts/build_srt_context_bundles.py
uv run python scripts/build_srt_context_bundles.py --check
```

Commit only official generated bundle output.

Do not hand-edit bundle content.

---

# Main-overlap / authority protection

## WTC-53

Before the first semantic edit and again before final Governance, fetch latest live main.

Check main-only overlap with:

```text
all 13 semantic owners
all three split directories
Operations/Context_Bundles/**
scripts/create_reading_split.py
scripts/refresh_split_metadata.py
scripts/build_srt_context_bundles.py

Core_Law/SRT_Constitution_V1.md
Core_Law/SRT_Generative_Ontology_Spine.md
Core_Law/SRT_One_Formation.md
Core_Law/SRT_Individuation.md
Core/SRT_Core_21b_Constitutive_Theorems.md
_SRT_D_VALUE_CANONICAL.md
_SRT_PSI_F_CANONICAL.md
_SRT_T_DIR_CANONICAL.md
```

If overlap affects the closure semantics or tooling:

```text
STOP
NO AUTO-REBASE
```

---

# Validation

## WTC-54 — exact commit scopes

Require exact semantic file sets for Commits A-D.

Require derivative-only scopes for Commits E-G.

## WTC-55 — standard governance

Run at least:

```text
git diff --check
changed-file frontmatter validation
baseline monotonicity
hook/dependency closure
registry consistency
authority routing
status rule
material-log consistency
book-split guard
strict split metadata/freshness
Context Bundle --check
active-theory-node check
full Governance Preflight against latest live main
```

## WTC-56 — four-class stale-backflow search

Search the active tree for residual positive claims equivalent to:

### Selection -> Agency

```text
Selection = Agency
all Selection is agency
selection occurrence proves agency
```

### Stable ISP / d -> subject

```text
Stable ISP -> subject / consciousness
d > 0 -> subject / consciousness
kappa_c1 -> consciousness admitted
sigma_sr^sub -> subject admitted
```

### d / Psi / four-axis -> morality / legitimacy

```text
high d -> good / moral / legitimate
Psi_f -> suffering / moral cost
four axes iff legitimacy
rights / authority generated directly by d
```

### formal model -> primitive Selection

```text
G_hat_theta = primitive Selection
L0 = manifold / Ruliad / possibility warehouse universally
kappa0 = primitive Selection direction
operator causes first actualisation
```

Remaining hits must be one of:

```text
explicit negative guard
historical / superseded quotation
candidate / scoped model
audit / proposal / handoff record
```

## WTC-57 — no cleanup maximalism

Do not rewrite files merely for style, prose quality, political examples, citation modernization or architecture elegance.

This is semantic closure, not a general refactor.

## WTC-58 — stop rule

After push and successful Governance:

```text
STOP
DO NOT declare whole-tree closure FINAL PASS
DO NOT merge #976
DO NOT mark ready
DO NOT begin new theoretical expansion
```

Independent review must close the whole-tree gate.
