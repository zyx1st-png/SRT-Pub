---
id: SRT-SELECTION-TOTALITY-WAVE-D3A-PATCH-SPEC-20260915
type: proposal
status: active
layer: operations
epistemic_layer: os
claim_mode: proposal
canonical: false
date: 2026-09-15
---

# Wave D3-A exact patch specification

## Status

```text
D2 = FINAL PASS
D3 prelanding review = PASS TO BOUNDED TWO-WAVE RECONSTRUCTION
D3-A semantic landing = NOT YET EXECUTED
D3-B = HOLD
MERGE #976 = NO
```

This specification is the single execution source for the first D3 owner landing. It does not authorize D3-B or broad downstream cleanup.

## Exact semantic owner scope

Only these two files may receive D3-A owner-semantic edits:

```text
Core_Law/SRT_Individuation.md
Philosophy/SRT_L0_Ontological_Status.md
```

Do not edit during the D3-A owner semantic commit:

```text
Core_Law/SRT_L1_Formalism.md
Core/SRT_Core_22_Equations.md
Core_Law/SRT_Occlusion_Dynamics.md
Core_Law/SRT_Irreversibility.md
_SRT_T_DIR_CANONICAL.md
Core_Law/SRT_L0_Metaphysics.md
Core_Law/SRT_One_Formation.md
Core/SRT_Core_21b_Constitutive_Theorems.md
```

Those are authority / consumer-check surfaces only at this stage.

---

# Part A — `Core_Law/SRT_Individuation.md`

## A1. Preserve owner routing

Keep explicit that this file is downstream of:

```text
subjectless Selection
-> One / Selection-position
-> stronger recurrent standing where separately established
-> subject-position / self-consciousness modelling here.
```

Hard guards:

```text
Individuation does not define primitive Selection;
Individuation does not define One;
Individuation does not define Selection-position;
Individuation does not redefine P1-T06 Stable ISP;
Individuation does not define Bearer by shortcut.
```

Do not write a new universal chain that silently identifies Stable ISP, subject-position, Bearer, and self-consciousness.

## A2. Retype the core claim

The present statement that individuation *is* a phase transition in self-reference ratio must no longer appear as a universal P1-candidate ontology theorem.

Retype `sigma_sr` as:

```text
P2 model-local / replaceable order-parameter interpretation
for a declared individuation model.
```

The three-phase / two-transition narrative may remain as a model architecture, but its thresholds are not universal ontology boundaries.

Recommended claim typing:

```text
three-phase architecture = P2 theory/model candidate;
sigma_sr = P2 model coordinate / proxy;
sigma_sub / sigma_self = P3/P4 threshold coordinates unless independently calibrated;
self-consciousness-as-second-order-condensate = P2 interpretation;
exact subject-position sufficiency = OPEN.
```

Do not promote any substitute scalar as a natural universal subject threshold.

## A3. Keep Def-IND-1 only as a model-local coordinate

The current ratio form may remain:

```text
sigma(P,t) = ||theta_trace|| / (||theta_trace|| + ||theta_ext||)
```

but its scope must remain explicit:

- depends on declared trace/ext attribution rules;
- depends on declared representation and norm/metric;
- does not establish invariant causal control share;
- does not establish natural subject boundary;
- does not establish identity or lineage by itself.

Remove any residual wording that treats `theta_ext` as perturbation from a primitive L0 possibility domain.

## A4. Replace T-IND-1 theorem-strength language

Keep the useful modelling hypothesis that stronger recurrent self-conditioning may be tracked by an order parameter, but retype it as a conditional model candidate.

Do not claim:

```text
sigma crossing alone -> subject-position;
sigma > 0 -> iteration proved;
sigma stable -> perspective proved;
theta_trace accumulation -> durable history universally proved;
sigma < 1 -> continued-selectability proved.
```

Those relations may be heuristic / operational correlations inside a declared model only.

## A5. Correct T-IND-2

Remove the current universal conjunction that requires:

```text
d > 0 stable concern;
epsilon-direction local readability;
B >= 2 versus B <= 1 continuation / self-erasure discrimination;
bearing-care-direction jointly anchored.
```

The replacement must say, in substance:

```text
sigma_sub is a model-local transition coordinate;
crossing it can be evidence inside a declared individuation model;
it is not by itself a universal subjecthood admission theorem.
```

Any positive subject-position claim must separately state which stronger upstream / downstream burdens are already independently established.

At minimum preserve:

```text
formed One / Selection-position != subject-position automatically;
Stable ISP != subject-position automatically;
subject-position != Bearer automatically unless a separate owner establishes that relation;
subject-position != self-consciousness;
```

Do not invent a replacement necessary-and-sufficient subjecthood theorem in this wave.

## A6. Direction-source discipline

Where direction / readability language remains:

```text
no independently typed declared direction -> T_dir claim not admitted;
epsilon_pg does not supply universal direction;
B-count does not supply universal direction;
primitive Selection does not supply universal direction.
```

A domain or pathology model may use T_dir only after a separately declared direction source is specified.

Do not use `epsilon`, continuation, non-self-erasure, option count, or persistence as a primitive good / value direction.

## A7. d and Psi_f discipline

`d > 0` must not remain a universal subject-position admission theorem.

`d` may appear as a downstream stake / concern measure after its own owner conditions are met.

`Psi_f` may appear as a cost / payability quantity after its own owner conditions are met.

Neither quantity by itself establishes:

```text
subjecthood;
Bearer;
consciousness;
health;
legitimacy;
moral standing.
```

## A8. Self-consciousness

Preserve the useful distinction:

```text
subject-position != self-consciousness.
```

The second-order-condensate model may remain at P2 strength. Any `sigma_self` threshold is a model coordinate, not a universal natural phase boundary.

## A9. Summary / retrieval consistency

Search the entire file and update all current summary / quick-reference / theorem / example surfaces so none reintroduces the old universal gate.

Targeted terms include:

```text
P1-candidate
sigma_sub
sigma_self
d > 0
epsilon
B >= 2
B <= 1
subject-position
ISP entry
bearing-care-direction
关切
承担
方向
potential domain
潜在域
```

Every hit must be either:

```text
current bounded model statement;
explicitly OPEN;
historical / superseded;
or FAIL.
```

---

# Part B — `Philosophy/SRT_L0_Ontological_Status.md`

## B1. Remove false authority claim

The file must no longer state or imply that it is the authority source for L0 ontology.

Add an explicit owner boundary:

```text
Core_Law/SRT_L0_Metaphysics.md = unique canonical L0 authority;
this Philosophy file = downstream philosophical interpretation / clarification only.
```

The file may discuss what it means to speak about the L0-side, but may not define primitive ontology.

## B2. Replace warehouse / operator-first opening

Remove as current doctrine:

```text
L0 = potential domain;
G-hat_theta pulls L1 slices from L0;
L0 exists as a source structure independent of Selection.
```

Use the current canonical frame:

```text
Selection = primitive generative mode;
L0-side = analytic / model-facing open / non-preclosed aspect of Selection reality;
L0/L1/L2 != three independent ontic substances.
```

Do not turn the L0-side into a new hidden object.

## B3. Remove kappa/Psi/direction realism criterion

Delete or explicitly supersede any equation or prose equivalent to:

```text
L0 reality iff Psi_f > 0 and kappa0 > 0 and directional asymmetry.
```

Do not replace it with another scalar existence test.

Current boundary:

```text
kappa0 does not define primitive Selection;
Psi_f does not certify L0 existence;
directional asymmetry is not a universal primitive value/direction law.
```

## B4. Remove kappa0 as unique access surface

Delete or supersede:

```text
kappa0 = unique accessible face of L0;
d = Align(theta, kappa);
non-alignment = higher-cost resistance to L0 curvature.
```

Any curvature / anisotropy / alignment model must be declared model / bridge scoped and must not back-define L0.

## B5. Remove epsilon consciousness-seed route

Delete as current doctrine:

```text
epsilon_pg = L0-side direction seed for consciousness;
electron-scale directional seed -> progressively amplified consciousness;
e_pg as a minimal inward bias inherited toward bare consciousness.
```

Hard guard:

```text
Selection-totality -/> pan-consciousness;
epsilon_pg -/> consciousness seed by default;
electron / microphysical asymmetry -/> proto-consciousness by default.
```

Do not replace this with another primitive consciousness precursor.

## B6. Retain the philosophical question at lower strength

The file may retain a structured discussion of false alternatives such as:

```text
L0 as independent Platonic warehouse;
L0 as operator-produced substance;
L0 as merely unreal placeholder.
```

But these must be framed as philosophical options / superseded framings, not as co-equal current canonical positions.

A permitted current interpretation is:

```text
asking whether the L0-side is a separately existing object is partly a category mistake;
its role is to name the open / non-preclosed analytic aspect of Selection reality,
without creating a second substance behind actuality.
```

Do not derive stronger claims about time, consciousness, normativity, physics, or mathematics than current owners independently license.

## B7. Update references / lineage

Remove obsolete authority references where they imply superseded owners (for example old `Core_21_Formal_Axioms` / operator-first bootstrap authority) and point current normative statements to the actual owners.

Do not broaden this into repository-wide link cleanup.

## B8. Summary / retrieval consistency

Search the entire file for at least:

```text
权威来源
潜在域
potential domain
possible worlds
G-hat
Ĝ
pull / 拉出
kappa0
κ₀
Align
方向种子
epsilon_pg
ε_pg
primordial direction
原初方向
electron
电子
bare consciousness
原初意识
Psi_f > 0
d > 0
L0 reality
L₀ 实在性
```

Every current hit must be classified as:

```text
current bounded philosophical statement;
model / bridge scoped;
historical / superseded;
or FAIL.
```

No FAIL may remain before semantic commit.

---

# Part C — non-closure guards

D3-A must not settle:

```text
exact subject-position necessary/sufficient conditions;
subject-position <-> Bearer equivalence;
Bearer necessary/sufficient conditions;
phenomenality;
consciousness sufficiency;
O2-M;
kappa0 inheritance;
epsilon_pg inheritance;
universal natural sigma boundary;
D3-B irreversibility / occlusion questions.
```

If execution encounters a sentence that requires one of these questions to be closed, STOP and report rather than invent an answer.

---

# D3-A acceptance gates

```text
D3A-01 unique L0 authority remains SRT_L0_Metaphysics.md;
D3A-02 L0-status file no longer treats L0 as a potentiality warehouse;
D3A-03 no G-hat operator is primitive L0 -> L1 producer;
D3A-04 kappa0 / epsilon_pg / B-count do not supply primitive universal direction;
D3A-05 no electron-scale / primordial direction seed is promoted toward consciousness;
D3A-06 Individuation remains downstream of One / Selection-position;
D3A-07 sigma_sr remains model-local / replaceable, not a universal ontology threshold;
D3A-08 d > 0 is not a universal subjecthood admission theorem;
D3A-09 T_dir / direction readability only appears after independently typed direction admission;
D3A-10 Stable ISP, subject-position, Bearer and self-consciousness remain non-identical;
D3A-11 exact subject sufficiency remains OPEN unless separately author-adjudicated;
D3A-12 no new primitive, canonical symbol, or layer is introduced;
D3A-13 semantic commit touches exactly the two D3-A owner files;
D3A-14 D3-B files remain untouched;
D3A-15 git diff --check passes.
```

Any failure keeps D3-A at REVISE and D3-B at HOLD.

---

# Execution sequence after authorization

1. Recheck live `main`, PR head, and overlap.
2. Edit exactly the two D3-A owner files.
3. Run targeted residual searches and classify every hit.
4. Verify D3A-01..D3A-15.
5. Commit the two-owner semantic landing.
6. STOP for independent semantic review before touching `SRT_L1_Formalism.md`, Core22, generated splits, context bundles, or D3-B.

Recommended semantic commit message:

```text
Truth-up Wave D3-A subject and L0-status owners
```

## Stop rule

```text
D3-A semantic landing requires explicit author authorization after this spec is reviewed.
D3-A consumer truth-up is a later bounded step.
D3-B remains HOLD.
MERGE #976 = NO.
```
