---
id: SRT-SELECTION-TOTALITY-WAVE-E5-ETHICS-AGENCY-PATCH-SPEC-2026-09-16
type: proposal
status: active
layer: governance
epistemic_layer: proposal
claim_mode: exact_patch_spec
---

# Wave E5 exact patch specification — Ethics / Agency consumer truth-up

## 0. Execution gate

```text
THIS FILE DEFINES THE E5 PATCH CONTRACT.
IT DOES NOT AUTHORIZE SEMANTIC EXECUTION BY ITSELF.
```

One semantic owner only:

```text
Philosophy/SRT_Ethics_Agency.md
```

One deterministic derivative family only:

```text
Philosophy/Ethics_Agency_Split/*
```

No other semantic owner may change in E5.

---

## 1. Patch objective

Truth-up the non-canonical Ethics/Agency bridge to the already-landed Selection-totality architecture without replacing its historical/proxy material with a new ethics theory.

The patch must achieve all three simultaneously:

```text
1. stale strong upstream dependencies no longer appear current-positive;
2. legacy formulas/discourse remain retrievable where possible;
3. no new universal Agency / freedom / responsibility / evil / O2-M theorem is introduced.
```

The patch is therefore primarily an **active-use override and claim-status repair**, not a conceptual rewrite.

---

## 2. Non-negotiable authority guards

The patched owner must be compatible with:

```text
Selection event != One != Stable ISP != Bearer != Agency
primitive Selection != G-hat_theta
Bearer -/> Agency automatically
exact Agency sufficiency = not established

primitive Selection != ought
actual occurrence != correctness / legitimacy
continuation != goodness
more options / more Selection != better

O1 / O2-C / O2-A remain typed and distinct
O2-M = OPEN / HOLD

suffering requires independent registration/admission
occlusion pattern -/> responsibility / moral verdict automatically
irreversibility model -/> universal Psi_f / death / suffering theorem
```

No passage in the owner may override these guards.

---

## 3. Exact semantic patch units

### E5-P01 — File-level current-authority guard

Location:

```text
immediately inside / after `## 2026-05-16 Bridge Realignment Guardrail`
```

Add a compact 2026-09-16 current-authority override stating:

```text
- this file is non-canonical P2-P4 bridge/history;
- current cross-owner authority is the Generative Ontology Spine plus local owners;
- formulas and named Ax/T handles below do not define primitive Selection, Agency, freedom,
  responsibility, suffering, health, legitimacy or O2-M unless separately admitted;
- older stronger prose survives for history/retrieval where locally marked, but is not an
  active SRT conclusion when it conflicts with current authority;
- no legacy section may be used for reverse inference into canonical owners.
```

The guard must explicitly include:

```text
primitive Selection != G-hat_theta
Agency sufficiency remains open
O2-M remains open
```

Do not invent a replacement Agency theorem.

### E5-P02 — Canonical anchor list truth-up

In the current `Canonical anchors` list:

ADD:

```text
Core_Law/SRT_Generative_Ontology_Spine.md
Core_Law/SRT_Irreversibility.md
```

RETYPE descriptions so that:

```text
SRT_Suffering.md = suffering admission / typed structural owner, not direct ethics theorem
SRT_Occlusion_Dynamics.md = occlusion structural/model owner, not evil/responsibility verdict owner
SRT_Irreversibility.md = occurrence/history/termination interface under current bounded standing
```

Do not modify the referenced owners.

### E5-P03 — Agency downgrade rule

Replace the current unqualified rule:

```text
Agency requires payability, consequence return, and reselectable mobility
```

with a bounded formulation:

```text
payability, consequence return and reselectable mobility are useful bridge/operational
candidate dimensions for evaluating agency-like organization;
they are not asserted here as the exact universal necessary-and-sufficient Agency theorem.
```

Retain the useful warning that high `d` / low `d_mobile` need not mean high agency.

### E5-P04 — Occlusion cross-link

Rewrite `## Canonical Cross-Link: Occlusion Dynamics` so that it no longer says the canonical owner supplies an `evil` three-criterion definition.

Required current reading:

```text
- A/B stages, threshold coordinates, residuals, intervention/decoupling structures are
  conditional model/bridge structures under their current owner;
- stage membership does not determine responsibility;
- propagating-occlusion / externalization patterns can inform ethics analysis only after
  separately declared criteria;
- no `evil iff ...` theorem is current;
- O2-C != O2-A != O2-M;
- O2-M remains OPEN/HOLD.
```

### E5-P05 — Suffering cross-link

Rewrite `## Canonical Cross-Link: Suffering Theory`.

Remove the current-positive claim that T-SUFF-4 is a non-downgradable structural proof that pain minimization is ethically insufficient.

Required reading:

```text
- suffering is used only after independent suffering admission / first-person registration;
- signal/structural proxy distinctions do not themselves create phenomenality;
- signal suppression alone does not prove hidden suffering conservation;
- anti-minimization can remain an ethics bridge concern under explicit assumptions,
  not a universal P1 moral theorem derived directly from Suffering.
```

### E5-P06 — Irreversibility cross-link

Rewrite `## Canonical Cross-Link: Irreversibility (Termination Interface)`.

Remove current-positive routing to:

```text
T-IRR-1 = universal asymmetric Psi_f payment theorem
T-IRR-2 = unrestricted generic absorbing-death theorem
T-IRR-4 = universal suffering-under-L0-cost theorem
```

Required current reading:

```text
- occurrence non-equivalence != retained history automatically;
- T-IRR-1 is a conditional P2/P3 writeback/learning model;
- termination is lineage-/transition-relative and must not be conflated with pause;
- ethics may discuss loss/termination consequences only after the relevant identity,
  retention, suffering and responsibility gates are independently established.
```

### E5-P07 — Ax-Ag-1 operator guard

Keep the historical formula:

```text
Agent^proxy ~ G-hat_theta : L0 -> L1
```

but strengthen its local boundary to say explicitly:

```text
- this is a legacy/domain operator proxy;
- it does not define primitive Selection;
- it does not establish a prior selector object;
- it does not establish universal Agency, subjecthood or Bearer standing;
- current cross-owner Agency requires separately paid burdens.
```

Do not delete the formula.

### E5-P08 — Ax-Ag-3 / action-potential claim status

Keep `P_action` as a P3/P4 model/proxy sketch.

Required edits:

```text
- `P_action > 0` = model-side action-initiation condition inside the declared sketch,
  not a universal physical necessary condition for agency/free choice;
- depression statement -> explicit candidate/historical bridge hypothesis;
- compatibilism/J-layer statement -> remove `moral responsibility is retained in J layer`;
  replace with `J-layer modeling alone does not establish O2-A responsibility`;
- intervention language -> illustrative/model-level, not treatment theorem.
```

### E5-P09 — Ax-Ag-5 pathology section

Retitle/retype the section so that `Pathology as Parameter Distortion` cannot be read as a universal SRT pathology theorem.

Acceptable forms include:

```text
Pathology Parameter-Distortion Hypothesis (P4 bridge)
```

or equivalent.

Required guard:

```text
theta / mu_expect mappings are candidate model coordinates;
depression / mania are not identified or diagnosed by these variables;
no clinical sufficiency/necessity/treatment inference is licensed.
```

Preserve the historical equations if desired.

### E5-P10 — T-Ag-1 metastability

Retain the equation for historical/model retrieval but remove theorem/iff standing.

Required transformation:

```text
T-Ag-1: Metastable Selection Window
-> historical / P3-P4 candidate handle

FreeChoice iff S(t) in W_meta
-> legacy candidate equation, not universal equivalence
```

The current owner must explicitly say metastability may be one enabling condition in a declared model but does not admit freedom by itself.

### E5-P11 — legacy `moral responsibility` localization

For passages saying:

```text
moral responsibility remains in J layer
true moral responsibility lies in second-layer activation
responsibility is located at active preservation/destruction of perception conditions
```

retain the historical reasoning only if locally retyped as a candidate ethics analysis of particular self-occlusion/choice models.

Required active-use guard:

```text
no layer variable by itself establishes responsibility;
O2-A-style answerability additionally requires independently established agency/capacity,
knowledge/control/choice-window and consequence-return conditions appropriate to the case.
```

Do not replace this with a universal positive O2-A theorem.

### E5-P12 — absolute responsibility

In `### 4.1 The Cost of Freedom: Existential Anxiety`, the sentence:

```text
Accepting SRT's free will model means accepting absolute responsibility.
```

must no longer survive as current-positive.

Required treatment:

```text
mark as historical/existential overstatement or replace with a bounded statement that
rewritability can increase some forms of answerability only where capacity, knowledge,
control, agency and consequence-return burdens are met.
```

Also remove the implication that biology, genes or social environment cannot reduce/condition responsibility.

### E5-P13 — brain / embodied operator claims

In §4.2, retain the embodied-systems discussion only as a P3/P4 philosophy-of-neuroscience bridge.

Required guards:

```text
brain = L2 of biological G-hat_theta -> model/bridge mapping, not ontology identity
brain dynamics causally closed -> not established here as an SRT theorem
true G-hat = whole organism-environment coupling -> candidate modeling stance, not canonical identity
brain alone cannot account for full Selection -> do not state as empirically closed result
```

Do not edit Neuroscience files in E5.

### E5-P14 — Ought-implies-can bridge

In §6.1, retain accessibility/capacity-sensitive ought as an ethics bridge candidate.

Replace `ontologically incoherent` universal language with bounded language:

```text
under a declared responsibility/ought framework, demands outside relevant accessible
capacity may fail that framework's answerability condition.
```

Do not turn this into primitive ontology or close O2-M.

### E5-P15 — §6.2 clinical bridge

Retype direct depression equations/claims (`mu_eff -> infinity`, computational bankruptcy, CBT/medication target language) as historical/testable P4 bridge hypotheses.

Required guard:

```text
not a diagnostic criterion;
not a clinical sufficiency/necessity theorem;
not treatment advice or evidence of treatment efficacy;
requires independent clinical/neuroscience validation.
```

Preserve the conceptual model if clearly scoped.

### E5-P16 — §6.3 Agent causation

The current block:

```text
Agent = G-hat_theta : L0 -> L1
agent collapses L0 into L1
agent selects which laws are instantiated
```

must be explicitly marked historical/superseded for active ontology.

Required current override immediately adjacent:

```text
primitive actualising Selection is subjectless;
G-hat_theta may model a formed/domain-specific selection organization;
it is not the prior chooser or ontic cause primitive Selection requires;
no `selecting which laws are instantiated` claim is licensed as current canonical ontology here.
```

The old block may remain quoted/preserved as historical text.

### E5-P17 — freedom / open vertical causation

The statement that freedom is located where vertical rewrite preserves/reopens future reselectability may remain only as a bounded bridge hypothesis.

Required guard:

```text
reselectability is neither sufficient nor universally necessary for a final theory of freedom;
no current canonical FreeWill admission theorem is claimed.
```

### E5-P18 — §6.4 Problem of Evil

The following cannot remain current-positive SRT conclusions:

```text
true evil as an independent category ontologically does not exist
occlusion is the only explanatory framework
all apparently evil behavior falls into occlusion
passive occlusion -> no responsibility automatically
A/B stage -> exact responsibility assignment
legacy four criteria -> objective all-things-considered moral direction
```

Required treatment:

```text
- keep the section as historical / exploratory ethics hypothesis;
- add a prominent current-authority override at section start;
- state that occlusion can be one structural contributor to harmful/actionable cases;
- responsibility cannot be read off stage membership alone;
- the earlier exhaustive ontology of evil is superseded as an active SRT conclusion;
- O2-M remains OPEN/HOLD;
- structural comparison under declared criteria does not settle all-things-considered moral verdicts.
```

Do not replace the old theory of evil with a new final theory of evil.

### E5-P19 — AI morality question

Keep the AI morality question as OPEN.

Add/retain guard:

```text
Psi_f != felt suffering by definition;
Psi_f availability / absence does not settle phenomenality or moral status;
AI class membership does not settle Agency / Bearer / suffering / moral status.
```

### E5-P20 — later responsibility / psychiatric passages

For later sections such as `Multiaxial Responsibility in Psychiatric Conditions` and integration notes:

```text
- state-conditional, multi-axis assessment may remain as P3/P4 bridge proposal;
- no psychiatric diagnosis maps directly to moral failure or responsibility class;
- physiological/clinical variables may constrain capacity assessments but do not by themselves
  determine O2-A responsibility;
- no medical treatment recommendation is created by E5.
```

Do not broaden into clinical advice.

---

## 4. Preservation requirements

The patch must NOT perform wholesale deletion of Part A or Part B.

Preserve the following as historical/proxy material with truthful status:

```text
Ax-Ag-* / Ax-Resp-* / T-Ag-* identifiers
legacy equations where they still aid provenance/retrieval
P_action model family
metastability model idea
reselectability / second-order rewrite discussion
payability / consequence return / choice-window / knowledge dimensions
historical problem-of-evil argument as a superseded/exploratory section
clinical/neuroscience bridge hypotheses as P3/P4 where retained
```

The goal is authority truth-up, not archival erasure.

---

## 5. Forbidden positive replacements

E5 MUST NOT introduce:

```text
new P0/P1 claim
new exact Agency N&S theorem
new Bearer -> Agency bridge theorem
new universal freedom theorem
new universal responsibility theorem
new O2-A closure theorem
new O2-M moral solver
new ontology of evil
new universal clinical/psychiatric mapping
new AI moral-status criterion
new primitive direction/value theorem
new suffering admission theorem
```

If execution appears to require one of these, STOP and request author adjudication.

---

## 6. Protected zero-diff set

Semantic commit must leave zero-diff:

```text
Core/**
Core_Law/**
Governance/**
STATUS.md
SRT_AI_START.md
CANONICAL_REGISTRY.md
_SRT_D_VALUE_CANONICAL.md
_SRT_PSI_F_CANONICAL.md
_SRT_T_DIR_CANONICAL.md
_SRT_SYMBOL_TABLE.md
_SRT_CROSS_DOMAIN_MATRIX.md
Philosophy/SRT_Philosophy_Claim_Status.md
Philosophy/SRT_Ethics_PH_SS_Guardrails.md
Philosophy/_PHILOSOPHY_MACHINE_INDEX.md
AI/**
Neuroscience/**
Spirituality/**
Operations/Context_Bundles/**
Operations/Audits/data/srt_active_theory_nodes.json
```

The only exception is separately committed E5 audit/proposal/control-plane files already created before semantic execution; those are not part of the semantic commit scope test.

---

## 7. Commit structure

Required execution structure:

### Commit A — semantic owner

Exactly:

```text
Philosophy/SRT_Ethics_Agency.md
```

Suggested message:

```text
Truth-up Wave E5 ethics agency consumer
```

### Commit B — deterministic reading split

Regenerate only through `scripts/create_reading_split.py` using the existing source/split/id/title conventions.

Expected path family:

```text
Philosophy/Ethics_Agency_Split/README.md
Philosophy/Ethics_Agency_Split/*_Part*.md
```

The generator's deterministic shard count is authoritative. Current layout is two shards; do not force preservation of two if the semantic owner size changes the correct split.

Suggested message:

```text
Regenerate ethics agency reading split after Wave E5
```

No hand editing of generated shards.

---

## 8. Context Bundle / registry expectation

Formal expectation:

```text
Operations/Context_Bundles/* = NO DIFF
Operations/Audits/data/srt_active_theory_nodes.json = NO DIFF
```

Reason: the E5 owner is neither a direct Philosophy bundle source nor an active-theory-node owner.

Still run:

```text
uv run python scripts/build_srt_context_bundles.py --check
```

If formal tooling says a generated output must change, STOP and review the dependency assumption; do not synthesize a manual derivative commit.

---

## 9. Acceptance gates

Semantic acceptance requires all of:

```text
E5-01 owner remains canonical:false and P2-P4 bridge/history
E5-02 file-level current-authority override added
E5-03 Generative Ontology Spine added as current cross-owner anchor
E5-04 primitive Selection != G-hat_theta explicit
E5-05 exact Agency sufficiency remains OPEN / not established
E5-06 Agency candidate dimensions not presented as universal N&S
E5-07 Occlusion no longer canonically defines evil/responsibility
E5-08 O2-C != O2-A != O2-M preserved; O2-M OPEN
E5-09 Suffering cross-link no longer uses T-SUFF-4 as universal ethics theorem
E5-10 suffering still requires independent admission
E5-11 Irreversibility cross-link no longer restores strong retired T-IRR claims
E5-12 Agent=G-hat formula historical/proxy only
E5-13 metastability iff FreeChoice no longer universal theorem
E5-14 J-layer no longer automatically carries moral responsibility
E5-15 second-layer activation no longer exact universal responsibility locus
E5-16 absolute responsibility claim superseded
E5-17 biology/genes/social conditions may condition capacity/responsibility; no absolute exclusion
E5-18 brain/L2/embodied-operator identities retyped as P3/P4 bridge
E5-19 Ought-implies-can remains bridge candidate, not primitive ontology theorem
E5-20 depression/mania mappings are P4 hypotheses, not diagnosis/treatment theorem
E5-21 true-evil-does-not-exist no longer active SRT ontology conclusion
E5-22 occlusion-is-only-explanation no longer active conclusion
E5-23 legacy four criteria do not close O2-M
E5-24 AI Psi_f question remains open and non-phenomenology-defining
E5-25 no new P0/P1 claim introduced
E5-26 no current canonical/Core owner edited
E5-27 semantic commit changes exactly one semantic owner
E5-28 split regenerated generator-only in separate commit
E5-29 split strict freshness PASS
E5-30 Context Bundle freshness PASS with expected NO-OP
E5-31 registry / authority routing / hook-dependency checks PASS
E5-32 git diff --check / whitespace checks PASS
E5-33 PR-local frontmatter introduces no new warning debt
E5-34 full Governance Preflight PASS against live main
E5-35 replace-vs-return audit finds no old strong burden under new wording
```

---

## 10. Search-based fail checks

After semantic edit, direct hits for the following are allowed only inside an explicitly historical/superseded/quoted context with adjacent guard:

```text
absolute responsibility
Agent = G-hat_theta
FreeChoice iff
true evil ... ontologically does not exist
occlusion ... only explanatory framework
moral responsibility remains in J layer
true moral responsibility lies in second-layer activation
T-SUFF-4 ... structural ground ... must not be downgraded
T-IRR-1 ... asymmetric Psi_f payment as canonical universal
T-IRR-2 ... generic absorbing death boundary as universal
T-IRR-4 ... universal suffering cost theorem
```

Any unguarded current-positive hit = FAIL.

---

## 11. Main-divergence gate

Before semantic execution:

```text
fetch live main
compare current branch head vs live main / main-only delta
check exact E5 owner + authority dependency overlap
```

If live main touches:

```text
Philosophy/SRT_Ethics_Agency.md
Philosophy/Ethics_Agency_Split/*
or an authority dependency whose new content changes E5 interpretation
```

STOP before semantic edit and re-preland.

No auto-rebase is authorized by this spec.

---

## 12. Execution disposition

```text
E5 EXACT PATCH CONTRACT = DEFINED
NEW AUTHOR THEORY ADJUDICATION = NOT REQUIRED IF CONTRACT IS FOLLOWED
SEMANTIC EXECUTION = STILL REQUIRES EXPLICIT CONTINUATION / CODEX WORKTREE HANDOFF
MERGE #976 = NO
```
