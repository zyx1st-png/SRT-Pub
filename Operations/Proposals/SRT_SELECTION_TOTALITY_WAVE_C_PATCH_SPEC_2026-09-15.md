---
id: SRT-SELECTION-TOTALITY-WAVE-C-PATCH-SPEC-20260915
type: proposal
status: active
layer: operations
epistemic_layer: os
claim_mode: proposal
canonical: false
date: 2026-09-15
---

# Selection-totality Wave C patch spec

## Execution contract

This is the controlling execution source for Wave C. Apply it in a normal full working tree / local Codex.

Authorized canonical owner scope is exactly:

1. `Governance/SRT_CLAIM_LADDER.md` — §0A only
2. `_SRT_T_DIR_CANONICAL.md`

Do not edit other semantic owners in this pass. Do not enter Wave D. Do not merge PR #976.

Read first:

- `Operations/Handoffs/SRT_SESSION_CONTINUATION_2026-09-15.md`
- `Operations/Audits/SRT_SELECTION_TOTALITY_WAVE_B_FINAL_INDEPENDENT_REVIEW_2026-09-15.md`
- `Operations/Audits/SRT_SELECTION_TOTALITY_WAVE_C_PRELANDING_REVIEW_2026-09-15.md`
- `01_Source_Intuition/SRT_AUTHOR_ADJUDICATION_SELECTION_TOTALITY_NONFLAT_MONISM_2026-09-14.md`

## Fixed guards

```text
Selection-monist != Selection-flat;
primitive Selection != formed G-hat;
primitive Selection has no universal goal / value / continuation preference / fitness objective / consciousness;
B >= 2 primitively preferred over B <= 1: NO;
κ0 / ε_pg inheritance from primitive non-flatness: OPEN unless independently re-established;
Selection occurrence != value / legitimacy;
O1 / O2-C / O2-A remain separately typed downstream routes;
O2-M remains OPEN.
```

---

# A. Claim Ladder §0A corrective patch

## A1. Keep Gate 0's governance role

Preserve these functions:

- Gate 0 is a governance-canonical type / admission boundary;
- it carries no P-level;
- global notation cannot turn a lower-typed object into a primitive;
- semantic / evaluative / teleological content cannot enter primitive ontology by notation or cross-reference;
- Class B remains an admission gap, not a categorical prohibition.

Do not turn §0A into a second metaphysics owner.

## A2. Retype the opening rule

Replace any warehouse-like or object-kind reading with this bounded rule in substance:

```text
A primitive / L0-side claim may carry only contentless structural burdens already licensed by the current L0 / Spine owners.

Gate 0 governs admissibility and typing; it does not independently define primitive ontology.
```

Explicitly state:

```text
L0-side != pregiven possibility warehouse;
primitive Selection != operator acting on a prior inventory.
```

## A3. Replace the Class A / B table content

### Class A

Use only already-landed contentless structural burdens as examples:

- non-maximal indifference / minimum non-flatness;
- openness not exhausted or fully preclosed by completed determinate actuality;
- finite positional indexing without a prior formed operator / subject.

Add:

```text
The examples summarize current owner commitments; Gate 0 does not make them independent new primitives.
```

### Class B

Move / include the following as reference-, model-, realization- or derivation-dependent candidates unless independently re-established:

- probability measure / prior;
- entropy over a declared state space;
- reachability / accessibility ordering;
- `κ₀` anisotropy;
- `ε_pg`;
- `B >= 2` vs `B <= 1` option-count preference;
- any formal cost / gradient / direction whose value requires a model, reference structure or stronger realization.

State:

```text
κ0 / ε_pg are not forbidden;
they are not primitive by inheritance from O0;
their exact relation to primitive non-flatness remains OPEN unless separately adjudicated.
```

### Class C

Keep semantic / evaluative / teleological objects non-admissible as bare primitive content.

## A4. Rewrite `Ordering discipline`

Remove the current claim that `κ₀` and `ε_pg` are Class A primitive orderings and remove the primitive two-class preference `B >= 2` over `B <= 1`.

Replace with:

```text
structural ordering in a declared model != primitive value ranking;
structural ordering in a declared model != preferred endpoint / telos;
formal anisotropy does not inherit primitive status merely because it is contentless;
option-count preservation is not the primitive direction of Selection.
```

## A5. Retype declaration requirements

For situated / contentful objects, require the reference structures the object actually depends on.

Do not universally require a prior finite operator as if primitive Selection already had a formed chooser.

Preferred language:

- declared formed position / relation / operator interface **when the construct depends on one**;
- declared admissible / reachable domain when comparison requires it;
- history only when history-dependent;
- probability measure only when probability / entropy is used;
- horizon only for reachability / accessibility claims;
- any additional model index the claim actually uses.

## A6. Update Read-back rule

Keep `order`, `good`, `original intention`, convergence and direction as downstream / situated / bridge language only.

Replace the old protection of `ε_pg` as an existing bare-L0 structural role with:

```text
ε_pg may remain lineage / stronger-realization material;
it does not currently name the primitive direction of Selection or O0.
```

## A7. Update `What Gate 0 does not do`

Add explicitly:

```text
Gate 0 does not settle κ0 / ε_pg necessity or inheritance;
Gate 0 does not supply a primitive value direction;
Gate 0 does not make T_dir's reference direction primitive.
```

Keep O2-M and other unresolved normative questions outside Gate 0.

---

# B. `_SRT_T_DIR_CANONICAL.md` corrective patch

## B1. Add a Selection-totality authority override near the top

After the existing status / RC-A notes, add a visible current override:

```text
2026-09-15 Selection-totality override:
primitive Selection is normatively contentless;
T_dir does not read a universal order/value vector already contained in Selection;
T_dir is admitted only relative to an independently typed, declared direction signal in a formed system / relation / model;
T_dir never by itself establishes good, legitimacy, generative health or O2-M authority.
```

## B2. Rewrite §0

Keep the operational problem: SRT needs a variable for whether a formed system can access / use a direction relevant to its own current selection organization.

Remove the assumption that `value is already present in every Selection`.

## B3. Rewrite §1

Retitle if useful to something like:

`§1 Direction hiddenness / downstream value hiddenness`

Current canonical substance should become:

```text
primitive Selection is not intrinsically good, valuable or ordered toward a universal endpoint;
formed positions / relations / agents may acquire typed directional constraints;
a system may fail to read or use a direction that is already well-typed at that downstream level;
that failure is the narrow phenomenon T_dir can track.
```

Claims such as `nihilism is a diagnostic error`, `meaning = access to hidden universal value`, or `every Selection carries an order direction` must be downgraded to bridge / phenomenological hypotheses or removed from canonical proof load.

## B4. Rewrite §2 — source of direction

Replace `Selection intrinsically points toward order` with a direction-source discipline.

Allowed declared direction sources include, only when independently typed:

- a formed positional / relational constraint;
- an agent objective / policy / commitment;
- a constitutive relation-integrity criterion;
- a domain-specific bridge quantity;
- an explicitly declared model gradient.

Hard guard:

```text
O0 does not supply the direction;
κ0 does not automatically supply the direction;
ε_pg does not automatically supply the direction;
B-count does not automatically supply the direction;
T_dir does not create the direction it measures.
```

## B5. Rewrite Def-T-1

Use this semantic target:

```text
T_dir := a formed system's current ability to self-read and use an independently typed, declared direction signal for reorientation / recalibration.
```

Typing:

```text
no declared direction signal -> T_dir is not admitted / undefined for that claim;
declared direction exists but is inaccessible -> T_dir may be 0;
T_dir > 0 -> some degree of self-readability / reorientation access;
T_dir -> 1 -> high access only relative to the declared model / scale;
T_dir > 0 -/-> good / legitimate / healthy / morally correct.
```

Replace `自身秩序方向` with `declared direction signal` or equivalent.

## B6. Retain and retype §3.1 functional

Keep `R_self` and `A_reorient` as the useful v0 operational role.

Retype:

```text
Dir(Delta G,t)
```

as a declared model / formed-system direction signal, not a primitive ontology object.

The functional must not infer moral or ontological status from readability.

## B7. Retype §4 d / Psi_f / ii relations

Do not present these as universal necessary laws unless independently established by their own owner.

Preferred status:

```text
d, Psi_f and ii may be implementation constraints / proxies in current T_dir bridge models;
their exact necessity / sufficiency is OPEN outside those declared models.
```

Specifically demote:

- `d = 0 -> T_dir = 0` from universal theorem to current working/model hypothesis;
- `Psi_f up -> T_dir up` to a conditional mechanism hypothesis;
- `ii` as integration capacity to a domain/model candidate;
- the three-condition conjunction to an operational working gate, not universal ontology.

Do not alter the independent canonical meaning of d or Psi_f.

## B8. Scope §5

Keep `Psi_f_actual / Psi_f_felt` only as pathology / bridge operational concepts where Psi_f is already independently admitted.

Add:

```text
this split does not imply that every primitive Selection event must instantiate this pathology model.
```

## B9. Repair §6–§8 RC-A wording

Where these sections say `active / real Selection is replaced` or `script execution is not Selection`, replace the ontology claim with a downstream agency/reorientation claim:

```text
Selection occurrence may continue;
what can be displaced is formed self-readability, reorientation, agency-level revision or relation-sensitive navigation.
```

Do not imply habit / script / gradient / L2 automation = no Selection.

## B10. Update §9 explicit misreadings

Add explicit rejects:

```text
T_dir is not primitive value detection;
T_dir is not proof that a direction is good;
T_dir is not generative-health or legitimacy score;
T_dir cannot import κ0 / ε_pg / B-count as universal direction;
T_dir undefined is not the same as T_dir = 0.
```

## B11. Update §10 cross-reference table

At minimum:

- Claim Ladder §0A: direction source must pass Gate 0 / downstream typing;
- Core01: old A7/A11 primitive-strength readings are superseded by Wave B corrective definitions;
- L0 Metaphysics / Spine: primitive Selection is normatively contentless and does not contain a universal direction vector.

Remove / retype any cross-reference sentence that still says A7 fitness or A11 vulnerability is the ontological basis of T_dir.

## B12. Part II local authority guard

Keep Part II as bridge / theory-clarifying historical development, but strengthen its header:

```text
Part II does not define primitive Selection, O0, universal value, legitimacy, generative health or O2-M;
`proto-gradient`, `expected L0`, `higher order`, `value attractor`, `rise / degeneration` and similar language must be read as downstream / bridge hypotheses requiring a declared criterion;
κ0 / ε_pg / B-count cannot serve as universal direction sources by inheritance.
```

## B13. Local retrieval guards for §§11–18

Add a short local banner at the start of each section §11–§18, because connectors may retrieve these sections without the Part II header.

Banner substance:

```text
Bridge / theory-clarifying only.
This section concerns formed systems / relations under declared criteria.
It does not supply primitive value or a universal order direction.
```

## B14. Bounded semantic rewrites in §§11–18

Do not rewrite the entire historical exposition. Apply only enough local edits to prevent current canonical backflow.

Required replacements:

- §11: `value cannot be stored, only generated` -> candidate downstream reading about enacted / reconstituted value in formed systems; not primitive universal theorem.
- §12: `thing's proto-gradient` -> retained historical directional organization / relation-relative direction under a declared criterion; `expected L0` -> prospective relation / reachable future configuration, not a pregiven L0 object.
- §13: `value induction reads proto-gradient directly` -> hypothesis that agents can infer / sense downstream relational direction; not direct access to primitive value.
- §14: symmetry perception -> bridge hypothesis for detecting relational invariants / prospective constraints; no Noether-style proof of value.
- §15: `value is attractor` -> model-specific value-attractor hypothesis; `maximum order` cannot be universe-global optimum.
- §16: `rise / degeneration` -> criterion-relative generative / relational comparison; do not claim universal hierarchy.
- §17–§18: penetrative / absorbed Psi_f and development paths remain bridge hypotheses; do not imply direct contact with primitive L0 value.

## B15. Preserve useful T_dir core

Preserve:

- `T_dir` as a v0 operational proxy;
- `R_self` / `A_reorient` distinction;
- distinction from valence, confidence, semantic coherence, reward and commitment strength;
- pathology / civilization / practice sections as lower-hardness applications if properly scoped;
- RC-A no-absence-of-Selection guard.

---

# C. Acceptance gates

Report PASS / FAIL with textual evidence.

```text
W1  §0A no longer lists κ0 / ε_pg / B>=2 preference as primitive Class A by inheritance.
W2  §0A remains a governance admission rule, not a new metaphysics owner.
W3  primitive Selection remains value / goal / continuation neutral.
W4  T_dir requires an independently typed declared direction source.
W5  no declared direction -> T_dir undefined / not admitted, not automatically zero.
W6  T_dir > 0 does not imply good / legitimacy / generative health / O2-M.
W7  Def-T-1 no longer uses a universal intrinsic `order direction`.
W8  R_self / A_reorient functional survives without primitive-direction smuggling.
W9  d / Psi_f / ii relations are model / bridge hypotheses, not universal ontology laws.
W10 script / habit / L2 automation does not imply absence of Selection occurrence.
W11 Part II cannot back-define primitive value / proto-gradient / universal order.
W12 §§11–§18 each carry local bridge / authority guards.
W13 `expected L0` is not retained as a pregiven future object / warehouse.
W14 `maximum order` / value attractor language is model / criterion scoped.
W15 κ0 / ε_pg / B-count are not imported into T_dir as universal direction sources.
W16 O1 / O2-C / O2-A remain separately typed; O2-M remains OPEN.
W17 no owner outside the authorized two receives semantic edits.
W18 no new primitive ontology layer or symbol is introduced.
```

Any W1–W18 failure -> STOP before derivative regeneration / Wave D.

---

# D. Execution / verification

After editing the two owners:

```bash
git diff -- Governance/SRT_CLAIM_LADDER.md _SRT_T_DIR_CANONICAL.md
git diff --stat
```

Targeted grep at minimum:

```text
κ₀
ε_pg
B >= 2
B ≤ 1
value embedded
价值内嵌
指向秩序
order direction
maximum order
最大秩序
proto-gradient
expected L0
预期 L₀
T_dir = 0
Normative Closure
Fitness
primitive direction
universal direction
```

For every residual, classify as:

- current bounded statement;
- explicitly OPEN;
- model / bridge scoped;
- historical / superseded;
- FAIL.

Run repository-required deterministic derivatives / context bundles only after W1–W18 PASS. Do not hand-edit generated artifacts.

Run normal Governance Preflight.

Final required report:

1. live main SHA;
2. PR head before / after;
3. Wave C semantic commit SHA;
4. derivative closure commit SHA if derivatives change;
5. changed files;
6. W1–W18 table;
7. targeted grep residual classifications;
8. generator checks;
9. Governance Preflight result;
10. independent Wave C verdict: PASS / REVISE;
11. new theory blocker if any;
12. whether next synchronization wave is eligible;
13. `MERGE #976: NO` unless separately authorized.
