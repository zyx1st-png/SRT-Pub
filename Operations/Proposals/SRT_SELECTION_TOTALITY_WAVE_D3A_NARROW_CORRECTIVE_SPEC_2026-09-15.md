---
id: SRT-SELECTION-TOTALITY-WAVE-D3A-NARROW-CORRECTIVE-SPEC-20260915
type: proposal
status: active
layer: operations
epistemic_layer: os
claim_mode: proposal
canonical: false
date: 2026-09-15
---

# Wave D3-A narrow corrective specification

## Status

```text
D3-A semantic landing = NARROW REVISE
D3-A main owner architecture = substantively PASS
D3-B = HOLD
MERGE #976 = NO
```

This is a bounded corrective only. Do not reopen the D3-A architecture and do not enter D3-B.

## Corrective 1 — restore one deleted OPEN burden

Semantic owner scope:

```text
Core_Law/SRT_Individuation.md
```

Add one concise entry under the existing OPEN / burden section with a stable anchor such as:

```text
**Generative reselectability operationalization (OPEN)**
```

The content must preserve only this burden:

```text
Stable execution / recurrent standing
!=
generative reselectability automatically.

Still OPEN / P3 pressure:
- how to measure consequence return;
- how to measure revisability of comparison rules / boundaries / candidate-generation conditions;
- how to represent stake-space restructuring without collapsing it into bare d or sigma growth.
```

Hard guards:

```text
generative reselectability -/> subject-position by itself;
generative reselectability -/> Bearer by itself;
generative reselectability -/> consciousness;
generative reselectability != d;
generative reselectability != sigma;
no new symbol;
no new natural threshold;
no D3-B irreversibility claim.
```

Do not restore the old universal T-IND-2 gate, old epsilon direction, B-count, or `d > 0` subjecthood language.

## Corrective 2 — PH-CONSC04 routing-only anchor truth-up

File:

```text
Philosophy/hooks/PH_CONSC04_Phenomenal_Necessity_Zombie_Deletion_Integration_Hook.md
```

Update only the `landing_ledger` anchor for `Core_Law/SRT_Individuation.md` so that it points to stable current text preserving:

```text
subject-position does not establish phenomenality;
phenomenality / SRT-zombie remains OPEN.
```

Prefer an exact stable current anchor in the OPEN section rather than recreating the deleted numbered sentence.

Do not change the substantive payload or integration status.

## Corrective 3 — PH-IND03 anchor truth-up after restoration

File:

```text
Philosophy/hooks/PH_IND03_Simondon_Transduction_Operator_Structure_Integration_Hook.md
```

After Corrective 1 restores the OPEN burden, update the `landing_ledger` anchor for `Core_Law/SRT_Individuation.md` to the new stable generative-reselectability operationalization anchor.

Do not broaden the hook payload, alter other target states, or treat Simondon as a canonical definition source.

## Commit discipline

Preferred split:

Semantic corrective commit:

```text
Core_Law/SRT_Individuation.md
```

Suggested message:

```text
Restore D3-A generative-reselectability open burden
```

Then routing corrective commit:

```text
Philosophy/hooks/PH_CONSC04_Phenomenal_Necessity_Zombie_Deletion_Integration_Hook.md
Philosophy/hooks/PH_IND03_Simondon_Transduction_Operator_Structure_Integration_Hook.md
```

Suggested message:

```text
Retarget D3-A integration hooks
```

Do not touch `Philosophy/SRT_L0_Ontological_Status.md` in this corrective.

## Acceptance gates

```text
D3A-C1 generative-reselectability operationalization burden is explicitly restored as OPEN/P3;
D3A-C2 restoration does not define subject-position, Bearer, consciousness or Agency;
D3A-C3 no d/sigma collapse and no new symbol;
D3A-C4 old d/epsilon/B universal subject gate remains superseded;
D3A-C5 PH-CONSC04 anchor resolves to current phenomenality-open text;
D3A-C6 PH-IND03 anchor resolves to restored generative-reselectability OPEN text;
D3A-C7 hook substantive payload/state is otherwise unchanged;
D3A-C8 D3-B files remain untouched;
D3A-C9 L1 Formalism/Core22 remain untouched;
D3A-C10 git diff --check passes;
D3A-C11 strict Governance Preflight passes.
```

Any failure keeps D3-A at REVISE and D3-B at HOLD.

## Stop rule

After corrective commits and remote CI:

```text
STOP for fresh independent confirmation.
Do not enter D3-B.
Do not truth-up L1 Formalism yet.
Do not regenerate context bundles unless a later explicitly authorized consumer step requires it.
MERGE #976 = NO.
```
