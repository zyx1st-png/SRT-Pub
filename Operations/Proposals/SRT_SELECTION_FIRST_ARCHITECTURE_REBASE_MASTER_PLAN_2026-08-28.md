---
id: SRT-OPS-PROPOSAL-SELECTION-FIRST-ARCHITECTURE-REBASE-MASTER-PLAN-20260828
type: proposal
status: active
record_stage: active_planning
date: 2026-08-28
layer: meta
epistemic_layer: os
claim_mode: governance
claim_level: mixed_governance_P0_reading_P1_P2_candidates
canonical: false
ai_do_not_use_for_definition: true
dependency:
  - Governance/SRT_CANONICAL_FREEZE.md
  - Governance/SRT_EDIT_PROTOCOL.md
  - Governance/SRT_CLAIM_LADDER.md
  - Core/SRT_Core_21_Minimal_Axioms.md
  - Core/SRT_Core_21b_Constitutive_Theorems.md
  - Core/SRT_Core_22_Equations.md
  - Core/SRT_Core_21_Formal_Axioms.md
  - Core_Law/SRT_L0_Metaphysics.md
  - Core_Law/SRT_Reference_Ontology.md
  - Core_Law/SRT_Constitution_Seven_Theses.md
  - Core_Law/SRT_Individuation.md
  - Core/SRT_OPEN_TENSIONS.md
  - Bridge/SRT_Adjacent_Theory_Interface_Index.md
  - Operations/Audits/SRT_BOUNDED_RETRIEVAL_PROTOCOL_2026-08-08.md
  - 01_Source_Intuition/BOOK/Drafts_26Q/Q02_对象化.md
  - 01_Source_Intuition/BOOK/Drafts_26Q/Q05_选择不是挑选.md
  - 01_Source_Intuition/BOOK/Drafts_26Q/Q25_选择广于意识.md
  - Operations/Proposals/SRT_SELECTION_ARTICULATION_HARDENING_PACKET_2026-08-28.md
  - Operations/Audits/SRT_SELECTION_ARTICULATION_OWNER_SUBTRACTION_2026-08-28.md
  - Operations/Audits/SRT_SELECTION_ARTICULATION_EXTERNAL_NOVELTY_AUDIT_2026-08-28.md
  - Operations/Audits/SRT_BEARER_FROM_HERE_RESIDUAL_NOVELTY_AUDIT_2026-08-28.md
  - Operations/Proposals/SRT_NEURAL_UNITY_D2_DISCRIMINATOR_DESIGN_2026-08-28.md
tags: [ArchitectureRebase, SelectionFirst, WholeRepo, Actualisation, Individuation, Object, Bearer, Subject, Consciousness, ResearchProgramme]
---

# SRT Selection-First Architecture Rebase — Master Plan (2026-08-28)

> **Boundary:** planning-only, non-canonical programme record. It does not modify P0/P1/P2, register `articulation` as a canonical term, redefine `L0/L1/L2`, `G_hat_theta`, Stable ISP, bearer, subjecthood, consciousness, `d`, `Psi_f` or `T_dir`, or authorize novelty claims.
>
> **Default posture after review:** this is more likely a **dependency-propagation and subtraction programme** than a new-P0 programme. The theoretical yield, if any, is expected mainly at individuation / bearer structure and source-intuition recovery, not from manufacturing a new primitive amendment.

---

## 0. Executive decision and carve-outs

Pause this workflow **for new topic construction only**:

```text
new venue / call appears
-> search for an SRT novelty angle
-> patch theory to fit
```

Replace it with:

```text
read-only architecture/subtraction pass
-> only-if-needed owner edits
-> bounded propagation
-> research-question discovery
-> paper / venue selection
```

### Existing-manuscript carve-out

This pause does **not** apply to already-submitted manuscripts or journal-requested revisions.

- Frontiers 1837760 is already published; APC / billing is administrative and is outside this rebase.
- *Costly Selective Closure* / *Adaptive Behavior* is already submitted and not yet externally reviewed. Unless the journal explicitly requests it, the rebase must not expand its framing, add experiments, or silently rewrite manuscript dependencies while it is under editorial handling.

The current consciousness / neural-models route remains a **candidate downstream venue path**, not the organizing target.

---

## 1. Architecture being tested, not pre-declared

The strongest currently landed floor is P0-01 AM-A. The following sentence is quoted as the owner wording, not rewritten as rebase terminology:

> **non-objectified potential, under real constraints, acquires a determinate manifest distinction**

with two already-landed guards:

```text
no menu of completed latent objects
G_hat_theta is a formal role-carrier, not a prior chooser entity
```

Therefore the rebase starts with a presumption of **owner sufficiency**, not a presumption that P0 needs amendment.

The downstream question surface remains:

```text
primitive actualisation
!= persistence / historical efficacy
!= object individuation
!= finite position / mature operator
!= bearer consequence closure
!= selector / subject admission
!= phenomenal experiencer
```

This is a dependency audit, not an automatic evolutionary ladder.

---

## 2. Mandatory governance gate before any owner edit

### Step 0 — freeze / authorization check

Before editing any proposed owner:

1. check `Governance/SRT_CANONICAL_FREEZE.md`;
2. classify the edit under `Governance/SRT_EDIT_PROTOCOL.md`;
3. if the target is a frozen canonical anchor, treat any substantive wording change as **C-class / high-risk**;
4. record why documentation-only routing is insufficient before changing canonical text.

**Definition used throughout this programme:** `documentation-only` means recording the result in navigation, bridge, audit, operations or other non-owner surfaces **without changing theory-owner wording**. Any wording inserted into a frozen canonical anchor—including a scope note or clarification—is an **owner edit**, must pass Step 0, and consumes the B1 owner-edit budget.

Frozen anchors relevant to the current plan include at least:

```text
Core_Law/SRT_L0_Metaphysics.md
Core/SRT_Core_21_Minimal_Axioms.md
Core/SRT_Core_21b_Constitutive_Theorems.md
Core/SRT_Core_22_Equations.md
Core/SRT_Core_21_Formal_Axioms.md
_SRT_SYMBOL_TABLE.md
```

No rebase plan overrides the freeze merely by naming one of these files as a target.

---

## 3. Migration invariants

### G1 — primitive thinness

```text
actuality
!= persistence
!= memory
!= stable object
!= bearer
!= subject
!= phenomenality
```

### G2 — non-menu reading is already the default P0 reading

Do not define primitive Selection as choosing among completed latent options. Treat any proposed P0 addition as guilty until it shows a genuine owner gap.

### G3 — no prior chooser by relabeling

Do not replace `chooser` with an ontologically prior operator, perspective, embodied standpoint or theta-entity.

### G4 — model/state-space scope

`G_theta : S -> S` remains legitimate after variables, grain, boundary and admissible states are declared. It must not automatically be read as the literal pre-individual primitive ontology. `Core/SRT_Core_22_Equations.md` is a mandatory route in any audit of this point.

### G5 — occurrence before historical efficacy

Durable writeback, L2 sedimentation, habit and future-constraint modification are not universal P0 admission conditions.

### G6 — no new Unity primitive

`one`, `many`, `boundary`, `relation` and stronger unity types are downstream individuation questions, not a second floor beside Selection.

### G7 — bearer has not survived strong subtraction

Self-maintenance, autonomy, viability, history dependence and consequence return are prior-art territory. `non-outsourcing` / consequence ownership remains a high-risk residual candidate, not an established novelty.

### G8 — phenomenality stays open

```text
Bearer / Subject-position !-> Experiencer
```

### G9 — no novelty laundering

Vocabulary convergence with Whitehead, Peirce, Simondon, Barad, enactivism, process ontology, Damasio, IIT, GNWT or personal-identity literature is not SRT novelty.

### G10 — scientific distinctiveness is downstream

P0/P1 coherence is not D2/D3. Use Core 24 / PD-A rather than recreating their gates locally.

---

## 4. Revised programme shape: two tracks

The programme is no longer a 13-PR serial chain.

### Track A — read-only / subtraction first

Run before closing any R1 wording decision. These workstreams may proceed in parallel because they do not need canonical mutation.

#### R4-pre — source-intuition extraction

Priority:

```text
Q01-Q10
then Q11-Q17, Q25-Q28
```

For each candidate:

```text
source intuition
-> strongest structural claim
-> current owner
-> already-owned portion
-> residual question
-> neighboring-theory pressure
-> claim ceiling
-> failure / deletion condition
```

No source intuition lands canonically by itself.

#### R5-sub — existing comparison-owner subtraction

Do **not** build a parallel comparison owner.

Start from:

```text
Bridge/SRT_Adjacent_Theory_Interface_Index.md
Philosophy/SRT_IIT_Comparison.md
Philosophy/SRT_FEP_Comparison.md
Philosophy/SRT_Process_Logic_and_NPI_Comparison.md
Philosophy/SRT_MetaTheory_Framework_Comparison.md
Philosophy/SRT_Philosophy_Tradition_Comparison_PH_SS.md
Neuroscience_Annex/10_Integration_Theory_Comparisons.md
Operations/Audits/SRT_BOUNDED_RETRIEVAL_PROTOCOL_2026-08-08.md
```

The new increment is only a **dependency-graph comparison axis** attached beneath the existing adjacent-theory index after owner subtraction. The proposed comparison axes are:

```text
primitive / floor
potentiality
actualisation
one-many / boundary / relation
individuation
historical efficacy
habit / law
object identity / reidentification
autonomy / viability
bearer / consequence ownership
perspective / position
selector / subject
phenomenality
normativity / value
scientific-discrimination burden
```

These axes are an audit scaffold, not a second theory-comparison owner. If an existing comparison owner already answers an axis for a neighbor, R5 records the answer and moves on rather than recreating it.

Initial thin-neighbor priority:

```text
Peirce
structural realism
neutral monism
```

Do not re-read already-dense IIT/FEP/enactivism/process corpora merely to inflate coverage.

#### R2C-pre — bearer / non-outsourcing subtraction

Run the middle-level bearer target early because it can kill or sharply demote a major downstream workline.

Controlling question:

> After giving a rival persistent latent state, history dependence, consequence return, autonomous regulation and viability dependence for free, does non-outsourcing add any independent classification, derivation or prospective prediction?

PR #859 is a prerequisite owner/audit input. R2C execution must not pretend #859 is landed while it remains open.

Terminal outcomes:

```text
distinctive residual
translation / Outcome C
unresolved
spec-stage NO-GO
```

### Track B — serial edits only after Track A informs them

```text
R1 owner-sufficiency audit
-> only-if-needed R1A/R1B/R1C edits
-> R2A/B/D bounded individuation edits if warranted
-> R3 domain propagation
-> R7 public/frontstage synchronization
```

No R3 propagation occurs before relevant owner decisions land.

---

## 5. R1 early-exit rule

This rule is mandatory.

If R1 finds that P0-01 AM-A plus P0-04 already carries the non-menu / non-prior-operator reading without contradiction, then:

```text
R1A-R1C = documentation-only / scope-routing
```

and no P0 amendment is manufactured merely to make the rebase look substantive.

Because `documentation-only` is defined in §2 as **outside the theory owner**, this early-exit consumes **zero** B1 owner-edit slots. A scope note inserted into a frozen owner is not documentation-only under this programme.

Theoretical work then shifts to:

```text
R2 individuation / bearer
+
R4-pre source-intuition recovery
+
R5-sub external dependency subtraction
```

R1 may substantively edit a frozen owner only if all are true:

1. a current owner gap survives subtraction;
2. the gap cannot be fixed in a non-canonical or navigation layer;
3. the proposed change passes freeze / Edit Protocol C-class review;
4. downstream stale surfaces are enumerated before the edit;
5. the change does not reopen Gate 0, PD-A, B-A, RC-A, EX-A, ST-A or unrelated kappa/epsilon decisions by accident.

---

## 6. R1 routing

### R1A — P0 / L0 / actualisation owner-sufficiency audit

Read first:

```text
Core/SRT_Core_21_Minimal_Axioms.md
Core_Law/SRT_L0_Metaphysics.md
Core/SRT_Core_21b_Constitutive_Theorems.md
Core/SRT_Core_21_Formal_Axioms.md
```

Questions:

- Is any completed-menu reading still actually licensed by an owner, or only by stale exposition?
- Does any primitive occurrence wording require persistence or concern?
- Does the split index / theorem layer become stale if wording changes?

### R1B — operator / state-space scope

Read first:

```text
Core_Law/SRT_Reference_Ontology.md
Core_Law/SRT_Constitution_Seven_Theses.md
Core/SRT_Core_13a_Operator_Basics.md
Core/SRT_Core_22_Equations.md
_SRT_SYMBOL_TABLE.md
```

Question:

```text
formal primitive role-carrier
!=
pre-individuated mature operator
```

### R1C — occurrence / trace / L2 historical efficacy

Preserve:

```text
historical facticity
!= persistent trace
!= L2-grade historical efficacy
```

Route through existing L2 and PH-IND05 owners; do not create a second history taxonomy.

### R1 close condition

R1 cannot close before R4-pre has completed the first-pass Q01-Q10 extraction, so the core wording is not edited and then immediately reopened by source-intuition recovery.

---

## 7. R2 individuation map — extend the existing positive owner, do not reinvent it

R2 is a **partial-order generative audit**, not a total adjacency ladder and not a fresh taxonomy exercise.

`Core_Law/SRT_Individuation.md` already supplies a positive owner-side mechanism for the selector/subject/self-consciousness region:

```text
theta_t = theta_t^trace + theta_t^ext
sigma(P,t) = ||theta^trace|| / (||theta^trace|| + ||theta^ext||)

expanded selection
-> [candidate first transition]
-> subject-position
-> [candidate second transition]
-> self-consciousness condensation
```

The owner already types `T-IND-1` as P1-candidate, `sigma / Def-IND-1` as P2 canonical interpretation, the two transitions as P1-candidate / P2 strong candidate, and concrete threshold values as P3/P4. R2B must not spend a round rediscovering that maturity table.

The five role contrasts remain **guards against collapse**, not the primary R2 deliverable:

```text
occurrence vs persistent object
object vs bearer
bearer vs selector-position
selector-position vs stake-bearing subject-position
subject-position vs experiencer
```

### R2 named residual 1 — downward extension before sigma

The current sigma model is defined on an already parameterized selection pattern whose `theta` can be decomposed into trace and external contributions. It therefore does **not by itself derive** earlier object individuation or bearer closure.

R2 must ask:

> **Do object individuation and bearer closure each admit a genuine order parameter / transition structure, or are they different kinds of generative stabilization that should not be forced into the sigma phase-transition grammar?**

Required outcomes include:

```text
independent lower order parameter survives subtraction
sigma can be extended with a non-circular derivation
different non-phase-transition mechanism
translation to existing process/autonomy machinery
unresolved / NO-GO
```

Do not assume `object -> bearer -> sigma` is a total evolutionary sequence. The burden is to determine whether there is any derivable lower extension at all.

### R2 named residual 2 — upward extension beyond structural subject-position

The current individuation owner reaches subject-position and self-consciousness condensation, but HP-B-B keeps:

```text
structural bearing B_s
!=
phenomenal bearing B_p

B_s -> B_p ?
```

open. The second transition in `SRT_Individuation.md` is therefore **not** a phenomenality theorem.

R2 must ask:

> **What, if anything, would have to be added above the existing sigma/self-consciousness architecture to explain phenomenal experiencer status without defining phenomenality into structural bearing?**

A valid outcome may be that the bridge remains open; R2 is not required to close HP-B-B.

### R2A — objectification / physical individuality

Use Q02 + PH-IND04 Stage O + existing process / individuation owners. Subtract Simondon, process ontology and Peirce before claiming novelty. R2A is the main input to the **downward-extension** residual.

### R2B — finite position / operator individuation

`Core_Law/SRT_Individuation.md` already owns subjectless-selection -> perspective-bearing position dynamics and the sigma phase model. Do not create a duplicate `Selection -> Position` theorem or a duplicate maturity table.

For §4.3 specifically, R2B may **cite** the owner’s existing roles for `kappa_0` and `epsilon` as constraints on the current model, but must not re-derive, upgrade, repair or silently reopen them. Preserve exactly the current boundary:

```text
kappa_0 = structural precondition in the owner model, not a new driver
ε roles at the two transitions = P2/P3 conditional candidates
former P1-T07 unconditional theorem = withdrawn under ST-A
```

Any attempt to repair former P1-T07 or establish epsilon-pg independence requires its own separately authorized workline; it is not an R2B side task.

### R2C — bearer

Execute Track-A subtraction first. Only after its result is known may bearer language propagate into consciousness / AI framing. R2C is the second main input to the **downward-extension** residual.

### R2D — selector / subject / experiencer boundary

Use the existing sigma owner as the positive middle segment and HP-B-B as the upper stopping condition. No bearer or self-consciousness result may auto-promote phenomenality. R2D is the main input to the **upward-extension** residual.

---

## 8. R3 bounded domain propagation

Propagation is a repair phase, not an opportunity for new theory.

Audit only surfaces made stale by landed R1/R2 decisions:

- consciousness / neuroscience;
- AI;
- physics;
- value / ethics / social;
- book/prose where current canonical meaning is actually misrepresented.

### In-flight manuscript guard

While *Costly Selective Closure* is under editorial handling, R3 must not rewrite its submitted manuscript or use the rebase as a reason to broaden it. If owner changes create a future inconsistency, record it for post-editorial handling unless the journal requests revision.

---

## 9. Frontstage correction already identified

`01_Source_Intuition/CORE_REVERSALS.md` is non-canonical navigation, not a definition owner.

The risky phrase is not `constraint` itself: P0-01 explicitly permits actualisation **under real constraints**.

The stale compression is narrower:

```text
existence = stabilized trace ... concern ... history
```

because `stabilized trace` imports persistence and `concern` imports downstream stake; `cost/history` are over-thick if presented as universal causes of bare actuality. Future R7 correction should be a narrow clause-level edit, not a rewrite and not removal of `constraint`.

---

## 10. Programme budget and partial-success exits

### Budget checkpoint B1

After Track A + at most **four owner-edit PRs**, stop and review programme yield.

**B1 counter source of truth:** this subsection. Current count at R0 freeze:

```text
owner-edit PRs landed under this programme: 0 / 4
```

Every later rebase PR that lands a theory-owner edit must increment this count in the same PR and name the owner touched. Read-only audits, navigation/bridge/operations `documentation-only` records, and the frontmatter governance hotfix do not consume the budget.

At B1 ask:

```text
Has any P2-or-stronger residual increment survived owner + external subtraction?
Has any owner contradiction required a real repair?
Has any downstream paper candidate materially improved?
```

If all three are no, stop broad rebase propagation and move directly to topic discovery / publication triage.

### Partial-success exit PSE-1

If:

```text
R1 = documentation-only
AND
R2C = Outcome C / translation
```

then do not manufacture R2/R3 work. Record no-change disposition, run only necessary stale-surface cleanup, and proceed to R4/R5/R6 topic discovery.

### Partial-success exit PSE-2

If R4-pre identifies a strong candidate but it requires reopening P0, do not reopen R1 automatically. Create a separate author-gated decision packet with the candidate, owner subtraction, prior-art comparison and explicit reason the existing AM-A wording is insufficient.

---

## 11. Open-PR policy

Open PRs are not globally frozen by this programme.

Current policy:

- **#859**: prerequisite for R2C; adjudicate/merge/close before bearer execution.
- **#855**: bearer/processual material may inform R2C/R2A but must not be silently treated as landed while open.
- **#858**: attention-rival work remains independent and may strengthen R5/R2C subtraction; no need to block it.
- **#845 / #834**: consolidation/material archive work may proceed if they do not touch frozen rebase owners.
- **#828**: RC-A semantic burn-down remains independent; if it edits a file later targeted by R1/R3, merge or close it first to avoid competing owner patches.

General rule:

```text
same owner + overlapping semantics -> resolve older PR first
read-only/material-only + no owner overlap -> may proceed independently
```

---

## 12. Topic selection after rebase

Do not derive a paper from vocabulary novelty.

A candidate must pass, in order:

1. owner subtraction;
2. existing-comparison-owner subtraction;
3. external role-matched subtraction;
4. derivability from landed SRT;
5. either D2-ready discrimination or a clearly bounded explanatory/formal surplus appropriate to the venue;
6. only then venue fit.

The current consciousness Collection remains a possible destination, not a deadline that controls the theory.

---

## 13. Completion definition

The programme is complete enough for topic discovery when:

- primitive / persistence / L2 / object / bearer / subject / experiencer dependencies are readable without contradiction;
- frozen canonical anchors were either left unchanged under early-exit or edited through explicit C-class authorization;
- no parallel comparison owner was created;
- R4-pre intuition cards and R5-sub dependency comparison are available;
- bearer has an explicit residual / Outcome-C / unresolved disposition;
- the existing sigma individuation owner has been treated as the positive middle segment, with explicit downward and upward residual dispositions rather than duplicated;
- in-flight manuscripts were not opportunistically reframed;
- frontstage/public wording, if changed, points back to landed owners rather than defining theory itself.

The programme does **not** require every domain file to be rewritten. No-change and documentation-only dispositions count as successful completion when the owners were already adequate.
