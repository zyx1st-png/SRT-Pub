---
id: SRT-SELECTION-TOTALITY-WAVE-E4-COLLECTIVE-PATCH-SPEC-2026-09-16
type: proposal
status: active
layer: governance
epistemic_layer: proposal
claim_mode: patch_spec
---

# Wave E4 exact patch specification — Collective consumer + tower hardening truth-up

## 0. Purpose and execution shape

Wave E4 synchronizes the collective-selection family to the already-landed Selection-totality / One / Stable-ISP / suffering / formal-model boundaries.

It is deliberately staged:

```text
E4-A = Core_Law/SRT_Collective_Selection.md
E4-B = Core_Law/SRT_Collective_Tower_Hardening_Notes.md
       + deterministic registered reading split regeneration
```

Do not combine the two semantic owners in one semantic commit. Do not enter later Philosophy / AI / Neuroscience consumer cleanup in E4.

The author's current continuation instruction authorizes bounded execution of this exact spec without a new adjudication. If implementation reveals a theory choice not fixed here, STOP for author input. If a full worktree/generator is required, use Codex/local execution; do not simulate generated files manually.

---

# 1. Fresh live-main gate

Immediately before semantic execution:

```text
fetch latest live main
compare from 6c13dc5fb4a20c209b5a91455bb1fbca49705060 to latest
```

Verify no main-only edit to:

```text
Core_Law/SRT_Collective_Selection.md
Core_Law/SRT_Collective_Tower_Hardening_Notes.md
Core_Law/Collective_Tower_Hardening_Notes_Split/*
Core_Law/SRT_Generative_Ontology_Spine.md
Core_Law/SRT_One_Formation.md
Core/SRT_Core_21b_Constitutive_Theorems.md
Core/SRT_Core_21c_Bridge_Hypotheses.md
Core_Law/SRT_Suffering.md
Core_Law/SRT_L1_Formalism.md
Core_Law/SRT_L1_Hardening_Notes.md
Core_Law/SRT_Individuation.md
Core_Law/SRT_Occlusion_Dynamics.md
Core_Law/SRT_Irreversibility.md
Core/SRT_Core_22_Equations.md
_SRT_T_DIR_CANONICAL.md
_SRT_D_VALUE_CANONICAL.md
_SRT_PSI_F_CANONICAL.md
```

If overlap exists: STOP; report exact paths; no automatic rebase/merge.

---

# 2. E4-A exact semantic scope

Modify exactly:

```text
Core_Law/SRT_Collective_Selection.md
```

Protected zero-diff for this semantic commit:

```text
Core_Law/SRT_Collective_Tower_Hardening_Notes.md
Core_Law/Collective_Tower_Hardening_Notes_Split/*
all upstream owners listed in §1
CANONICAL_REGISTRY.md
Governance/SRT_CLAIM_MODE_AUDIT.md
_SRT_INDEX.md
STATUS.md
Philosophy/*
AI/*
Neuroscience/*
Spirituality/*
```

## 2.1 Frontmatter / role

Truth-up:

```text
status: draft_v0 -> draft
blanket claim_level: P1-candidate -> mixed P2 / P3 model-and-bridge surface
```

Keep `claim_mode: canonical`: this file remains the authority for collective-selection vocabulary/routing. Canonical ownership does not imply P1 epistemic standing.

Preferred role:

```text
L1 canonical owner for declared multi-process / collective-selection structures,
collective Stable-ISP standing candidates,
consequence-return models,
and conditional collective formal extensions.
```

Hard front guards:

```text
collective Stable ISP -/> collective subjecthood
collective Stable ISP -/> Bearer
collective Stable ISP -/> consciousness / phenomenality
collective Stable ISP -/> generative health
M(t) -/> health / legitimacy / evil / injustice
sigma_coll / d_c_coll / T_dir_coll -/> collective standing by themselves
formal closure -/> P1
```

---

# 3. E4-A Def-C-1 / Def-C-2 / Def-C-3

## 3.1 Def-C-1 shared L2 field

Preserve the structural object, but do not require every participant to be a subject or Bearer. If stable-ISP participants are assumed for a given model, state that as the declared domain rather than as a universal prerequisite for all collective interaction models.

Shared L2 / retained structure means a retained constraint/history can affect multiple candidate processes; it does not establish a higher-order One, collective Stable ISP or collective subject.

## 3.2 Def-C-2 consequence-return matrix

Preserve `M(t)` as a declared consequence-return/distribution matrix or model object.

Replace current-positive normative/health labels with descriptive readings:

```text
M_ii = same-position / same-process consequence-return entry
M_ij = cross-position consequence-return entry
symmetry/asymmetry = descriptive relation feature
row/column summaries = declared model diagnostics
```

Hard:

```text
M symmetry -/> reciprocity as moral verdict
M asymmetry -/> suffering
M asymmetry -/> evil / domination / injustice
M pattern -/> collective subjecthood
M pattern -/> health
M pattern -/> legitimacy
```

Burden externalization may be inferred only inside a declared burden/consequence model with boundary and transfer semantics. Suffering externalization requires independent suffering registration at affected positions.

## 3.3 Def-C-3 shared candidate space

Preserve as a downstream operational candidate-space construction.

Keep the anti-objectification guard:

```text
operational option profile -/> primitive Selection definition
```

Add that candidate-set dimension/non-collapse is a model property, not a universal value/health measure.

---

# 4. E4-A T-COLL-1 — collective Stable-ISP standing

Retype T-COLL-1 as:

```text
P2 structural/admission candidate for applying P1-T06 standing
at a declared collective/group-level candidate unit.
```

It is not a new P1 theorem and does not define collective subjecthood.

## 4.1 Upstream admission

T-COLL-1 may only be applied after a candidate group/unit/boundary has been declared for analysis. It does not solve the OPEN exact collective One/unit/scale-attribution problem and does not back-define One formation.

## 4.2 Four standing burdens

Align the four burdens with P1-T06 without adding health/normativity:

1. **group-level iterativity** — the declared group-level process repeatedly receives currently effective non-equivalent candidates rather than merely replaying a closed script;
2. **group-level perspective-bearing organization** — a structured group-level view / state organization participates in later selection; this is standing language, not `subjecthood`;
3. **group-level history-bearing writeback** — prior group-level outputs/relations remain materially effective in later group-level candidate/selection conditions;
4. **group-level continued selectability** — the same history-bearing group-level process continues receiving candidates and consequence return can enter that same process's later conditions.

`M(t)` may be one declared operational route for consequence return under 3/4, but:

```text
healthy M range -/> T-COLL-1 requirement
M symmetry -/> T-COLL-1 requirement
non-externalization -/> T-COLL-1 requirement
```

## 4.3 Hard result boundary

```text
T-COLL-1 PASS
-/> collective subject
-/> Bearer
-/> Agency
-/> consciousness / phenomenality
-/> moral status
-/> generative health
-/> legitimacy
```

Consensus / voting / institutional automation remain insufficient by themselves to establish standing, but their presence or absence also does not decide whether primitive Selection occurs.

---

# 5. E4-A T-COLL-2 structural diagnostic types

Preserve:

```text
Aggregation
Asymmetric Absorption
Collapsed into Higher L2 / closure-type retained-constraint pattern
```

as **P2 descriptive/model diagnostic types**, not normative classes.

Remove/demote:

```text
"规范判断" as owner-level verdict
"最危险"
direct structural evil identity
direct lethal-L2 identity
automatic suffering-externalization identity
```

Safe readings:

- Aggregation: group-level Stable-ISP standing not established; this is not itself pathology.
- Asymmetric Absorption: declared consequence/burden return is asymmetric under the chosen matrix/boundary; downstream injustice/domination/suffering interpretations require separate owners/evidence.
- Closure-type / higher-L2 pattern: declared retained constraints increasingly dominate selected group-level model coordinates; `sigma_sr^coll -> 1` may be one model extreme, not the definition or proof of pathology/lethality.

Do not rank combinations normatively inside this owner.

---

# 6. E4-A collective coordinates and ODEs (§4)

## 6.1 General scope

Retain the programme as P2/P3 conditional multi-process model candidates.

Every equation must be read under declared:

```text
group boundary / unit
component admissions
model scale and horizon
measurement map
coefficient meanings
domain criterion
failure conditions
```

## 6.2 `sigma_sr^coll`

Retain as boundary-relative, model-local, replaceable historical/endogeneity coordinate.

```text
sigma_sr^coll -/> collective standing
sigma_sr^coll -/> subjecthood / agency
sigma_sr^coll -/> natural health/pathology boundary
sigma_sr^coll -> 1 -/> lethal-L2 theorem
```

`Theta_shared_L2` placement in trace/ext is a declared attribution convention/model choice. Do not state that shared L2 is universally a self-reference amplifier.

## 6.3 participant weights `w_i`

Current `w_i = M column sum` language is too strong.

Use:

```text
w_i = declared model/measurement participation weights;
M-derived summaries may constrain or instantiate them in a specified model.
```

No unique intrinsic weighting follows from M or stake.

## 6.4 `d_c^coll`

Retain as declared collective access/correction/candidate-space model coordinate. `M_asym` terms can be candidate couplings, not universal causal identities.

```text
M_asym -/> d_c increase universally
sigma_coll -/> member B phase universally
```

## 6.5 `T_dir^coll`

No direction admission -> `T_dir^coll` and `T_dir_alg^coll` are undefined / not admitted for that claim.

Do not use narrative coherence, group agreement or model alignment as a substitute for an independently typed direction signal.

`DeltaPsi_f^{gap,coll}` may be a criterion-relative model gap; do not call it moral/social debt by equation alone.

The current lethal-collective-L2 equivalence must be retyped as a conditional model signature/candidate under separately declared lethality criterion, not an iff theorem.

## 6.6 collective burden / suffering branch

Default collective formal language should be `burden`, `mismatch`, `consequence load`, `externalization` unless suffering has independently been admitted at the relevant affected positions.

If `S_sig^coll` / `S_str^coll` are retained, label them conditional suffering-model coordinates after admission.

Hard:

```text
M_ext -/> suffering
M_asym -/> suffering
sigma_coll -/> suffering
signal suppression -/> S_str increase universally
signal suppression -/> conserved hidden suffering
```

No collective subject is required for distributed burden modeling.

## 6.7 individual <-> collective couplings

Retain as candidate coupling terms only. They do not establish exhaustive or universal causal laws.

Each downstream individual `d_c`, `T_dir` or `S` term inherits that owner's independent admission conditions.

---

# 7. E4-A former health/pathology region (§4.6)

Rename/retype the default mathematical regions as criterion-relative **model regimes** (e.g. closure/lock-in/stability/correction-access regime) rather than natural health/pathology attractors.

A stronger generative-health interpretation may be stated only as a B13 bridge under a separately declared generative-reselectability criterion.

Hard:

```text
mathematical attractor -/> pathology
stability -/> health
low signal -/> health
high T_dir -/> health
sigma range -/> health
```

Do not erase useful dynamical-regime mathematics; retype its interpretation.

---

# 8. E4-A T-PROJ-1^coll

Retype:

```text
P1-candidate -> P2 conditional multi-process projection/model result
```

## 8.1 Closure assumptions

C1-C5 remain model assumptions, not theorem consequences.

In particular:

```text
T-COLL-1 -/> compact/Lipschitz neighborhood automatically
MOC -/> exact M(t) matrix/Hessian identity
MOC -/> complete measurement closure
```

C3 must explicitly assume the needed boundedness/Lipschitz conditions rather than claim T-COLL-1 proves them.

C5 may say declared MOC proxies support/instantiate selected measurements under a specified mapping; it must not identify MOC output with the full M matrix or upstream Hessian without an additional bridge.

## 8.2 Component-first admission

`T-PROJ-1^coll` only projects components already independently admitted.

```text
T_dir branch requires direction admission
S branch requires suffering admission
sigma/d coordinates remain model-local
```

## 8.3 Mapping strength

`lambda_M tr M`, `gamma_asym ||M_asym||`, `nu_ext ||M_ext||` are candidate model correspondences/terms. Do not present them as uniquely derived canonical identities from Core22 merely because a closure assumption is written.

`T-PROJ-1^coll` cannot back-define Core22, Psi_f, d, T_dir, suffering, MOC or collective standing.

---

# 9. E4-A later sections

## 9.1 T-COLL-3

Preserve current ST-A status:

```text
P2/P3 conditional anti-closure candidate
not P1 theorem
```

`generative reselectability` remains stronger than structural stability and not sufficient by itself for overall health.

## 9.2 T-COLL-4

Preserve as P2/P3 downstream collective agency/revision guard. It does not define Selection, Stable-ISP standing, subjecthood, consciousness, freedom, moral responsibility or legitimacy.

## 9.3 Political/economic/spiritual interfaces

Change direct structural verdicts into routing statements:

```text
collective model pattern -> possible downstream evidence/input
```

not:

```text
collective model pattern = injustice / legitimacy / healthy community / evil
```

Do not edit the domain files during E4-A.

## 9.4 AI/platform

Keep structural effects on consequence-return and retained-constraint models. Remove class-based categorical membership/exclusion claims. AI inclusion in a declared collective unit requires independent standing analysis; failure of one Stable-ISP model does not prove impossibility.

---

# 10. E4-A acceptance gates

Report:

```text
E4A-01 file default no longer blanket P1-candidate
E4A-02 canonical ownership retained without epistemic upgrade
E4A-03 T-COLL-1 no longer uses collective subjecthood as standing condition
E4A-04 T-COLL-1 no longer uses health/non-externalization as P1-T06 requirement
E4A-05 T-COLL-1 does not prove Bearer/subject/Agency/consciousness/health/legitimacy
E4A-06 M(t) is descriptive/model object, not normative/health verdict
E4A-07 T-COLL-2 structural types retained without direct normative ranking
E4A-08 sigma_coll remains model-local/replaceable
E4A-09 w_i not uniquely derived from M/stake
E4A-10 d_c collective couplings are model-conditional
E4A-11 T_dir collective branch is direction-admitted only
E4A-12 suffering branch requires independent registration
E4A-13 no universal suppression -> S_str/conservation inference
E4A-14 mathematical regime does not equal health/pathology
E4A-15 T-PROJ-1^coll is conditional P2 only
E4A-16 C3/C5 are assumptions/mappings, not automatic consequences
E4A-17 component-first projection discipline is explicit
E4A-18 T-COLL-3 remains P2/P3 conditional
E4A-19 T-COLL-4 remains downstream P2/P3 guard
E4A-20 domain normative verdicts remain downstream
E4A-21 no new primitive/symbol/universal scalar introduced
E4A-22 semantic changed files exactly one
E4A-23 git diff --check PASS
```

Suggested semantic commit:

```text
Truth-up Wave E4-A collective selection consumer
```

STOP for E4-A independent review before E4-B semantic commit unless the executing environment can preserve distinct commits and review artifacts without scope drift.

---

# 11. E4-B exact semantic scope

Modify exactly:

```text
Core_Law/SRT_Collective_Tower_Hardening_Notes.md
```

Do not edit split shards manually in the semantic commit.

Protected zero-diff includes E4-A owner and all upstream authority owners.

## 11.1 Frontmatter / role

Truth-up:

```text
status: draft_v0 -> draft
claim_level: P1-candidate / P2-P3 -> P2 / P3 conditional hardening/model surface
```

Role:

```text
late-stage tower/nested mathematical and cross-scale model hardening;
not a P1 promotion surface and not a source of collective standing, subjecthood,
health, suffering or normativity admission.
```

---

# 12. E4-B recursive standing / projection guard

Every tower level that is treated as a collective Stable ISP must have that level's candidate unit/boundary and standing independently established under the E4-A T-COLL-1 reading.

Recursive construction itself does not bootstrap:

```text
One
Stable ISP
Bearer
collective subject
Agency
phenomenality / consciousness
suffering
```

Retype `T-PROJ-1^{coll,nested}` and related tower projection/family theorems as conditional P2 model results under declared closure assumptions.

---

# 13. E4-B `T-DELTA-1^coll`

Synchronize exactly to E3-B:

```text
Delta_coll = declared weighted summary/proxy
of selected admitted projections of Rhat_coll
```

Remove current-positive:

```text
Delta_coll ≡ ||Rhat_coll|| unconditionally
three components + M term total conservation
stake uniquely fixes collective weights
```

A stronger norm-equivalence/information-preserving relation needs explicit completeness/injectivity/metric/weight/residual-subspace assumptions.

Conservation/redistribution requires explicit:

```text
loss channels
unobserved channels
measurement map
transfer rule
horizon
boundary conditions
```

Collective weights are declared model/measurement parameters; independently typed stake data may constrain them but does not uniquely determine them.

---

# 14. E4-B tower suffering / burden language

Default cross-scale transfer object is burden / consequence load / constraint / model residual.

Use `S_sig/S_str` at a level only after independent suffering registration at the relevant affected position(s).

Hard:

```text
M_ext transfer -/> suffering transfer automatically
lower-level pathology label -/> upper-level suffering
spectral instability -/> suffering
Lyapunov instability -/> suffering
```

No collective phenomenality is assumed.

---

# 15. E4-B spectral stability

Keep spectral mathematics where valid, but retype interpretation:

```text
rho(T_loop) < 1 under stated linearization assumptions
= local asymptotic stability of the declared linearized feedback model

rho(T_loop) > 1
= linear instability of that model/equilibrium
```

Do not infer:

```text
rho < 1 -> healthy tower
rho > 1 -> pathological tower
unstable eigenvector -> civilization pathology / suffering theorem
```

Domain examples such as cultural lock-in may remain explicitly P3 illustrative mappings under separate criteria.

---

# 16. E4-B Lyapunov theorem scope

Resolve the existing local/global wording inconsistency.

A global exponential-stability statement is allowed only if the theorem explicitly supplies and assumes the required Lyapunov bounds / derivative inequality over a declared forward-invariant domain sufficient for that conclusion.

Otherwise state local exponential/asymptotic stability in the neighborhood where the assumptions hold.

Do not call mathematical stability `tower health` by itself.

Retype the current boxed four-axis health condition into a conditional mathematical-stability condition, with any generative-health reading separately routed through B13.

Remove generic `P1-candidate basis` / `path to P1` language.

---

# 17. E4-B family/universality language

Historical names `P-univ`, `Q-univ`, family universality, etc. may remain for citation stability only if their current role is made explicit:

```text
model-family invariants / sufficient structural conditions within the declared family
```

They must not be read as universal ontological truths, health conditions or P1 standing.

Recursive closure, layer-skip graphs and spectral/Lyapunov constructions are useful mathematical programmes; preserve them while narrowing their epistemic standing.

---

# 18. E4-B residual audit

Search at minimum:

```text
P1-candidate
升 P1
P1 health
健康
病理
pathology
healthy
suffering
S_str
S_sig
Delta
Rhat
守恒
conservation
stake
weight
T-PROJ
T-DELTA
T-FAMILY
T-TOWER-STAB
T-LAYER-SKIP
T-LYAPUNOV
spectral
Lyapunov
global
local
collective subject
Stable ISP
Bearer
Agency
legitimacy
evil
domination
lethal L2
civilization
```

Classify every current-positive hit as:

```text
P2 conditional formal/model result
P2 operational proxy
P3 domain interpretation/example
historical label
OPEN
FAIL
```

Direct FAIL examples:

```text
recursive projection -> Stable ISP/subject
Delta_coll <-> Rhat_coll unconditional equivalence
stake -> unique collective weights
signal suppression -> conserved burden/suffering
spectral stability -> health
spectral instability -> pathology/suffering
Lyapunov stability -> health
mathematical closure -> P1
```

---

# 19. E4-B semantic acceptance gates

Report:

```text
E4B-01 file default is P2/P3 conditional hardening/model surface
E4B-02 no generic hardening-to-P1 route remains
E4B-03 recursive tower does not bootstrap standing/subjecthood
E4B-04 nested projection is conditional P2
E4B-05 Delta_coll is summary/proxy, not unconditional R equivalence
E4B-06 no universal collective residual conservation
E4B-07 stake does not uniquely determine weights
E4B-08 burden transfer separated from suffering transfer
E4B-09 spectral result interpreted as mathematical stability only
E4B-10 spectral instability does not establish pathology/suffering
E4B-11 Lyapunov local/global scope internally consistent
E4B-12 Lyapunov result does not establish health
E4B-13 family/universality language model-scoped
E4B-14 no new primitive/symbol/universal scalar introduced
E4B-15 semantic changed files exactly one
E4B-16 git diff --check PASS
```

Suggested semantic commit:

```text
Truth-up Wave E4-B collective tower hardening
```

---

# 20. E4-B deterministic split closure

After the E4-B semantic commit, regenerate the registered split with the repository script from a clean full worktree:

```bash
python scripts/create_reading_split.py \
  Core_Law/SRT_Collective_Tower_Hardening_Notes.md \
  Core_Law/Collective_Tower_Hardening_Notes_Split \
  --id SRT-COLLECTIVE-TOWER-HARDENING-SPLIT-INDEX \
  --title "SRT Collective Tower Hardening Notes" \
  --force
```

The current topology is:

```text
README.md
00_Part01.md
01_Part02.md
02_Part03.md
```

Do not assume the part count must remain four. The formal generator may move section boundaries or change part count after semantic edits. Accept only the exact generator output and verify every changed file is inside the registered split directory.

Create one separate derivative-only commit containing the complete generator delta and no semantic owner changes.

Suggested derivative commit:

```text
Regenerate collective tower reading split after Wave E4-B
```

No manual shard edits.

---

# 21. Context Bundle / other derivative checks

After each semantic commit run repository governance/freshness checks. If Context Bundles or another registered deterministic derivative becomes stale, use its formal generator and record the exact resulting scope. Do not create synthetic changes if formal freshness is already PASS.

If an unexpected semantic dependency would require editing another owner, STOP and report; do not broaden E4 automatically.

---

# 22. Final governance checks

From the final E4 derivative head run at least the repository-prescribed equivalents of:

```text
git diff --check
registered longform split freshness / owner hash verification
Context Bundle freshness check
hook closure / dependency checks
strict Governance Preflight
```

Then inspect remote Governance Preflight after push.

Report separately:

```text
latest live main SHA
PR stored base SHA / workflow base SHA
main-only overlap result
E4-A semantic commit
E4-B semantic commit
E4-B derivative commit
final branch head
remote Governance Preflight status/conclusion
```

Do not claim payload-level current-base revalidation unless the workflow payload explicitly reports the actual latest live-main SHA.

---

# 23. Final independent review targets

Final E4 independent review must answer:

1. Was collective Stable-ISP standing preserved without sneaking in collective subjecthood?
2. Did standing/health/normativity remain separated?
3. Did M(t) retain structural usefulness without becoming a moral scalar?
4. Did collective burden remain distinct from suffering?
5. Did all T_dir branches retain direction admission?
6. Did T-PROJ-1^coll fall to the same conditional P2 discipline as E3-A?
7. Did Tower retain useful recursive/spectral/Lyapunov mathematics without P1/health/pathology overreach?
8. Is T-DELTA-1^coll fully synchronized with E3-B summary/conservation/weight boundaries?
9. Is the generated split an exact derivative reading aid with no authority drift?
10. Did any downstream domain consumer remain a known E5+ debt rather than being silently treated as fixed?

---

# 24. Hard stop

After E4-A semantic + review, E4-B semantic + deterministic split + final independent review:

```text
Wave E4 = FINAL PASS only if all E4A/E4B gates pass
E5+ = HOLD until separately prelanded
MERGE #976 = NO
```

At the present tool boundary, this exact spec is ready, but E4 semantic execution should move to Codex / a full worktree because E4-B is a large owner and its registered split must be regenerated by the repository script. Connector-only manual replacement is not an acceptable substitute for that deterministic execution.
