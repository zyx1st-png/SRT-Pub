---
id: SRT-POST-MIGRATION-WHOLE-TREE-SEMANTIC-CLOSURE-D-VALUE-CANONICAL-ADDENDUM-2026-09-18
type: proposal
status: active
layer: governance
epistemic_layer: proposal
claim_mode: binding_addendum
canonical: false
date: 2026-09-18
---

# Bounded addendum — canonical d-value consciousness/subject scope correction

> Source control record: PR #976 issue comment `5729003502`.

The whole-tree execution correctly STOPped on an out-of-scope active contradiction in `_SRT_D_VALUE_CANONICAL.md`.

## Authority verdict

This **does not require a new author theory adjudication**.

`_SRT_D_VALUE_CANONICAL.md` owns the definition and usage discipline of `d-value`. It does **not** own universal subjecthood, consciousness, phenomenality, Bearer, or self-consciousness admission.

Current controlling authority already fixes:

```text
Generative Ontology Spine:
  d > 0 -/> Bearer / subject
  Stable ISP -/> subject
  Bearer -/> subject / consciousness / phenomenality
  cognition / subject-position / phenomenality remain separately gated

Individuation:
  d > 0 -/> subject-position theorem
  d / Psi_f do not establish subjecthood, Bearer, consciousness,
  generative health, legitimacy or moral standing
  exact subject-position sufficiency = OPEN
  phenomenality = OPEN

Constitution_V1:
  stronger standing must be independently paid
```

Therefore the residual is a **canonical-owner scope overreach**, not a new theoretical choice.

## Addendum scope

Authorize exactly one additional semantic owner:

```text
_SRT_D_VALUE_CANONICAL.md
```

Do **not** reopen or redefine:

```text
Def-d-canonical
R/A/C stake gate
D_eff / Fisher proxy mathematics
d-vector / d-gate
d_max / d_mobile formulas except local consciousness overread
d-value direction / normativity guards already landed
```

## WTC-59 — top-level terminology / routing

Truth-up active phrases that describe `d` as an "意识带宽" or route `Def-d-canonical` as an "AI 意识门槛".

Allowed reading:

```text
d = stake-coupled concern / risk-sensitivity quantity
d may be used as one downstream feature in declared consciousness /
subjecthood evaluation models
d itself is not a consciousness or subjecthood admission gate
```

Historical aliases may remain only if marked historical.

## WTC-60 — §3 domain projection table

Keep the domain-specific `d` formulas / proxy status.

Remove categorical phenomenality verdicts derived from `d`, including active readings equivalent to:

```text
neural/cognitive d package = consciousness sufficient-and-necessary zone
social/collective d => no phenomenality
spiritual higher d => stronger consciousness
current AI d ~= 0 => unconscious
quantum/cosmic d value itself decides consciousness absence
```

The table may instead state:

```text
phenomenality / consciousness inference from d alone = NOT ESTABLISHED
domain-specific consciousness claims defer to independent owners
```

Negative anti-panpsychist guard remains valid in the narrower form:

```text
nonzero d -/> phenomenality
```

## WTC-61 — §3.1 anti-panpsychism clause

Preserve:

```text
SRT does not infer pan-consciousness from d
d is not phenomenality
```

Remove / demote:

```text
kappa_c1 = bare-consciousness admission threshold
d >= d_min + stable L2 closure = consciousness exists
kappa_c1.5 = consciousness alive
Layer 3 = consciousness participates in collective order
```

Retype as:

```text
kappa_c1 / kappa_c1.5 are downstream candidate coordinates
from the repaired consciousness-model family;
this d-value owner does not certify their universal necessity,
sufficiency or natural-boundary status.
```

The example table must not classify rock / AI / collective / frozen state / human consciousness solely from `d`, `Psi_f`, `G_hat`, `d_mobile`.

## WTC-62 — §5 misuse 1

Replace:

```text
d is a necessary condition of consciousness
and the conjunction with Psi_f / G_hat supplies the required package
```

with:

```text
d is not a consciousness meter and no universal necessary-or-sufficient
relation between d and consciousness is established.
In a declared downstream model, d may be one candidate evidence dimension.
```

## WTC-63 — d semantic scale

Remove subject admission from the scale:

```text
d=0 = non-subject
d=1 = minimal subject
d>1 = expanded-concern subject
d=0 vs d=1 = ontological non-subject/subject difference
```

Retain only d-internal / model-internal semantics, for example:

```text
d ~= 0 = no admitted / negligible stake-coupled concern in the declared model
d ~= 1 = approximately one dominant admitted concern dimension
d > 1 = multiple admitted concern dimensions / broader concern scope
```

No value on this scale establishes subjecthood, consciousness, Bearer or moral standing.

## WTC-64 — frozen-state residual

Truth-up:

```text
high d + d_mobile ~= 0 = pathological consciousness
```

to:

```text
within a declared d/d_mobile model this can indicate reduced
reorientation / mobility; consciousness or pathology requires
independent establishment.
```

## WTC-65 — no canonical backflow

After correction, the owner must explicitly preserve:

```text
d > 0 -/> subject-position
d > 0 -/> consciousness
d = 0 -/> non-subject
d = 1 -/> subject
d -/> phenomenality
d -/> Bearer
d -/> moral standing
```

## WTC-66 — exact semantic commit

Create a new semantic commit containing exactly:

```text
_SRT_D_VALUE_CANONICAL.md
```

Suggested message:

```text
Remove d-value consciousness and subject admission overreach
```

Do not amend or rewrite the existing local A-D commits.

## WTC-67 — derivative consequence

Do not touch Core/AI splits because this owner has no registered reading-split family in the current closure.

Run Context Bundle `--check` after the canonical correction.

If NON-NOOP, regenerate only through the official bundle generator and create a **new** generated commit on top of the existing local bundle commit. Do not amend `6186e278`.

## WTC-68 — continuation / fail-closed rule

The existing local unpushed commits A-G are retained as-is.

Because remote PR head remains `66a41d0e...`, do not reset or discard the local chain.

Create a local addendum record with this exact authorization, then the one-file canonical correction, then any required new Context Bundle regeneration.

After that:
1. re-run the four-class WTC-56 audit;
2. if another active contradiction appears outside the now-authorized 14 semantic owners, STOP again;
3. otherwise perform the final live-main fetch, overlap check, full Governance Preflight, and only then push.

Return WTC-01...WTC-68 matrix.

Still forbidden:

```text
whole-tree FINAL PASS declaration
merge #976
mark ready
new theory wave
automatic scope widening
```
