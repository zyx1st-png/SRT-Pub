---
id: SRT-SELECTION-TOTALITY-WAVE-E7-BOUNDED-RESIDUAL-REPAIR-SPEC-2026-09-17
type: proposal
status: active
layer: governance
epistemic_layer: proposal
claim_mode: exact_patch_spec
canonical: false
date: 2026-09-17
---

# Wave E7 bounded residual repair — exact patch specification

## 0. Binding basis

This bounded repair is required by:

`Operations/Audits/SRT_SELECTION_TOTALITY_WAVE_E7_FINAL_INDEPENDENT_REVIEW_2026-09-17.md`

It does not reopen the nine-owner E7 execution. It repairs three local standing leaks only.

Current reviewed execution head before review-control commits:

```text
6506ff58e36c4421f6d1f00af760e00804cfac1a
```

Control-plane review commit:

```text
ca5ae126337ad853f2c226a1e787128853930b70
```

## 1. Exact semantic repair set

Edit exactly these three semantic owners:

```text
Philosophy/SRT_Rights_Calibration_Hardening.md
Philosophy/SRT_Political_Rights.md
Philosophy/SRT_Political_Philosophy.md
```

Do not edit any other semantic owner during this repair.

Protected zero-diff set includes at minimum:

```text
Core/**
Core_Law/**
_SRT_D_VALUE_CANONICAL.md
_SRT_PSI_F_CANONICAL.md
_SRT_T_DIR_CANONICAL.md
_SRT_SYMBOL_TABLE.md
Philosophy/SRT_Social_Political_PH_SS_Guardrails.md
Philosophy/SRT_Social_Economics.md
Philosophy/SRT_Social_Economics_CompactCore.md
Philosophy/SRT_Political_Philosophy_CompactCore.md
Philosophy/SRT_Political_Casebook.md
Philosophy/SRT_Rights_Interface_Completion.md
STATUS.md
CANONICAL_REGISTRY.md
```

## 2. Repair A — Rights Calibration Hardening

Owner:

`Philosophy/SRT_Rights_Calibration_Hardening.md`

### A1 — T0-T3 compressed rules

Retain T0-T3 only as candidate diagnostic categories.

Replace active compressed wording equivalent to:

```text
Psi_f transfer starts diagnosis;
high d blockage triggers rights update.
```

with framework-relative wording such as:

```text
burden / cost / friction proxy asymmetry may trigger candidate structural review;
within a declared rights framework, wider-scope consequence blockage may support a rights-calibration proposal.
```

Do not call generic burden transfer canonical `Psi_f` unless full canonical semantics are independently established.

### A2 — U1-U4 ladder

Retain U1-U4 only as candidate design responses.

Truth-up local table / compressed slogans so they do not read as mandatory escalation or automatic rights generation.

In particular remove active standing from phrases equivalent to:

```text
minimum effective T3 update
changes who may represent L2 selection
generates a new protected selection-domain interface
small tension repair parameters / large tension rewrite gates / persistent tension change agents / new tension generate rights
```

Current reading must be:

```text
candidate escalation options under a declared framework;
no step is entailed by SRT ontology;
no automatic authorization or entitlement follows.
```

### A3 — M1-M3 / new-right generation compressed rule

The paragraph already says M1-M3 are candidate criteria. Make every compressed / operational rule no stronger.

Forbidden active wording:

```text
only when old rights cannot carry the tension is a new right generated
new rights are generated because X
```

Required standing:

```text
inside the declared framework, M1-M3 may support proposing an interface right;
independent normative and institutional admission remains required.
```

### A4 — protected object section

Retype section 8 from essence language:

```text
SRT new rights protect X and Y
```

into proposal language:

```text
this candidate rights architecture prioritizes / proposes protection for X and Y.
```

Do not define what SRT rights universally are or must protect.

### A5 — high-d process model

Retype `Def-Highd-CoupledCapacity` and nearby statements as a candidate process / deliberative model only.

Do not redefine canonical `d` in this bridge.

Required guard:

```text
this model does not define canonical d;
high d -/> authorization / legitimacy / office / priority / standing.
```

Replace the open question “when d-tendency may safely guide authorization scope” with research language that does not presuppose that `d-tendency` should guide political authorization at all.

Example:

```text
Open: whether any empirically validated d-related proxy adds useful information to a separately justified delegation procedure, and under what failure conditions.
```

## 3. Repair B — Political Rights

Owner:

`Philosophy/SRT_Political_Rights.md`

### B1 — d-tendency anti-capture claim

Retype the claim that d-mimicry self-consumes and true d-tendency inevitably emerges.

Required standing:

```text
empirical / model hypothesis;
not guaranteed by large sample size;
requires operationalization and adversarial testing.
```

Do not claim convergence to a “true” political trait without a measurement model.

### B2 — voting section title and mechanism

Keep voting-as-d-calibration only as a historical / candidate model.

The local section title, process diagram and prose must visibly state:

```text
candidate model / empirical hypothesis
```

not an established validation mechanism.

### B3 — voting effectiveness table

Replace categorical entries such as:

```text
validates d-tendency
effective
noise-resistant / capture-resistant
validation failure
```

with hypothesis / expected-signal wording, for example:

```text
candidate model predicts higher signal fidelity under X;
requires empirical validation;
potential failure mode under Y.
```

`Psi_f_felt / Psi_f_actual` may be retained only if the file establishes their operational semantics. Otherwise use experienced-outcome / burden / consequence proxies and mark any mapping to canonical `Psi_f` as OPEN.

### B4 — “unified failure explanation”

Retype as a model-specific failure hypothesis, not the explanation of voting failure in general.

### B5 — democracy comparison

Retain “low-precision implementation” only as historical / candidate comparative framing. Do not state or imply that democracy is validated by SRT or that any voting result measures political truth / legitimate authority.

## 4. Repair C — Political Philosophy long owner

Owner:

`Philosophy/SRT_Political_Philosophy.md`

### C1 — people / representation prose

Retype universal wording equivalent to:

```text
representing the people must be continuously validated by procedure and feedback;
claiming to be the people's natural spokesperson means political pathology has begun.
```

into candidate political-theory language:

```text
a declared anti-monopoly / delegation framework may treat revocable procedure and feedback as candidate evidence;
claims of natural or unrevisable representation may trigger a pathology / closure review under that framework.
```

No universal political legitimacy theorem follows from SRT ontology.

### C2 — emergency authority section

Retype the section title and prose equivalent to:

```text
crisis authority can only be functional authorization;
emergency authority can expand;
its legitimacy comes only from X.
```

Required standing:

```text
within a declared emergency framework, functional / scoped / temporary delegation is one candidate model;
SRT ontology alone does not determine whether authority should expand, who may hold it, or what makes it legitimate.
```

### C3 — pathology / transformation prose

Search the long owner for local universal forms including:

```text
must
only / only comes from
once X, political pathology begins
SRT's priority criterion
SRT requires
```

Where these make political legitimacy / authority / pathology / required-action claims, convert them to candidate / declared-framework standing.

Do not weaken negative ontology guards such as:

```text
L2 stability -/> legitimacy
high d -/> political authority
O2-M = OPEN / HOLD
```

### C4 — final boundary section

If retained, phrase “institutional direction” as candidate political / institutional research direction, not an SRT-wide political programme.

## 5. Derivative rule

The five Political Philosophy split shards have already been independently reviewed as no stronger than the intended repaired owner. Their content is not reopened by default.

Because `Philosophy/Political_Philosophy_Split/README.md` binds to the source owner hash, after editing `SRT_Political_Philosophy.md`:

```text
review all five shards for exact repaired-claim leakage = required
content edits = expected NOOP
README owner bytes / SHA refresh = required
```

Authorized derivative file by default:

```text
Philosophy/Political_Philosophy_Split/README.md
```

If an exact stale claim requiring shard content modification is discovered, STOP and report it rather than silently widening the bounded repair.

`Philosophy/Social_Economics_Split/**` must remain zero-diff.

## 6. Context Bundle rule

After semantic + metadata repair:

```text
run official Context Bundle --check
```

If NOOP:

```text
no bundle commit
```

If NON-NOOP solely because authorized repaired inputs changed:

```text
run official generator
commit only official Operations/Context_Bundles outputs
require final --check PASS
```

Do not modify bundle generator or unrelated inputs.

## 7. Required residual search

At minimum search repaired files for active forms of:

```text
才触发权利更新
权利更新触发点
才生成新权利
SRT 新权利保护
minimum effective T3 update
generates a new protected
Def-Highd-CoupledCapacity
may safely guide authorization
真实d倾向最终浮现
有效验证d倾向
投票的有效边界
验证失效
代表人民.*必须
天然发言人.*政治病理
危机中的权威只能
紧急状态中的权威可以扩大
合法性.*只来自
```

Residual occurrence is allowed only when locally marked as:

```text
historical
candidate
hypothesis
quoted / superseded / rejected
```

and does not recreate a universal rights / legitimacy / authority solver.

## 8. Fresh-main / governance requirements

Before edit and before final governance:

```text
fetch latest main
compare main-only movement against three semantic owners + Political split README + controlling owners
if overlap: STOP / NO AUTO-REBASE
```

Then require:

```text
git diff --check
exact semantic file-set check
protected-zero-diff check
Political split shard read-only review
refresh_split_metadata.py --check with inspected would-change set
strict split metadata / freshness PASS
Context Bundle --check PASS
frontmatter changed-file validation
baseline monotonicity
hooks / dependencies
registry consistency
authority routing
status rule
material log
book split
active-theory-node check
full Governance Preflight against fresh live main
```

## 9. Commit discipline

Expected commits:

```text
A. semantic repair — exactly 3 owners
B. derivative metadata — Political_Philosophy_Split/README.md only
C. optional Context Bundle generated commit — only if NON-NOOP
```

No STATUS / Registry / upstream canonical / Freeze-A edit.

## 10. Stop condition

After push + validation:

```text
STOP
E7 FINAL PASS = NOT YET
```

Final pass remains reserved for a second focused independent review of the bounded repair.

Do not:

```text
enter Freeze-A
merge #976
mark ready
```
