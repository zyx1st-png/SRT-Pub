---
id: SRT-SELECTION-TOTALITY-P0-REFERENCE-CONSISTENCY-MATRIX-20260914
type: audit
status: active
date: 2026-09-14
layer: meta
epistemic_layer: os
claim_mode: audit
canonical: false
ai_do_not_use_for_definition: true
research_mode: U
dependency:
  - 01_Source_Intuition/SRT_AUTHOR_ADJUDICATION_SELECTION_TOTALITY_NONFLAT_MONISM_2026-09-14.md
  - Core/SRT_Core_21_Minimal_Axioms.md
  - Core_Law/SRT_Reference_Axioms.md
  - Core_Law/SRT_Reference_Ontology.md
  - Core_Law/SRT_Reference_Dynamics.md
tags: [Selection, P0, Reference, Consistency, CanonicalPlanning]
---

# Selection-totality P0 / Reference consistency matrix

## Overall verdict

```text
P0 ACTUALISATION KERNEL: KEEP
P0 DOMAIN/OUTPUT METAPHOR: RETYPE
PRIMITIVE SELECTION = GHOST OPERATOR: REJECT
REFERENCE AXIOMS: SEMANTIC REWRITE REQUIRED
REFERENCE ONTOLOGY: SEMANTIC REWRITE REQUIRED
REFERENCE DYNAMICS: PRIMITIVE/REALIZATION SPLIT REQUIRED
HALF-MIGRATED LANDING: NOT ACCEPTABLE
```

## A. `Core/SRT_Core_21_Minimal_Axioms.md`

### Selection-First Framing Note
Current burden:

```text
latent possibilities become manifest through constrained selection;
repeated manifestations harden into future constraints.
```

Disposition: **RETYPE**.

Candidate:

```text
Selection reality is open beyond any completed actuality;
actualising Selection institutes determinate manifestation;
retained effects of prior Selection can constrain later Selection.
```

Avoid treating latent possibilities as a stock before Selection.

### P0-00 vocabulary gate

Current:

```text
L0 = latent / unselected possibility domain;
L1 = manifest / selected reality slice;
L2 = convergence / sedimented selection-history domain.
```

Disposition: **REWRITE vocabulary semantics**.

Candidate:

```text
L0 = open / non-preclosed generative aspect of Selection relative to determinate actuality;
L1 = determinate manifest actuality aspect of Selection;
L2 = retained / sedimented Selection efficacy constraining later Selection.
```

Keep symbols; do not require three substances / containers.

### P0-01 Primacy of Selection

Current formula:

```text
exists x iff x in Range(G-hat)
```

and prose `existence is output/image of selection`.

Disposition: **KEEP primitive actualisation; RETYPE formula role**.

Needed guard:

```text
Range(G-hat) representation
= formal rendering of determinate actuality where an operator model is admitted;
!= universal proof that primitive Selection is an operator transforming a prior domain.
```

Candidate metaphysical wording:

```text
manifest existence
= Selection under determinate actuality.
```

### P0-02 occurrence / persistence boundary

Disposition: **KEEP**.

Retype only container language (`anchored into L1`) where needed.

Core distinction survives:

```text
actual occurrence
!= persistence
!= Stable ISP.
```

### P0-03 irreversible trace

Disposition: **KEEP CLAIM / RETYPE L1-L2 CONTAINER LANGUAGE**.

Candidate:

```text
when prior Selection remains historically effective,
its occurrence cannot be treated as never having occurred;
reversal is a new Selection relation / event.
```

Do not imply every Selection must leave durable L2 trace.

### P0-04 operator well-formedness

Disposition: **RETYPE FORMAL STATUS**.

Problem:

```text
primitive subjectless Selection
```

cannot ontologically depend on an already formed Ghost Operator.

Safe role:

```text
Any formal operator representation used to model Selection must be well formed.
```

not:

```text
Selection exists only because an operator object exists.
```

## B. `Core_Law/SRT_Reference_Axioms.md`

### A1 Existential Priority

Current:

```text
Existence = Selection(P)
```

Disposition: **RETYPE AS ASPECT RELATION**.

Candidate formal gloss:

```text
Existence_actual
:= the determinate-manifest aspect of Selection.
```

Do not treat `P` as completed object inventory.

### A1-C1 unselected states / L0 potential superposition

Disposition: **REWRITE**.

Reason: this reifies unselected possibilities as preformed states.

Use Selection-relative background / unexhausted openness instead.

### A2 anchoring boundary

Disposition: **KEEP**.

### A3 causality as projection

Disposition: **REVIEW BUT NOT PRIMARY BLOCKER**.

It can remain a downstream interpretation if `high-dim Selection process` is not reified as a separate hidden object.

### A4 Embodiment Necessity

Disposition: **RETYPE SCOPE**.

`All valid G-hat require finite theta` may apply to embodied / formed operator realizations; it must not imply primitive Selection has an embodied chooser.

### A5 Normative Closure / L2 fixed point

Disposition: **REWRITE / DEMOTE UNIVERSALITY**.

`L2 = stable fixed points` cannot remain the exhaustive ontology of Selection sedimentation.

Use fixed points as one strong realization / stabilization model of retained Selection-history, not the universal definition of L2.

### A6-A12

Disposition: **SEPARATE AUDIT / DO NOT LET THEM BACK-DEFINE SELECTION**.

Many are bridge/high-commitment legacy claims. Selection-totality landing should add a directional guard rather than silently validate them.

## C. `Core_Law/SRT_Reference_Ontology.md`

### O1 L0 latent domain

Current:

```text
unselected possibility field;
L0_abs = absolute potential source / chaos potential.
```

Disposition: **MAJOR REWRITE**.

Candidate:

```text
L0 notation tracks the open / non-exhausted generative aspect of Selection
relative to a determinate manifestation / model cut.
```

No absolute possibility warehouse is required.

### O1b L0_rel

Disposition: **RETAIN AS MODEL-RELATIVE ACCESSIBILITY CONSTRUCT**, not primitive ontology.

### O2 L1 manifest domain

Current:

```text
L1(t) = G-hat_theta[sigma(t)]
```

Disposition: **RETYPE FORMULA ROLE**.

Use as model realization of actualisation, not proof that L1 is operator output from a prior stock.

### O3 L2 vergence domain

Current:

```text
stable attractor / fixed-point domain.
```

Disposition: **RETYPE AS SPECIAL STABILIZATION REALIZATION**.

General L2-side burden:

```text
prior Selection remains effective in later Selection conditions.
```

Attractor/fixed-point forms are stronger implementations.

### kappa stabilization ontology

Disposition: **KEEP AS MODEL / FORMAL REALIZATION AFTER CORE_12A REPAIR**.

Do not make `kappa` phase map define the primitive ontology.

## D. `Core_Law/SRT_Reference_Dynamics.md`

### D1 Ghost Operator

Current:

```text
G-hat_theta : S -> S
```

Disposition: **KEEP AS FORMAL REALIZATION INTERFACE**.

Add hard guard:

```text
G-hat_theta != primitive Selection itself.
```

### D2 `essence = Fundamental Attention`

Disposition: **REWRITE / DEMOTE**.

Attention, resolution, intentional vector and goal-like structure are formed / embodied / domain-level properties, not the essence of primitive subjectless Selection.

Candidate:

```text
G-hat_theta may model a formed Selection organization with domain-specific attention / resolution / vector structure.
```

### D3 divisive normalization

Disposition: **KEEP AS REALIZATION CANDIDATE**.

Existing note already says it is one implementation prototype. Preserve that demotion.

### survival gate / agency / intentional vector formulas

Disposition: **KEEP DOWNSTREAM / ADD NON-BACK-DEFINITION GUARD**.

They must not define Selection globally.

## E. Cross-file consistency requirements

An eventual patch must satisfy all of:

```text
1. primitive Selection does not require a prior possibility inventory;
2. primitive Selection does not require a prior Ghost Operator;
3. L0/L1/L2 symbols remain usable without being three substances;
4. actualisation can be represented by operator formulas without being ontologically caused by the formal operator;
5. history can constrain later Selection without every event producing durable history;
6. fixed points / attractors are realizations of L2-side stabilization, not the universal definition of sedimentation;
7. attention / intention / agency are downstream Selection organizations, not primitive Selection essence;
8. no formal interface back-defines the metaphysical owner.
```

## F. Landing order

To avoid a half-migrated ontology:

```text
Wave 1
L0 Metaphysics + Core21A + Generative Ontology Spine

Wave 2
Core01 + Core12a + Reference Axioms + Reference Ontology + Reference Dynamics

Wave 3
Claim Ladder §0A + T_dir

Wave 4
symbol / registry / cross-domain / Constitution / split / generated synchronization
```

These waves may be separate commits but should be reviewed as one atomic semantic migration before merge.

## G. Fail conditions

Reject a patch if one surface still says, without explicit realization qualification:

```text
absolute latent possibility warehouse;
primitive Selection = embodied operator;
existence is merely operator product;
L2 universally = fixed point;
attention / intention = essence of primitive Selection;
primitive non-self-erasure preference;
T_dir proves universal hidden value.
```

## H. Current status

```text
Selection-totality semantics: author-fixed noncanonical direction;
P0/reference migration architecture: stable candidate;
canonical edits: still HOLD pending independent review of the 10-owner plan.
```