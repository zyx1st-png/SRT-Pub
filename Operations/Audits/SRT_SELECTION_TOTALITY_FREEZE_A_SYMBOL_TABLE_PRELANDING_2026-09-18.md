---
id: SRT-SELECTION-TOTALITY-FREEZE-A-SYMBOL-TABLE-PRELANDING-2026-09-18
type: audit
status: active
layer: governance
epistemic_layer: operations
claim_mode: prelanding_review
canonical: false
date: 2026-09-18
---

# Freeze-A — symbol-table authority-sync prelanding review

## Verdict

```text
E7 = FINAL PASS
FREEZE-A READ-ONLY AUDIT = PASS
FREEZE-A SEMANTIC EXECUTION = REQUIRED / BOUNDED
NEW AUTHOR THEORY ADJUDICATION = NOT REQUIRED
MERGE #976 = NO
MARK READY = NO
```

## Exact debt

The live symbol table still contains two stronger rows than their current owner permits:

```text
sigma_sr^sub
  "Subject-Position Entry Threshold"

sigma_sr^self
  "Self-Consciousness Condensation Threshold"
```

The current authority is `Core_Law/SRT_Individuation.md`, under the Generative Ontology Spine.

Current authority states:

```text
sigma_sr = P2 model-local / replaceable coordinate
sigma_sr^sub/self = P3/P4 threshold coordinates
crossing sigma_sr^sub may count as model-local evidence only
crossing sigma_sr^sub -/> subject-position admission
T-IND-2 != ISP entry
exact subject-position sufficiency = OPEN

sigma_sr^self may track candidate second-order condensation
subject-position must already be independently established
crossing sigma_sr^self -/> self-consciousness universally proved
phenomenality remains OPEN
```

Spine additionally fixes:

```text
finite positionality -/> subject
One -/> subject
Stable ISP -/> subject
Bearer -/> subject
positive subject-position gate = OPEN
```

Therefore the symbol table may register these coordinates, but must not name them as admission thresholds.

## What is already safe

No change is required to:

```text
sigma_sr
sigma_sr^health
sigma_sr^coll
Usage Rule 12
Tier-3 classification
```

Those surfaces already state model-local / replaceable / criterion-relative scope and block natural-boundary or bearer/selector readings.

## Separate later debt — not Freeze-A

The rows:

```text
kappa_c1
kappa_c1.5
```

still inherit stronger consciousness-window language from `Philosophy/SRT_Consciousness_Conditions.md`. This is not a symbol-table-only mismatch: the source owner itself still carries the older consciousness-threshold theory.

Do not hide that upstream debt by weakening only the registry row during Freeze-A.

Register for post-Freeze whole-tree closure:

```text
CONSCIOUSNESS-FAMILY AUTHORITY DEBT
  Philosophy/SRT_Consciousness_Conditions.md
  corresponding kappa_c1 / kappa_c1.5 symbol rows
  relevant downstream consumers
```

Other neuro / phenomenology symbols are outside this bounded Freeze-A unless an exact authority conflict is independently shown.

## Context Bundle consequence

`_SRT_SYMBOL_TABLE.md` is an explicit input to `Operations/Context_Bundles/SRT_CONTEXT_BUNDLE_SPINE.md`.

Therefore a real Freeze-A semantic edit is expected to make Context Bundle freshness NON-NOOP and requires the official generator:

```bash
uv run python scripts/build_srt_context_bundles.py
uv run python scripts/build_srt_context_bundles.py --check
```

Generated bundle files must be committed separately from the semantic owner edit.

## Freeze-A scope

```text
semantic owner:
  _SRT_SYMBOL_TABLE.md

authorized semantic rows:
  sigma_sr^sub
  sigma_sr^self

upstream owners:
  read-only

Context Bundles:
  generated derivative only
```

No STATUS, Registry, Core, Core_Law, Philosophy, split, d/Psi/T_dir owner, or other symbol-row edit is authorized.

## Gate

```text
NEXT = FREEZE-A EXACT TWO-ROW PATCH + CONTEXT-BUNDLE REGENERATION
FREEZE-A FINAL PASS = NOT YET
```
