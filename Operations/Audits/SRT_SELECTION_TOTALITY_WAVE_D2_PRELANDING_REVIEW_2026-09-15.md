---
id: SRT-SELECTION-TOTALITY-WAVE-D2-PRELANDING-REVIEW-20260915
type: audit
status: active
layer: operations
epistemic_layer: os
claim_mode: audit
canonical: false
date: 2026-09-15
---

# Selection-totality Wave D2 prelanding independent review

## 0. Purpose

Wave D1 has independently passed. D2 is the bounded formal-consumer truth-up. This review asks whether the four reverse-mapped formal consumers still carry current proof load that can recreate pre-Selection-totality direction, curvature, d/T_dir or L0-residual assumptions.

This is not a theory-discovery pass and not a broad cleanup pass. The D2 question is narrower:

> Can a current equation / formal consumer still close an inference that Waves A-C and D1 now require to remain typed, declared, model-scoped or OPEN?

## 1. Closed upstream constraints

D2 inherits and may not reopen:

```text
Selection is reality's one generative mode;
primitive Selection is normatively contentless;
primitive Selection != formed G-hat;
L0 is not a pregiven completed possibility warehouse;
Selection occurrence != persistence != retained history;
kappa_0 / epsilon_pg / B-count do not inherit primitive status from O0;
primitive Selection supplies no universal direction signal;
T_dir requires an independently typed, declared direction signal;
no declared direction -> T_dir undefined / not admitted for that claim;
declared but inaccessible direction -> T_dir may be 0;
T_dir > 0 does not establish good / legitimacy / generative health / O2-M;
universal d -> T_dir necessity is not established;
script / habit / L2 automation does not by itself imply no Selection;
O2-M remains OPEN.
```

D1 authority surfaces now route those constraints and may not be overridden by a formula merely because that formula remains in a canonical equation/formalism file.

## 2. Scope verdict

The reverse-map four-file D2 scope is confirmed. No expansion is required before landing.

```text
_SRT_D_VALUE_CANONICAL.md
Core/SRT_Core_22_Equations.md
Core_Law/SRT_L1_Formalism.md
Core_Law/SRT_L1_Hardening_Notes.md
```

D3 theory owners remain HOLD.

## 3. File-by-file findings

### 3.1 `_SRT_D_VALUE_CANONICAL.md` — bounded semantic repair

Main `Def-d-canonical` is **not reopened** in D2.

The blocking backflow is local and appears in two connected passages.

Current §8 still states:

```text
d = 0 -> T_dir = 0

d is a necessary condition for T_dir
```

and describes T_dir as readability of the system's own selection-order direction. This conflicts with the post-Wave-C owner, where T_dir admission depends first on an independently typed declared direction and no universal d/T_dir necessity has been established.

A later `选择时刻与 d-value` passage also says:

```text
real selection moment -> d is activated
L2 replaces selection moment -> d is not activated / shrinks
```

and then uses a readable `proto-gradient` as the direction that T_dir reveals. This can independently recreate two superseded claims:

1. L2 automation cancels Selection occurrence;
2. T_dir reads a direction already present prior to declared downstream typing.

Required disposition: **REPAIR LOCALLY**, preserving d as a stake-coupled concern / risk-sensitivity object while replacing universal d/T_dir necessity and no-Selection-under-L2 language with model/bearer/agency-scoped relations.

### 3.2 `Core/SRT_Core_22_Equations.md` — conditionalize Eq-DValue-Max-1

The file already contains many proxy / bridge guards. The blocking backflow is concentrated in `Eq-DValue-Max-1`.

Current equation:

```text
d_max(theta) = min(rank_eff(I_F(theta)), Psi_f^budget / kappa_0)
```

can remain useful as a declared candidate model, but its present prose says:

```text
kappa_0 = primitive L0 curvature;
kappa_0 is the unit cost of aligning one L0 direction;
kappa_0 determines which directions are worth aligning to;
d determines how many such directions can be stably aligned.
```

This directly recreates a primitive direction field and gives kappa_0 direction/value authority that D1 removed.

Required disposition: **KEEP ID / KEEP FORMULA AS A CONDITIONAL MODEL**, but retype kappa_0 in this equation as an explicitly declared model/realization cost parameter under the current symbol-table row. The equation is not admitted when no such kappa_0 model is independently specified. Remove all `worth aligning` / primitive direction-field semantics. The implication must be about a model-local d-capacity bound, not a universal consciousness-depth ceiling.

No other Core22 equation is authorized for semantic redesign in D2 without independent evidence.

### 3.3 `Core_Law/SRT_L1_Formalism.md` — conditionalize the T_dir branch

The file currently says it does not define T_dir, but its equations still effectively supply T_dir from d/d_c/sigma.

Blocking passages:

```text
T_dir^alg := Theta((d-d_c)/(d_narrow-d_c)) * (1 - |sigma-sigma_sub^dagger|)
```

followed by an ODE that relaxes T_dir toward that target. The current prose says the target is simultaneously sensitive to the d channel and sigma health and treats the four-variable system as a route from P1-candidate toward P1.

This creates a hidden source problem: d/d_c/sigma can modulate access/readability, but they cannot create or validate the direction to which T_dir is relative.

Required disposition:

- four-variable T_dir closure is **conditional on prior direction admission**;
- without a declared direction, the T_dir branch is undefined/not admitted, not automatically zero;
- `T_dir^alg` is only a model-local accessibility/readability target relative to that already-declared direction;
- d/d_c/sigma may modulate access or reorientation capacity, not supply direction truth;
- the T_dir ODE remains a candidate dynamics for access/readability, not a primitive or universal direction law;
- the `lethal L2` equation becomes a conditional pathology model under an admitted direction and does not validate that direction;
- the total four-variable closure is conditional; the non-T_dir subsystem can be analyzed without forcing a T_dir coordinate.

A second bounded issue appears in:

```text
nu_block := eta * epsilon_pg * kappa_Psi_f
```

The file already calls this conditional, but still assigns blanket P1-candidate hardness from the three positive premises. Post-D1, epsilon_pg is not available by primitive inheritance. The equation may remain as a model-local conditional candidate only when an epsilon_pg realization has itself been independently admitted. Its claim strength cannot be upgraded merely by algebraic positivity.

### 3.4 `Core_Law/SRT_L1_Hardening_Notes.md` — conditionalize residual decomposition

The current `Delta_avail` hardening defines:

```text
Delta
= w_dir ||R||_{T_dir}
+ w_pay ||R||_{Psi_f}
+ w_L0 ||R||_{L0}
```

and promotes the three-way decomposition to `T-DELTA-1` as a P1-candidate formal theorem under A1-A3.

Two components still carry pre-reconstruction burden:

1. `||R||_{T_dir}` is treated as a universal direction-readability subspace, despite T_dir now requiring an independently typed declared direction;
2. `||R||_{L0}` is described as pressure from candidate states that remain in L0 and were not realized in L1, which can recreate an L0 warehouse / latent-pressure ontology.

Required disposition:

- T_dir projection exists only in a declared-direction model;
- L0 residual term is a **declared representation/model residual over unrealized accessible candidates**, not pressure emitted by primitive L0;
- A2 orthogonality and all projection operators are model assumptions, not universal ontology;
- full three-term `T-DELTA-1` is a conditional formal/model theorem (or P2 formal candidate) unless independent owners establish universal projection structure;
- sign/direction claims are relative to declared model geometry and direction criteria, not primitive direction;
- do not delete the residual-operator research programme.

## 4. Explicit non-findings / non-targets

D2 does **not** justify reopening:

- `Def-d-canonical` itself;
- the whole d proxy hierarchy;
- the whole Core22 master equation set;
- sigma self-reference reconstruction;
- suffering theory;
- occlusion theory;
- individuation theory;
- irreversibility theory;
- O2-M;
- Wave A-C owners;
- D1 authority surfaces except deterministic derivatives.

Those require their own evidence / later D3 or secondary synchronization gates.

## 5. Main divergence

At the last verified checkpoint, live main is `a149e4ed930c652dbb03311804c1398828d54681`; the PR branch is behind main by five commits from the #977 material-archive merge. Verified main-only files are Materials / Material_Log surfaces and do not overlap D2.

This is non-blocking for prelanding review. Before D2 semantic landing, recheck live main and overlap; if main has since changed a D2 owner or a D2 dependency, stop and re-audit before editing.

## 6. D2 repair principles

Prefer:

```text
universal / primitive source
-> independently admitted direction or model condition

canonical general equation
-> same equation ID retained as declared conditional model

T_dir source claim
-> T_dir access/readability modulation after direction admission

L0 residual pressure
-> model-declared unrealized-candidate / open-state residual
```

Do not solve a formal backflow by inventing a new primitive symbol.

## 7. Prelanding verdict

```text
D2 SCOPE REVIEW: PASS
D2 PRIMARY FILES: 4
D2 BLOCKING BACKFLOWS: CONFIRMED
NEW THEORY BLOCKER: NONE
MAIN d DEFINITION REOPEN: NO
D3: HOLD
BROAD CLEANUP: NO
D2 EXACT PATCH SPEC: REQUIRED BEFORE LANDING
MERGE #976: NO
```
