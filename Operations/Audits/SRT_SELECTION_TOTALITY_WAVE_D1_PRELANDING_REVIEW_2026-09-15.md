---
id: SRT-SELECTION-TOTALITY-WAVE-D1-PRELANDING-REVIEW-20260915
type: audit
status: active
layer: operations
epistemic_layer: os
claim_mode: audit
canonical: false
date: 2026-09-15
---

# Wave D1 independent prelanding review — authority truth-up

## Scope reviewed

Exactly three current authority surfaces from the Wave D reverse-map:

1. `_SRT_SYMBOL_TABLE.md`
2. `_SRT_CROSS_DOMAIN_MATRIX.md`
3. `CANONICAL_REGISTRY.md`

No D2/D3 semantic consumer is authorized by this review.

## Why D1 is necessary before formal repair

Wave A-C changed the ontology and direction/value boundaries, but these three control-plane files can still route readers and agents back to the old semantics.

The risk is not lexical age. It is authority inversion:

```text
updated owner
<- stale symbol registry / usage matrix / canonical router
<- downstream file cites control plane
<- removed strong burden re-enters as current canonical meaning
```

D1 therefore repairs authority routing before repairing formulas.

## File verdicts

### 1. `_SRT_SYMBOL_TABLE.md` — REPAIR REQUIRED

Current contradictions include:

- `L0` still registered as a set of all unselected possibilities;
- `G-hat` still described as the formal carrier of primitive Selection / actualisation itself rather than a formed/model-level carrier;
- `T_dir` still reads a system's own current selection direction without the declared-direction admission condition;
- `kappa_0` still carries structural-prerequisite + universal directionality-bias language;
- `epsilon_pg` still carries `B>=2` over `B<=1` as an L0 postulate;
- Usage Rule 15 retains the old structural-prerequisite reading;
- Usage Rule 17 says `epsilon_pg` remains the registered L0 minimum-non-neutrality postulate;
- Usage Rule 16 already points type authority to Claim Ladder §0A, so the file is internally inconsistent.

Required repair type: **namespace / definition-registry truth-up**, not symbol deletion.

### 2. `_SRT_CROSS_DOMAIN_MATRIX.md` — REPAIR REQUIRED

Current contradictions include:

- d-value explanatory guard says minimum non-neutrality unfolds into concern/stake;
- T_dir core use remains readability of current selection direction;
- political/philosophy row allows visibility of value / legitimacy direction;
- epsilon core row still calls `epsilon_pg` an L0 minimum-non-neutrality postulate.

Required repair type: **governance-usage truth-up**. The matrix must route to updated owners; it must not define new metaphysics.

### 3. `CANONICAL_REGISTRY.md` — REPAIR REQUIRED

Current contradictions include:

- the Generative Ontology Spine summary is pre-Selection-totality;
- the T_dir entry still summarizes 'selection order direction' readability;
- it explicitly says `d` is necessary for `T_dir`;
- it can route old cross-domain usage as canonical priority even after Wave C.

Required repair type: **routing / priority truth-up**. The registry must summarize current owners without becoming a second definition source.

## D1 non-goals

D1 must not:

- change `kappa_0` / `epsilon_pg` equations in Core22;
- repair `T_dir^{alg}` in L1 Formalism;
- rewrite d-value's canonical definition;
- adjudicate kappa/epsilon necessity or inheritance;
- close O2-M;
- change subjecthood, consciousness, One, Bearer, Agency or Stable ISP gates;
- rewrite public/legacy/companion files.

Those are D2/D3 or secondary-sync questions.

## D1 invariants

```text
symbol registration != primitive admission;
contentless != primitive;
model notation != metaphysical owner;
L0 != pregiven possibility warehouse;
primitive Selection != formed G-hat;
kappa_0 / epsilon_pg / B-count = no primitive inheritance by registration;
T_dir requires independently typed declared direction;
T_dir > 0 != good / legitimacy / generative health / O2-M;
d/T_dir universal necessity = not established;
registry/matrix summarize owners; they do not overrule owners.
```

## Verdict

```text
D1 SCOPE: PASS
FILES: 3
SCOPE EXPANSION: NO
READY FOR EXACT PATCH SPEC: YES
READY FOR D1 LANDING AFTER SPEC: YES
D2/D3: HOLD
MERGE #976: NO
```
