---
id: SRT-OPS-AUDIT-POST-PHASE0-5-OPEN-TENSIONS-SELECTION-FRUITFULNESS-SYNC-20260819
type: audit_record
status: active
record_stage: executed_post_phase0_5_ledger_supersession
layer: meta
epistemic_layer: os
claim_mode: governance
claim_level: P3/P4_governance
canonical: false
date: 2026-08-19
dependency:
  - SRT-OPEN-TENSIONS
  - SRT-GOV-SUB01-SUBTRACTIVE-AUDIT
  - SRT-OPS-AUDIT-PHASE0-5-GOV-SUB01-RELATION-PASS3-J3D-20260818
  - SRT-OPS-AUDIT-PHASE0-5-J3D-DISPOSITION-20260818
  - SRT-OPS-AUDIT-POST-RCA-SELECTION-D2-RANKING-20260817
tags: [Phase0_5, GOV_SUB01, OpenTensions, Selection, Fruitfulness, DerivationalIndispensability, LedgerSync]
---

# Post-Phase 0.5 · Open Tensions §13 Selection-Fruitfulness Sync

> **Scope**: governance-only supersession record for the Selection-primitive fruitfulness / derivational-indispensability row in `Core/SRT_OPEN_TENSIONS.md`. No canonical definition, theorem, symbol, equation, Selection criterion, or P0/P1 claim is changed here.
>
> **Reason for a separate record**: the current connector does not expose a safe line-level patch operation for the large `Core/SRT_OPEN_TENSIONS.md` file. This record therefore supplies the authoritative post-#836 status for §13 until that ledger is next edited through a safe full-file workflow.

## 0. Verdict

The existing §13 ledger wording is stale in one material respect.

It currently says, in substance:

```text
no post-PD-A full GOV-SUB01 re-adjudication has reassigned residue
```

That was true before Phase 0.5 PR #836. It is no longer true after the merged post-RC-A relation-level GOV-SUB01 package.

The current post-#836 status is:

```text
primitive role currently named Selection
= P unresolved primitive admission

generic non-neutrality / asymmetry relation
= R4 compensated / unresolved

specific epsilon_pg
= no new promotion
= not a constitutive Selection gate

Selection-level downstream D2 differential
= not yet established
```

## 1. What #836 actually changed

Phase 0.5 did not prove Selection fundamental or irreducible.

It did three narrower things relevant to §13:

1. separated one-shot Selection occurrence from persistence, Stable ISP, and agency-level relations;
2. ran a broad-`K` rival-vocabulary stress against the primitive role currently named `Selection`;
3. reassigned the current GOV-SUB01 residue for that primitive role to:

```text
P unresolved primitive admission
```

The `P` label means only that the role is presently not derived or cleanly removed under the audited target lock and rival families, while remaining open to future subtraction. It is not an empirical proof or a claim of necessity simpliciter.

## 2. Historical Pass 1 / 1.5 status

The earlier Pass 1 / 1.5 records may retain their historical `UNASSIGNED` residue as provenance.

Do not infer:

```text
historical UNASSIGNED
-> current residue remains UNASSIGNED
```

The current post-PD-A / post-RC-A / post-Phase-0.5 governance reading is controlled by the later GOV-SUB01 re-adjudication:

```text
Selection primitive role -> P unresolved primitive admission
```

No retroactive rewrite of historical audit records is required.

## 3. What remains open

The main §13 pressure is now sharper, not closed.

Still not established:

- a non-circular middle-level consequence derived specifically from the Selection primitive role;
- a consequence that survives an independently specified rival rewrite without hidden reparameterization;
- a prospective bounded-rival disagreement `O_SRT != O_R2`;
- downstream D2 or D3 evidence;
- a proof that the word `Selection` is uniquely required rather than the current best primitive-role name.

Therefore the next legitimate question is no longer:

> has GOV-SUB01 assigned a residue?

It is:

> does retaining the Selection primitive role yield any **derivational surplus** over the strongest target-preserving rival vocabulary?

## 4. Exact ledger replacement for the next safe `SRT_OPEN_TENSIONS.md` edit

When `Core/SRT_OPEN_TENSIONS.md` is next safely edited, the §13 row should be synchronized approximately as follows without changing its open-tension status:

### Already stabilized

```text
PD-A separates P0 primitive admission from downstream D2; post-RC-A Phase 0.5 GOV-SUB01 re-adjudication assigns the primitive role currently named Selection the residue P unresolved primitive admission, while generic non-neutrality remains R4 and epsilon_pg is not promoted into a Selection gate.
```

### Still not derived

```text
no non-circular middle-level consequence has yet shown derivational surplus over an independently specified target-preserving rival and then yielded a prospective bounded-rival D2 disagreement downstream.
```

### Must not be overstated

```text
do not treat P as proof of fundamentality or necessity; do not treat D2 null as automatic P0 falsification; do not use downstream stake, agency, phenomenality, or old Real Choice criteria to repair the primitive Selection burden.
```

## 5. Routing consequence

The next work item should be a frozen **Selection Primitive Fruitfulness / Derivational Surplus Duel**, not another relation inventory and not a new empirical P24 execution.

The duel should compare:

```text
SRT-P:
primitive role = Selection

versus

V2:
pre-objective asymmetric / effective actualisation
without using Selection as a primitive label
```

under the same locked P0 targets.

The question is whether any middle-level consequence `C*` is derivable from SRT-P but unavailable to V2 without silently re-importing the same primitive role.

## 6. Hard guards

This sync record does not authorize:

- Selection Grammar;
- a primitive relation alphabet;
- a new symbol, scalar, equation, or operator;
- a new P0/P1 claim;
- promotion of `epsilon_pg`;
- revival of former P1-T05 / Real Choice Moment;
- empirical execution of P24-1 / P24-3 / P24-4 as SRT-superiority tests;
- reinterpretation of `P` as `N1`, `N2`, truth, fundamentality, or necessity simpliciter.

## 7. Stop rule

This record is complete when it has:

1. identified the stale §13 statement;
2. recorded the post-#836 residue accurately;
3. preserved historical `UNASSIGNED` records as provenance;
4. redirected the remaining burden to derivational surplus rather than residue assignment;
5. avoided canonical theory edits.

**Final disposition:** `OPEN-TENSIONS §13 STATUS SUPERSEDED / CURRENT SELECTION ROLE = P / DERIVATIONAL SURPLUS = OPEN / NO CANONICAL EDIT`.
