---
id: SRT-CONSTITUTION-PHASE6-REFERENCE-ONTOLOGY-SECTION-AUDIT-20260902
type: audit
status: active
version: v1
date: 2026-09-02
layer: meta
epistemic_layer: governance
claim_mode: evidence
canonical: false
ai_do_not_use_for_definition: true
dependency:
  - Operations/Audits/SRT_CONSTITUTION_PHASE6_CORE_ROLE_RECLASSIFICATION_PASS1_2026-09-02.md
  - Core_Law/SRT_Reference_Ontology.md
  - Governance/SRT_CLAIM_LADDER.md
  - Core/SRT_Core_21_Minimal_Axioms.md
  - Core/SRT_Core_21b_Constitutive_Theorems.md
  - Core/SRT_Core_21c_Bridge_Hypotheses.md
  - Core_Law/SRT_Individuation.md
  - 03_Bridges/SRT_Agency_Automation_Guard_2026-08-17.md
  - _SRT_D_VALUE_CANONICAL.md
  - _SRT_PSI_F_CANONICAL.md
tags: [Constitution, Phase6, ReferenceOntology, SectionAudit, EpistemicRole]
---

# Phase 6 — `SRT_Reference_Ontology.md` section-by-section role audit

> **性质**：只读审计。本文不改变 `SRT_Reference_Ontology.md` 的当前 canonical status，不改任何 definition / theorem / equation / symbol，也不替代 current owner。
>
> **目标**：把一个旧 `Reference Ontology` surface 中共居的 ontology-language、formal encoding、domain realization、operational proxy、cross-domain metaphor 与 historical high-commitment prose 拆回各自知识角色。
>
> **核心纪律**：`Reference Ontology` 文件名和 `canonical` frontmatter 不意味着其中每一段都具有同等 theory-canonical 或 ontology-level authority。

---

## 1. 文件级诊断

该文件自述已经承认：

```text
§1 = L0/L1/L2 formal realization
§2 gauge field
§3 topology
§4 information theory
```

其中 §2–§4 是**领域映射，不是 L0 命题本身**。

但当前文件仍把这些领域映射，以及后续 neuroscience / phenomenology / individuation / social consensus / active inference 等段落，放在同一个：

```text
type: definition
claim_mode: canonical
Reference Ontology
```

surface 中。

Phase 6 因而不是否定整份文件，而是恢复：

```text
canonical citation surface
!=
uniform epistemic role of every section
```

---

## 2. §1.1 `L0`：必须首先拆 `L0_abs` 与模型化 `L0_rel`

### O1 — `L0` as high-free-energy state set

当前式：

```text
L0 = {sigma in S : F[sigma] > F[sigma_L1]}
```

该对象依赖：

- 状态空间 `S`；
- 自由能 `F`；
- 一个已给定 `L1` reference state。

按 Gate 0，这属于典型 reference-/regime-dependent Class-B style object；它不能靠全局记号变成 bare `L0_abs` 的 primitive content。

**Proposed role**：

```text
primary: domain formalization
secondary: historical formalization
legacy/superseded expression: yes, if read as universal definition of bare L0
```

**Disposition**：只允许作为具名模型中的 relative-potential realization；不得继续按裸 `L0 ≡ high-free-energy set` 使用。

---

### O1a — `L0_abs` as inexhaustible potential

当前 strong prose：不可完全形式化、先于任一有限 formal system；同时后期 PC-A 已明确：non-exhaustion 不等于 content/cardinality/information conservation。

**Proposed role**：

```text
primary: conceptual bridge
secondary: constitutional source/commentary
```

**Reason**：它更接近 SRT 的 non-preformation / non-exhaustion philosophical burden，而不是一个可计算 ontology definition。

**Boundary**：不得由 `L0_abs` 不可穷尽推出具体 mathematical container、概率分布、熵、全局菜单或保存全部完成形式。

---

### O1a realization table — Hilbert / moduli / Ruliad / spirituality

该表本身已经写成“投影实现”。

**Proposed role**：

```text
primary: conceptual bridge
secondary: domain formalization / cross-domain metaphor
```

**Disposition**：不得反向证明这些对象彼此同一，也不得由 spirituality vocabulary 获得 ontology authority。

---

### O1b — `L0_rel(t+1)=f(L1(t),G_theta)`

**Proposed role**：

```text
primary: conceptual bridge
secondary: domain formalization
```

它与 Constitution `(4)` “历史结构如何改变后来什么现实可处理”有自然对应，但它是一个 stronger formal representation，不是 Constitution definition。

---

## 3. §1.2 `L1`：primitive role 与具体 cognitive/topological models 共居

### O2 — `L1(t)=G_theta[sigma(t)]`

AM-A 已明确此式只记录 formal input-output role，不解释 primitive actualisation。

**Proposed role**：

```text
primary: historical formalization
secondary: conceptual bridge
```

当前 formal-core owner 优先回 `Core21A P0-01`；Reference O2 作为 compatibility surface。

---

### O2a — hysteresis model + phenomenology table

包括：

```text
L1(t)=G[L0(t)] + eta L1(t-dt)
eta≈0 -> fragmented reality
eta≈0.5 -> normal coherence
eta≈1 -> cognitive fixation
```

**Proposed role**：

```text
primary: domain formalization
secondary: operational proxy
```

**Reason**：具体数值区间和 phenomenology mapping 明显需要 neuroscience/cognitive evidence，不是 general ontology definition。

---

### O2b — experienced/external beta mixture

**Proposed role**：

```text
primary: operational proxy
domain: cognition / neuroscience
```

`beta` 是具体 modeling coefficient，不是 Constitution 或 bare ontology 对象。

---

### O2c/O2d — simplicial complex / topological holes

O2c 可作为 applied-topology representation；O2d 进一步把 homology holes 读成 `L0_rel` counterfactual volume。

**Proposed role**：

```text
O2c: domain formalization
O2d: conceptual bridge + domain hypothesis
```

**Boundary**：topological cavity 不能反向定义 latent potential；成功 fit 也只说明该 representation 在某域有用。

---

## 4. §1.3 `L2`：semantic role、fixed-point form 与 proxy 必须分开

### O3 — stable fixed point

```text
L2 = {sigma : G_theta[sigma]=sigma and stable}
```

当前 21B P1-T03 只保留 `L2 downward constraint` 的 constitutive role，并把 domain mechanism 留给 bridge/lab。

**Proposed role**：

```text
primary: historical formalization
secondary: conceptual bridge
```

fixed-point form 是一个模型化表达，不是所有 historical sedimentation / institution / habit / law 的统一机制证明。

---

### O3d — `L2_theta` relativity guard

**Proposed role**：

```text
primary: conceptual bridge
```

这是重要 boundary：human science is not `L0_abs`。保留价值高，但它不要求所有 objectivity 都被解释成同一种 fixed point。

---

### O3a/O3b/O3c — hysteresis / automorphism hardness / plasticity threshold

```text
L2 accumulation equation
Hardness(L2) proportional to |Aut(L2)|
P_L2 = d_current E_available / (Hysteresis C_r)
```

**Proposed role**：

```text
primary: operational proxy / domain formalization
secondary: historical formalization
```

这些量需要 domain-specific state space、group interpretation、energy measure、hysteresis measure 和 calibration；不能因 `L2` ontology section 获得 universal status。

---

## 5. §1.4 `kappa`：当前最典型的 ontology-surface / P3-P4 mismatch

该节把 `kappa` 定义成 stabilization degree，并以 spectral gap / fixed point / phase transition 组织：

- `kappa_c1` = L1 birth / consciousness awakening / quantum collapse / paradigm establishment；
- `kappa_c2` = L2 crystallization；
- O-T3 进一步给 plasticity barrier model；
- FC-kappa-1/2 给 OCD/belief updating/meditation empirical predictions。

当前 dependency audit 已明确：`kappa_c1` consciousness-threshold existence itself is P3/P4 style and threshold could be posited directly；不是 bare ontology theorem。

**Proposed role**：

```text
primary: domain formalization
secondary: operational proxy / conceptual bridge
legacy/superseded expression: yes, for universal ontology/consciousness/quantum-equivalence reading
```

**Disposition**：

- stabilization-index idea may remain a model family；
- consciousness threshold / quantum collapse / paradigm shift cannot share one ontology truth-status；
- EEG/symptom predictions are Lab/domain hypotheses。

**Future gate**：whether `kappa` remains a productive cross-domain proxy family or should be split by domain is a later domain-adequacy question, not a Constitution decision。

---

## 6. §2 Gauge Field：文件内部已经存在直接 owner inversion

### O4 — `L0_true = A/G`

本定义写：

```text
L0_true = moduli space A/G
```

但同一文件 §1.1 已经后期明确：

```text
L0_abs != Ruliad / moduli space / Hilbert space
all are relative projections
```

因此这里不是需要新哲学判断，而是一个**文件内部已被后文 supersede 的 historical expression**。

**Proposed role**：

```text
primary: legacy/superseded expression
secondary: domain formalization (gauge-theory realization only)
```

**Disposition**：不得继续以 `L0_true` wording 承重。

---

### O5 — `L0` as differential manifold

**Proposed role**：

```text
primary: domain formalization
secondary: historical formalization
legacy/superseded expression: yes, if read as bare-L0 identity
```

Gate 0 允许具名 formal realization，但不允许一个 differential manifold silently become bare `L0_abs`。

---

### O-T1 path-integral realization

本文件自己已经 harden 成 conditional realization，明确不定义 primitive actualisation。

**Proposed role**：

```text
primary: domain formalization
```

这是 Phase 6 中“已自我完成角色降级”的正例。

---

## 7. §3 Topology：physics/formal bridge，不是 ontology definition

### O6 — Matter = Knot(L0)

### O7 / O-T2 — L2 as braid representation / unknotting principle

**Proposed role**：

```text
primary: domain formalization
secondary: conceptual bridge
legacy/superseded expression: yes, for universal identity claims
```

**Reason**：`matter = topological knot`、`L2 = braid representation` 都需要明确 physics/mathematical realization and evidence；不能从 shared metaphor 直接取得 identity。

---

## 8. §4 Information Theory：几乎全部应从“Ontology”降回 proxy/model

### O8 — `i_diff=-log p_max`

其上游 A6 information-existence equivalence 已被 21C 归入 P2/P3。

**Role**：`operational proxy / domain formalization`。

### O9 — epistemic bandwidth `B_e`

以及 table 中：external object ≈1 / emotion 0.3–0.7 / pure qualia ->0。

**Role**：`operational proxy`。

**Pressure**：numeric ranges require empirical calibration；不是 ontology definition。

### O10 — `dim(L1)<<dim(L0)`

**Role**：`conceptual bridge + domain formalization`。

**Boundary**：若 `L0` 指 `L0_abs`，dimension itself may be undefined；因此 unrestricted equation is a historical/generalization overreach。只有对 declared `L0_rel` representation 可建模。

---

## 9. §5 Modal Access：A13 是边界，O11 是模型

### A13 non-exhaustion

**Role**：`conceptual bridge`；与 current Gate 0/PC-A aligned。

### O11 accessibility by integrated Psi_f + O-C1 `R_accessible ∝ d^1.5`

**Role**：

```text
primary: domain formalization
secondary: operational proxy
```

**Reason**：需要位置、reachable domain、payability horizon、energy budget、metric and exponent calibration。Gate 0 本身已经要求这类 situated accessibility quantity 声明 reference structure。

**Boundary**：`d^1.5` 是具体 quantitative hypothesis，不是 modal ontology definition。

---

## 10. §6 Cross-Domain Metaphor Table

文件本身已经叫 `Metaphor Table`。

**Proposed role**：

```text
primary: conceptual bridge
secondary: constitutional source/commentary
```

**Use**：orientation / translation only。不得由 physics / cognition / society / spirituality 的 vocabulary proximity 得出 ontology identity。

---

## 11. §7 Qualia Residual：phenomenology bridge，不是 general ontology theorem

### O12 Qualia Residual

**Proposed role**：

```text
primary: conceptual bridge
domain: phenomenology / consciousness
```

该区分可表达“description does not replace experience”的 philosophical burden。

### O-T4 Gödel-like incompleteness

**Proposed role**：

```text
primary: legacy/superseded expression candidate as theorem
secondary: conceptual bridge
```

**Reason**：从 finite formal description 到 qualia non-equivalence 的 Gödel-like analogy 并未因公式形式而获得数学定理证明；应避免 theorem label over-hardening。

**Boundary**：保留“模型不等于体验”的问题，不等于已经证明一种原则性不可形式化定理。

---

## 12. §8 Operator Individuation：developmental/domain model，不能定义 universal bearer admission

### O13 / O-T5 shared-to-individual ledger split

以母胎共享账本、呼吸/代谢/内稳态闭环、`kappa_split` 和 energy-ratio threshold 定义 individuation。

**Proposed role**：

```text
primary: domain formalization
secondary: operational proxy / conceptual bridge
domain: developmental biology / individuation
```

**Reason**：这是一个具体 realization model。Constitution `(2)/(3)/(5)` 已明确禁止从一个 analysis cut / historical relation / consequence cut 直接完成 bearer admission。

**Current stronger owner context**：`SRT_Individuation.md` 本身是 hybrid L1 theory，specific thresholds P3/P4；因此 O13/O-T5 不应从 Reference Ontology 跳到 universal theorem。

**Future action**：route into future individuation deep-well audit；不在 Phase 6 直接删改。

---

## 13. §9 现有内容分成四类，而不是一个 ontology layer

### 13.1 已明确撤回的 O14 / Hyp-O8 / O15

- old global `Omega` operator；
- `L0_abs = global-operator limit`；
- Truth–Goodness–Beauty optimal manifolds。

文件自身已经标 `撤回 / 停驻 / C-A`。

**Role**：`legacy/superseded expression`。

**Action**：只需未来 registry/navigation 明确，理论无需重新裁。

---

### 13.2 O-T6 / O-T7 — manifest contingency / L1 non-self-sufficiency

这些是 stronger philosophical/formal claims：finite manifestation not self-necessary，maintenance requires payment，iterative donation of existence。

**Proposed role**：

```text
primary: conceptual bridge
secondary: historical formalization
```

**Boundary**：其 `Psi_f >0 for persistence` 只能处理 persistence/maintenance，不反向定义 primitive actualisation（EX-A 已明确）。

---

### 13.3 O-T8 / O16 / O-T9 / O17

- embodied-bliss asymptote；
- non-redundant persistence by `Psi_f_maint>0`；
- scale-orthogonal coexistence；
- anti-semantic-evasion / micro-macro linking。

**Proposed role**：

```text
primary: conceptual bridge
secondary: domain formalization
```

**Specific boundaries**：

- bliss asymptote is not an ethical endpoint；
- persistence criterion does not define first actuality；
- scale-orthogonal coexistence is a cross-scale bridge, not a theorem from notation alone；
- macro/micro link requires named coarse-graining and evidence。

---

### 13.4 O18/O-T10/O19/O20/O21/O-T11/O-T12/O-T13

这一组包含：

- pattern compressibility；
- `Psi_f ∝ K_theta`；
- underdetermination / DOF_onto；
- creativity formula；
- friction-minimizing grouping；
- consensus friction；
- L2 convergence under shared constraint；
- hierarchical persistence attractor candidate。

**Proposed primary role**：`domain formalization`。

**Secondary**：`operational proxy / conceptual bridge`。

**Domains**：cognition, representation, social coordination, multiscale persistence。

**Disposition**：这一组最适合未来按 domain bundle 拆出 Reference Ontology；当前不应被引用成 bare ontology definitions/theorems。

---

## 14. §10 Active-Inference rewrite：conceptual bridge + agency overreach must separate

### O22 External-State Construction Clause / O-T14 Projection-First Ontology

**Proposed role**：

```text
primary: conceptual bridge
secondary: historical formalization
```

它表达 selection-first / projection-first 的 stronger theory reading，但不等于 external reality is analyst-created；应受 Constitution 全局 anti-arbitrariness guard 约束。

### O23 Blanket Payability Condition

当前式：

```text
Agenthood iff B_MB and d>0 and Psi_f-payable
```

这是一个 universal necessary-and-sufficient agency definition。

但 active `SRT_Agency_Automation_Guard_2026-08-17.md` 已明确：

```text
positive agency criterion remains open
potential evidence includes several factors
none individually necessary-and-sufficient
no conjunction is promoted to theorem
```

因此存在当前清晰 owner pressure：

```text
O23 universal iff agency definition
vs
RC-A active agency guard: positive criterion remains open
```

**Proposed role**：

```text
primary: legacy/superseded expression candidate
secondary: domain formalization / conceptual bridge
```

**Disposition**：O23 不应继续以 Reference Ontology canonical surface 获得 positive agency-definition authority。Future author/action gate 可在 role-change phase 处理；本 audit 不静默改正文。

---

## 15. Section disposition table

| Section / item | Proposed primary role | Key disposition |
|---|---|---|
| O1 high-F `L0` | domain formalization | only relative/model L0; not bare L0 |
| O1a `L0_abs` | conceptual bridge | non-exhaustion/source commentary |
| realization table | conceptual bridge | translation, no identity |
| O1b `L0_rel` | conceptual bridge | stronger model of historical access |
| O2 | historical formalization | formal role, defer P0 owner to 21A |
| O2a/O2b | domain formalization / proxy | cognition/neuro model |
| O2c/O2d | domain formalization / bridge | topology realization/hypothesis |
| O3 | historical formalization | fixed-point model; L2 role to 21B |
| O3a/b/c | operational proxy | no universal hardness/plasticity formula |
| `kappa` section | domain formalization | consciousness/quantum mappings P3/P4 |
| O4 `L0_true=A/G` | legacy/superseded | directly superseded by §1.1/PC-A |
| O5 manifold `L0` | domain formalization | not bare-L0 identity |
| O6/O7 topology | domain formalization | physics/formal bridge |
| O8/O9/O10 info | operational/domain formalization | not ontology definitions |
| A13 | conceptual bridge | keep current non-exhaustion boundary |
| O11/O-C1 modal access | domain formalization/proxy | requires declared reference structure |
| §6 metaphor table | conceptual bridge | translation only |
| O12 qualia residual | conceptual bridge | phenomenology burden |
| O-T4 Gödel-like | legacy theorem candidate | analogy not proof |
| O13/O-T5 individuation | domain formalization | developmental realization, not universal bearer definition |
| O14/Hyp-O8/O15 | legacy/superseded | already withdrawn |
| O-T6/O-T7 | conceptual bridge | stronger philosophical theory |
| O-T8/O16/O-T9/O17 | conceptual bridge/domain formalization | not universal ontology theorem by label |
| O18–O21 / O-T10–13 | domain formalization/proxy | cognition/social/multiscale bundles |
| O22/O-T14 | conceptual bridge | selection-first stronger reading |
| O23 agenthood iff | legacy/superseded candidate | conflicts with current open positive-agency criterion |

---

## 16. Genuine future author/action gates exposed

这些 gate 只登记，不立即要求作者裁。

### Gate RO-1 — What remains of `Triadic Ontology` as theory, versus modeling vocabulary?

Need later distinguish at least:

```text
L0/L1/L2 as conceptual grammar
vs
specific set/fixed-point/free-energy equations
```

Constitution v1 does not decide the stronger formal ontology.

### Gate RO-2 — `kappa` cross-domain family

Is `kappa` intended to remain one cross-domain stabilization proxy family, or should consciousness / quantum / cognition / social uses be split into domain-local models?

Current threshold mappings do not have uniform P-level support.

### Gate RO-3 — Qualia residual strength

Keep only:

```text
description != experience
```

or retain a stronger theorem-like principled incompleteness claim?

The current `Gödel-like` formula requires independent argument if kept as theory claim.

### Gate RO-4 — O23 positive agency definition

Active agency guard says positive criterion remains open; O23 provides an iff definition. Later role-change phase must decide whether O23 is:

```text
historical expression
bounded domain hypothesis
or reopened positive-agency proposal requiring new adjudication
```

### Gate RO-5 — Domain bundle extraction

Should future architecture physically split cognition/topology/social/active-inference definitions out of `Reference Ontology`, or only reclassify them through registry/metadata while preserving historical file integrity?

This is an architecture/edit decision, not a theory decision.

---

## 17. Audit result

**PASS as a role-reclassification audit.**

The file has substantial retained value, but not as one uniform ontology owner. Its productive future role is better represented as:

```text
Reference Ontology
=
historical formalization hub
+ conceptual ontology grammar
+ multiple domain-model / proxy families
+ explicit legacy records
```

rather than:

```text
one canonical ontology layer
whose every definition/theorem shares the same authority
```

No Core/Core_Law file was edited in this pass.
