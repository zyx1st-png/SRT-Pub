---
id: SRT-SELECTION-TOTALITY-WAVE-D-REVERSE-MAP-20260915
type: audit
status: active
layer: operations
epistemic_layer: os
claim_mode: audit
canonical: false
date: 2026-09-15
---

# Selection-totality Wave D reverse-map audit

## 0. Purpose

Wave A-C are closed at their currently authorized strength. This audit asks a narrower question:

> Which current consumers can still carry stale pre-Selection-totality semantics with present proof load or routing authority?

This is **not** a grep-cleanup pass. Historical wording, archived material, public exposition, generated reading aids, and `canonical:false` companions are not primary targets merely because old vocabulary remains.

A file enters the primary Wave D repair scope only if its current role can still:

1. define or register a canonical object;
2. route current canonical interpretation;
3. supply P1/P1-candidate formal burden;
4. independently re-establish a primitive direction / value / trace / operator claim that Waves A-C removed.

## 1. Controlling architecture

The reverse-map is governed by the post-Wave-C state:

```text
Selection is reality's one generative mode;
primitive Selection is normatively contentless;
primitive Selection != formed G-hat;
occurrence != persistence != retained history;
history exists where prior Selection remains materially effective;
Selection occurrence does not guarantee durable trace;
terminal Selection remains genuine Selection;
kappa_0 / epsilon_pg / B-count do not inherit primitive status from O0;
primitive Selection supplies no universal direction signal;
T_dir requires an independently typed, declared downstream direction signal;
T_dir > 0 does not establish good / legitimacy / generative health / O2-M;
O2-M remains OPEN.
```

## 2. Primary backflow targets

### D-A — authority / registry truth-up

These files currently have repo-wide routing, symbol, or governance authority and still reproduce superseded semantics.

| File | Current authority | Backflow found | Disposition |
|---|---|---|---|
| `_SRT_SYMBOL_TABLE.md` | canonical symbol registry | `L0` remains a set of unselected possibilities; `kappa_0` is still a structural prerequisite and universal directionality bias; `epsilon_pg` still favors `B>=2` over `B<=1`; Rules 15/17 conflict with Rule 16 + current Gate 0 | **REPAIR** |
| `_SRT_CROSS_DOMAIN_MATRIX.md` | governance-canonical repo-wide usage matrix | `d` still unfolds primitive minimum non-neutrality; `T_dir` still reads current selection/value/legitimacy direction; `epsilon_pg` still registered as L0 minimum-non-neutrality postulate | **REPAIR** |
| `CANONICAL_REGISTRY.md` | canonical priority router | Spine summary remains pre-Selection-totality; T_dir summary still says readability of own selection-order direction; explicitly says `d` is necessary for `T_dir`; current usage matrix description inherits stale epsilon semantics | **REPAIR** |

### D-B — canonical / formal consumer truth-up

These files are current semantic or formal anchors whose formulas / definitions can independently recreate removed strong burdens.

| File | Current authority | Backflow found | Disposition |
|---|---|---|---|
| `_SRT_D_VALUE_CANONICAL.md` | canonical d-value owner | current cross-link says proto-gradient is readable, `T_dir` tells the system where the direction is, and `d>0` is necessary for `T_dir>0` | **BOUNDED REPAIR** — do not reopen d's main definition |
| `Core/SRT_Core_22_Equations.md` | canonical master-equation anchor | `Eq-DValue-Max-1` treats `kappa_0` as unit cost per L0 direction and says `kappa_0` determines which directions are worth aligning to; this recreates a primitive direction field | **BOUNDED REPAIR / CONDITIONALIZE** |
| `Core_Law/SRT_L1_Formalism.md` | canonical, P1-candidate formalism | `T_dir^{alg}` is generated directly from `d/d_c/sigma` with no independently typed declared direction signal; T_dir is assumed everywhere as an admitted `[0,1]` variable | **REPAIR / MODEL-SCOPE** |
| `Core_Law/SRT_L1_Hardening_Notes.md` | canonical P1-candidate/P2 hardening | `Delta` decomposition contains a universal `T_dir` subspace and `L0 residual pressure`; direction-readability is treated as structural without an independently typed direction source | **REPAIR / CONDITIONALIZE** |

### D-C — L1 theory backflow

These current L1 theory surfaces still use the removed primitive direction / universal trace / eternal-history burden as constitutive structure.

| File | Current authority | Backflow found | Disposition |
|---|---|---|---|
| `Core_Law/SRT_Individuation.md` | hybrid L1 subject-position / self-consciousness model | T-IND-2 makes `epsilon direction` local readability and `B>=2 vs B<=1` one of four subject-transition conditions; meaning of transition is written as bearing-care-direction jointly anchored | **REPAIR** |
| `Core_Law/SRT_Occlusion_Dynamics.md` | L1 canonical main theory | T-OCC-1 defines healthy/A/B regimes by whether `epsilon direction` / order-gap signal is readable or suppressed; inherits a universal direction source | **REPAIR** |
| `Core_Law/SRT_Irreversibility.md` | canonical P1-candidate | states every real learning leaves an undeletable trace, trauma is permanently sedimented, historical consequences cannot become zero, and termination is an absolute absorbing boundary; this exceeds the current occurrence/history boundary | **REPAIR / RETYPE TO RETAINED-EFFICACY CONDITIONS** |
| `Philosophy/SRT_L0_Ontological_Status.md` | self-described authoritative philosophy source | L0 reality is defined by `Psi_f>0 & kappa_0>0 & directional asymmetry`; `kappa_0` is the only accessible face of L0; `epsilon_pg` is a direction seed toward bare consciousness; electron-scale direction seeds are carried upward | **REPAIR / DEMOTE AUTHORITY OR RETYPE** |

## 3. Why these are real backflow, not mere old words

A residual becomes a Wave D target only where the file can still close an inference independently.

Examples:

```text
Symbol Table:
kappa_0 -> universal directionality bias

Cross-Domain Matrix:
epsilon_pg -> core minimum-non-neutrality postulate

D-value canonical:
proto-gradient readable -> T_dir direction -> d necessary

Core22:
kappa_0 direction field -> d alignment budget

L1 Formalism:
d/d_c/sigma -> T_dir^{alg}

Individuation:
epsilon direction readability -> subject transition gate

Occlusion:
epsilon direction readability -> healthy/pathological regime classification

Irreversibility:
Selection occurred -> undeletable trace / permanent sediment

L0 Ontological Status:
kappa_0/epsilon_pg -> L0 reality / consciousness-direction seed
```

Each of these can recreate a strong claim without passing through the updated Wave A-C owners. That is the criterion for inclusion.

## 4. Secondary / conditional dependents — do not edit in first landing by default

These files contain old language or depend on affected objects, but do not presently justify primary semantic-owner status.

| File / class | Status | Reason |
|---|---|---|
| `Core_Law/SRT_Suffering.md` | **conditional dependent** | consumes `T_dir` and L1 Formalism; direct owner statements defer T_dir definition upstream. Recheck after D-B before editing. |
| `Core/SRT_Closure_Compatibility_Hardening.md` and compatibility annexes | **secondary hardening debt** | active hardening language still says primitive asymmetry makes Selection possible, every real Selection leaves trace, and positive normative status is epsilon-direction constrained, but file explicitly does not replace canonical anchors. Recheck after primary landing. |
| `D_VALUE_ALIGNMENT.md` | **secondary sync debt** | stale kappa/epsilon geometry but `canonical:false`, `companion_exposition`. |
| `Core_Law/SRT_Core_Text_CN.md` | **legacy reader debt** | very stale primordial direction / epsilon material but `legacy_v1`, `canonical:false`, companion exposition. |
| `SRT_Quick_Start.md` | **public sync debt** | old possibility-space and T_dir wording; explicitly not a canonical definition source. |
| `_SRT_CONTEXT_ROUTER.md` | **navigation sync debt** | routes to current owners and declares no theory authority; update only after semantic landing if routing summaries materially mislead. |
| Operations / archived dependency maps / source materials / book archives | **historical provenance** | retain history; do not rewrite merely for lexical consistency. |

## 5. Explicit non-targets at this stage

- `_SRT_PSI_F_CANONICAL.md`: no direct T_dir back-definition found in this reverse-map; do not expand scope without independent evidence.
- Wave A-C owners already independently reviewed: do not reopen merely because secondary consumers are stale.
- Core01 legacy Part B: retrieval-guarded and previously adjudicated; no automatic reopening.
- generated context bundles / split shards: derivatives only; regenerate after semantic landing, never hand-edit as semantic sources.

## 6. Recommended Wave D landing structure

Do **not** land all 11 files as one unreviewed semantic blob. Use three bounded subwaves under one Wave D migration:

```text
D1 — authority truth-up
  _SRT_SYMBOL_TABLE.md
  _SRT_CROSS_DOMAIN_MATRIX.md
  CANONICAL_REGISTRY.md

D2 — formal consumer truth-up
  _SRT_D_VALUE_CANONICAL.md
  Core/SRT_Core_22_Equations.md
  Core_Law/SRT_L1_Formalism.md
  Core_Law/SRT_L1_Hardening_Notes.md

D3 — L1 theory truth-up
  Core_Law/SRT_Individuation.md
  Core_Law/SRT_Occlusion_Dynamics.md
  Core_Law/SRT_Irreversibility.md
  Philosophy/SRT_L0_Ontological_Status.md
```

Each subwave must receive an independent semantic review before the next subwave. Wave D closes only after D1-D3 plus deterministic derivatives and a final reverse-map check.

## 7. Repair invariants

Across all D1-D3 edits:

```text
no new primitive symbol;
no new primitive layer;
no restoration of B-count preference;
no restoration of kappa_0 / epsilon_pg primitive inheritance;
no implicit universal direction source;
no d/T_dir universal necessity without a declared model;
no T_dir without independently typed declared direction;
no occurrence -> durable trace theorem;
no eternal/permanent history without retained-efficacy conditions;
no subject / consciousness gate inherited from epsilon direction;
no O2-M closure.
```

Where an old formal object remains useful, prefer retyping:

```text
primitive/universal
-> declared model / realization / bridge / conditional candidate
```

rather than deleting the research programme.

## 8. Derivative obligations after landing

For any changed long-form owner with reading splits, regenerate shard bodies with the repository's official split generator before refreshing metadata. Do not perform metadata-only freshness updates.

Then regenerate official context bundles and run the normal Governance Preflight.

Expected derivative surfaces may include, depending on actual owner edits:

- `Core/Equations_Split/`
- `Core_Law/L1_Formalism_Split/`
- any registered split for changed L1 owners
- `Operations/Context_Bundles/`

Exact derivative scope must be discovered from the live working tree; do not invent or hand-edit generated hashes.

## 9. Current verdict

```text
WAVE D REVERSE-MAP: PASS
PRIMARY BACKFLOW TARGETS: 11
D1-D3 BOUNDED STRUCTURE: RECOMMENDED
BROAD CLEANUP: NO
CANONICAL LANDING: NOT YET STARTED
MERGE #976: NO
```

Next required step: independently review the 11-file scope and produce an exact Wave D patch specification before any canonical consumer edits.