---
id: SRT-SELECTION-TOTALITY-WAVE-D2-SECOND-FINAL-INDEPENDENT-REVIEW-20260915
type: audit
status: active
layer: operations
epistemic_layer: os
claim_mode: audit
canonical: false
date: 2026-09-15
---

# Wave D2 second final independent review

## Verdict

```text
D2 SECOND INDEPENDENT FINAL VERDICT: REVISE
D3: HOLD
MERGE #976: NO
```

The Core22-only corrective commit successfully repairs the two residuals identified by the first independent review:

- `Eq-Force-01` is now a declared-model reference-trajectory friction proxy and no longer defines `Psi_f` against a universal `L_0^{natural}`;
- `Eq-Friction-Comp` is now a P3/P4 quantum/computational bridge over declared model states and no longer infers metric geometry of primitive `L_0`.

Those repairs are accepted.

However, a fresh read of the current Core22 owner reveals a remaining active operator-first / warehouse-style backflow outside those two equation blocks. Because Core22 declares itself the canonical master-equations anchor and the official reading split reproduces the same summary text, these are current proof-load surfaces rather than harmless legacy wording.

## Blocking residuals

### R1. Active protocol definition still treats L0 as an operator input domain

`Def-Protocol-1` currently states:

```text
G-hat_theta : (L_0, Pi) -> L_1
```

and describes `Pi` as constraining the operator's selection space / transitions from `L_0` to `L_1`.

Under the Selection-totality migration, `L0/L1/L2` are analytic/model-facing aspects, and primitive Selection is not generated by a formed operator. A local formal operator may model a formed realization, but it may not re-establish primitive ontology as a map from a pregiven latent domain into actuality.

### R2. Methodological guard still reifies `L0 potentiality / Omega logical layer`

`Def-Protocol-3` still says empirical closure cannot deny `L_0` potentiality or an `Omega` logical layer. The safe current burden is only that empirical closure over declared observable/model variables does not settle primitive ontology. It must not restore a latent inventory or a separate primordial logical entity.

### R3. Formalization Summary calls Eq-Evo-01 a first-principles SRT dynamics equation

The current summary says reality evolves through the projection of `G-hat_theta` plus free-energy and attention terms and calls this SRT's `first-principles equation`.

This overstates a formed/model dynamical role-carrier into an ontology owner. The equation may remain as a model/realization dynamics candidate, but primitive Selection != `G-hat_theta` and the equation is not the first-principles definition of reality.

### R4. Mechanism Explanation restores the possibility warehouse explicitly

The current text says:

```text
G-hat_theta projects the possibility space of latent-domain L0
into selected reality L1.
```

This is a direct pre-reconstruction ontology backflow.

### R5. d-value explanation restores `L0 sampling`

The current mechanism summary says higher `d` means more open `L0` sampling and lower `d` corresponds to closed grammar with `Psi_f ~= 0`.

Canonical `d` is not a measure of access to a pregiven L0 inventory. Any relation among `d`, access/revision breadth, and friction must remain typed and model-scoped; low `d` does not universally imply zero friction.

## Accepted parts of the corrective pass

The following remain accepted and must not be reopened merely because this review returns REVISE:

- Eq-Force-01 corrective semantics;
- Eq-Friction-Comp corrective semantics;
- the other three D2 owners already passed by the first review;
- official split regeneration mechanics;
- context-bundle regeneration mechanics;
- Governance Preflight #2232 success.

## Minimal corrective scope

The next corrective pass remains strictly:

```text
Core/SRT_Core_22_Equations.md only
```

It should repair only the active operator-first / warehouse residuals in:

- Terminology / protocol framing where needed;
- `Def-Protocol-1`;
- `Def-Protocol-3`;
- `Formalization Summary`;
- `Mechanism Explanation`;
- directly duplicated retrieval surfaces.

Do not turn this into a whole-file rewrite and do not enter D3.

A separate exact patch spec controls execution.