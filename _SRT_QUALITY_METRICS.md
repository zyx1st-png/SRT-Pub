---
id: SRT-QUALITY-METRICS-AUTO
type: framework
tags: [Quality, Metrics, AutoGenerated]
status: rolling_v1
claim_mode: evidence
updated: 2026-07-20
dependency: [_SRT_QUALITY_SCORECARD]
---

# SRT Quality Metrics (Auto)

- Frontmatter Coverage: 285/308 (92.5%)
- Terminology Governance Coverage (core set): 4/4 (100.0%)
- Boundary Header Coverage (theory candidates): 107/170 (62.9%)

## Governance Load Indicators (2026-07-20 治理减负轮基线)

治理是否随活跃度成比例，看这四个数。目标是让治理负担可观测、不再单向增长。

计数口径唯一权威：**深度 1 的 `*.md` 文件数**（`find <dir> -maxdepth 1 -type f -name '*.md' | wc -l`）；「含子目录的顶层条目数」是另一指标，须显式命名，不与前者混用。

| 指标 | 减负前 | 减负后 | 目标方向 |
|---|---|---|---|
| Boot 必读文件数（fresh-session mandatory，见 `AGENTS.md §Session Start`） | 4 | 3 | 保持 ≤ 3 |
| 根目录 `*.md` 文件数（深度 1） | 80 | 54 | 保持 ≤ 60 |
| Operations/ 顶层 `*.md` 文件数（深度 1） | 84 | 34 | 只留活跃 pipeline |
| Operations/ 顶层条目数（含子目录，参考值） | 92 | 44 | — |
| 状态镜像数（描述「当前状态」的独立文件） | 3（STATUS + STATUS_FAST + STATUS_Split） | 1（STATUS.md） | 保持唯一 |
| 「描述当前状态却 > 30 天未更新」的文件数 | ≥1（STATUS_FAST 停在 05-23） | 0 | 保持 0 |

复核方式：上方 find 命令、检查 `AGENTS.md §Session Start` 强制读单长度、grep 状态镜像文件的 `updated` 字段。任何新增 index/router 文件都应触发一次"是否合并或废除一个旧文件"的检查（治理原则④，见 `Governance/README.md`）。
