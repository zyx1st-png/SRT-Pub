---
id: INDEX-2026-09-08-TODD-REALISATION-GAP-SRT
type: material_packet_index
status: active
layer: operations
epistemic_layer: os
claim_mode: evidence
canonical: false
date: 2026-09-08
source_id: SRC-2026-09-08-METHOD-TODD-BAYESIAN-DESCRIPTIONS-REALISATION-GAP
tags: [MaterialPacket, Mechanism, Realisation, HeldOutIntervention, SubtractiveAudit, PredictiveProcessing]
---

# Todd realisation-gap material packet — SRT routing index

## 1. Verdict

```text
Pipeline 1:
A — bounded non-canonical governance-method hardening

Direct target:
Governance/SRT_GOV_SUB01_Subtractive_Audit_Protocol_v0_1.md

O-track:
none / not claimed as ontology synthesis

D-track:
method-enabling gate only; no SRT empirical discriminator is claimed

canonical edit:
NO
```

Todd is not used as support for SRT. The article is used to make SRT's own mechanism claims harder to earn.

---

## 2. SourceCard

```text
Materials/2026/SRC_2026_09_08_Method_Todd_Bayesian_Descriptions_Realisation_Gap.md
```

Source anchor:

> Ian Todd (2026), *Bayesian Descriptions are not Mechanisms: Predictive Processing and the Realisation Gap in Embodied Neural Dynamics*, *Minds and Machines* 36:45. DOI `10.1007/s11023-026-09801-z`.

---

## 3. Retained source contribution

Todd separates:

```text
successful computational description
!= algorithmic identification
!= physical mechanism realisation
```

and supplies a concrete realisation burden:

```text
candidate mapping
-> independently specified physical / operational realiser
-> bounded rival mapping
-> held-out intervention
-> prospectively divergent causal signature
-> bounded result
```

When histories grouped into one candidate state respond differently under intervention because of timing, phase, contact or relational structure, the state mapping is too coarse for the declared mechanism claim.

---

## 4. SRT-side hardening

Existing GOV-SUB01 already owns:

```text
E_int
E_cf
E_mech
replacement test
perturbation sweep
hidden reparameterization
measurement blindness
bearer relocation
```

Todd adds the missing lock:

```text
same-projection fit
!= mechanism identification

shared disruption effect
!= mechanism identity

post-hoc mapping enlargement
= new hypothesis

failed mapping
!= failed mechanism family
```

Therefore the owner is hardened directly rather than creating a parallel methodology family.

---

## 5. Selection deletion consequence

For a future mechanism-level selection claim, the packet requires a bounded comparison such as:

```text
M_S = SRT-selection mapping
M_K = constraint / dynamics rival
I*  = held-out intervention not used to fit or select either mapping
```

with preregistered divergence:

```text
P(Y | do(I*), M_S) != P(Y | do(I*), M_K)
```

within a declared margin and horizon.

If both mappings preserve the same intervention signature, selection remains unproven as a mechanism-level irreducible for that target.

---

## 6. Bowler cross-read

```text
Bowler:
history can be sedimented into recurrent structure and alter later strategy.

Todd:
matching output / generalization does not identify the realising mechanism.
```

Combined pressure:

```text
history efficacy != irreducible history provenance
same present performance != same mechanism
same generalization != same mechanism
```

The next positive burden is a held-out intervention where candidate mechanisms actually diverge.

---

## 7. Explicit non-claims

Do not infer:

```text
Todd proves SRT
Todd disproves Bayesian models
coupling is automatically mechanistic
formal closure distinguishes SRT
one failed mapping eliminates a theory family
correlation with d / Psi_f / T_dir identifies their physical realisers
```

---

## 8. Reuse trigger

Retrieve this packet for:

```text
selection irreducibility
GOV-SUB01 full audits
mechanism-level d / Psi_f / T_dir projection claims
history-provenance tests
matched-state mechanism comparisons
future Bowler-style reversal / recombination experiments
```