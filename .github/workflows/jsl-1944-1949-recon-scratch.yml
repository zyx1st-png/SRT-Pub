name: JSL 1944-1949 Archive Recon Scratch (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Disabled after fixed-boundary reconnaissance completed.'
