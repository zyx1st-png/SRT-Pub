name: JSL 1944-1949 Inventory Generator Scratch (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Disabled after bounded inventory generation; retained only as scratch history.'
