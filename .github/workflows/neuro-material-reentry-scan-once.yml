name: Neuroscience Material Re-entry Scan Once

on:
  push:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: neuroscience-domain-reconstruction-framework-20260904
      - name: Scan material surfaces
        run: python .github/neuro_material_reentry_scan_once.py
