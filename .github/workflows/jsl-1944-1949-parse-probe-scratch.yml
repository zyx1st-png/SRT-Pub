name: JSL 1944-1949 Cambridge Parse Probe Scratch (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Disabled after publisher DOM and section probing completed.'
