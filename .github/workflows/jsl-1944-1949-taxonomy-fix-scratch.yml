name: JSL 1944-1949 Taxonomy Correction Scratch (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Disabled after taxonomy correction landed on the formal Phase-8B branch.'
