---
id: SRT-D3-OPTION-SPACE-READOUT-NOTE
type: research_note
tags: [D3, ClosureBoundary, OptionSpace, Reselectability, Externalization, Direction2, Seed]
status: note_v0
layer: meta
epistemic_layer: research_program
claim_mode: proposal
canonical: false
ai_do_not_use_for_definition: true
created: 2026-07-01
provenance: 从 `_SRT_DISTRIBUTIONAL_PAYABILITY_CANDIDATE_SEED.md §4.3`(D-3 检测问题)的一轮建构-攻击-修正对话中提炼;作者将读出对象从"主观/生理信号"重定位为"可选空间轨迹"
candidate_promotion_target: none (稳定后拟并入 `_SRT_DISTRIBUTIONAL_PAYABILITY_CANDIDATE_SEED.md` §2/§4.2-§4.3;本文件当前独立,不改写该 seed)
dependency: [SRT-DISTRIBUTIONAL-PAYABILITY-CANDIDATE-SEED, SRT-CLOSURE-BOUNDARY-CONVERGENCE-RECORD, SRT-DIRECTION2-PHASE-TRANSITION-DUEL, SRT-DIRECTION2-MORAL-GENEALOGY-SEED, SRT-OBJECTHOOD-AS-RESELECTABILITY-META-STANDARD, SRT-T-DIR-CANONICAL, SRT-PSI-F-CANONICAL]
---

# D-3 读出对象重定位 —— 从主观/生理信号到可选空间轨迹

> **文件地位(先读)**
> - **non-canonical research note**。不修改任何 canonical 文件,不改写 `_SRT_DISTRIBUTIONAL_PAYABILITY_CANDIDATE_SEED.md`。
> - 目标**不是**解决 D-3(闭包边界记录里"T_dir 是 d-门控的、遮蔽自我强化"那个 killer 攻击),是把它的检测对象重新定位到一个更精确、更接近可操作化的位置,并诚实标出重定位后剩下什么没解决。
> - 待本文件稳定后,拟回头把 `_SRT_DISTRIBUTIONAL_PAYABILITY_CANDIDATE_SEED.md §2` 加一条"支撑/转嫁关系"条件,并把该 seed §4.2-§4.3 改为指向本文件——**当前尚未做**,本文件是独立单元。

---

## 0. 一句话

> D-3 的检测对象不应是"`P_i` 有没有喊痛"或"某个代理指标有没有升高",而应是"`P_i` **真实可选空间的轨迹**,在结构变化前后是否被压缩"。这个重定位杀死/降级了两个旧候选,但没有消灭 D-3——它把 D-3 精确移动到了"反事实基线构造"与"归因"这两步。

---

## 1. 背景

`_SRT_DISTRIBUTIONAL_PAYABILITY_CANDIDATE_SEED.md §4.3` 指出:检测"某位置的 `Ψ_f` 局部份额是否已越阈"需要一个方向性读数,自然候选是 `T_dir`;但 `T_dir` 按其自身 canonical 定义(`_SRT_T_DIR_CANONICAL.md §1`),恰恰在受益于该分布的位置上最可能已经退化——这是闭包边界记录的 **D-3 killer**("T_dir 是 d-门控的、遮蔽自我强化 → 被遮蔽的外部化者自我背书窄边界")在 distributional payability 场景下的重现。

本文件处理:**用什么作为主证据,才能不重蹈 D-3。**

---

## 2. 候选降级:自我报告 / 生理代理 → 辅助证据

在导出本文件的主证据之前,两个候选被排查并降级(不是否定,是明确降为**辅助**):

- **自我报告(`P_i` 主观感受)**:被排除位置往往同时被剥夺发声渠道;且按 `_SRT_T_DIR_CANONICAL.md` 核心不对称命题("退化是熵增方向,恢复是逆熵操作",line 619),`P_i` 自身的 `T_dir`/`d` 可能**无需任何外部打压、单靠默认熵增**就已萎缩——自我报告不可靠,不只是因为被压制,也因为能力本身可能已衰退。
- **生理/行为代理指标**(HRV、SCR、语言情态比等,`_SRT_PSI_F_CANONICAL.md:31`):canon 自己的误用条款(line 247)已警告,这类指标的升高**可能主要来自旁路活动、噪声,而非真实的支付负担**——用它做主证据继承这条confound 风险。

**两者现在的地位**:辅助信号,可用于交叉验证,**不得**单独作为"`P_i` 是否受损"的判定依据。

---

## 3. 主证据:可选空间轨迹

> **主张(proposal)**:判断 `P_i` 是否被外部化,应比较结构变化**前后**:
>
> 1. `P_i` 原本真实可走的路径是否减少;
> 2. 被关闭的路径是否还能以可支付成本恢复;
> 3. `P_i` 是否还能申诉、再进入、触发修正;
> 4. 这种路径压缩是否**支撑了**整体系统或受益方位置的稳定。

**与既有材料的关系(非新发明,是重新对准)**:

- 第1-3条直接对应 `_SRT_OBJECTHOOD_AS_RESELECTABILITY_META_STANDARD.md §5` 的审计问题(真实选项场、可恢复性、再准入),以及 `_SRT_DIRECTION2_MORAL_GENEALOGY_SEED.md §6` RCI 的 V(真实可生成替代路径)、R(可恢复性)、A(申诉/再准入)三个分量。
- 第4条是本文件对 `_SRT_DISTRIBUTIONAL_PAYABILITY_CANDIDATE_SEED.md §2` 的一次**加固提案**(见 §5)。
- Wedge 1(`_SRT_DIRECTION2_WEDGE1_SIM_RESULTS.md`)的"sudden swap"实验设计——同一系统在 regime shift 前后追踪 option-diversity 轨迹——是这条主证据在 toy landscape 上**已经建成并跑通**的原型;本文件把它从"动力学半的 toy 验证"推广为"X 检测的真实世界证据来源",尚未在真实场景实现。

---

## 4. 这不是主观读数问题,而是反事实基线 + 归因问题

D-3 的原始表述("如何在不依赖受益方 `T_dir` 的情况下,检测遮蔽")在这个重定位下,变成了一个更窄、更具体的问题:

> **谁有资格构造 `P_i` 的真实可选空间基线,并判断这次压缩是否由原结构/受益方造成?**

这一步仍然需要一个读出位置,拆成两个动作:

1. **反事实基线构造**:若没有这次结构变化,`P_i` 原本还能有哪些真实可走路径?这不是数名义选项,而要判断哪些路径真实可达、可行动、可恢复——继承 `_SRT_DISTRIBUTIONAL_PAYABILITY_CANDIDATE_SEED.md §4.2` 已点名的反事实基线问题(与 ISP 入场阈值 σ_sub 结构同构)。
2. **归因**:压缩是这次结构变化造成的,还是外部冲击、自然退化、`P_i` 自身选择造成的?

**D-3 在这里重新出现,而不是消失**:若这两步由受益方自己完成,它可以把压缩解释为市场、环境或个人选择(D-3 原样重现);若由第三方完成,回到"第三方独立性"问题——第三方的续存(经费、standing、任期)若可被受益方单方面掐断,第三方就是原结构里的另一个 `P_i`,不是外部读出点(此问题本文件不处理,留作独立残余,见 §6)。

**进展的精确刻画**:D-3 从"如何检测主观/代理信号"收窄为"反事实基线构造 + 归因"这两个具体动作——比上一轮更接近可操作化,但**不是终结**。

---

## 5. 对 distributional payability §2 的加固提案(仅记录,未应用)

`_SRT_DISTRIBUTIONAL_PAYABILITY_CANDIDATE_SEED.md §2` 现有判据只要求"聚合可支付 **且** 某 `P_i` 局部越阈"两个事实并列成立。本文件提出加固:

> 仅有两个事实同时成立并不足够,还应要求二者之间存在**支撑/转嫁关系**——即 `P_i` 的可选空间压缩,正在**支撑**整体系统或受益方位置的稳定,而不只是巧合并存。

**状态**:proposal,记录于此,**未应用**到该 seed。待本文件稳定后,拟将其并入该 seed §2 作为新增条件,并把该 seed §4.2-§4.3 改为指向本文件。

---

## 6. 未被本文件关闭的残余

1. **"真实可达路径"的操作化**:V 分量(哪些路径算真实可达、可行动)仍未操作化,继承 Objecthood seed §5 / RCI 既有残余。
2. **反事实基线构造方法**:Wedge 1 的"前后对照"设计只在 toy landscape 验证过;真实世界的 matched-baseline / synthetic-control 式方法尚未给出。
3. **归因方法**:如何把"压缩"与"这次结构变化"而非其他原因关联起来,未给出具体方法,只指出这是必要步骤。
4. **第三方独立性(递归)**:谁有资格构造基线/做归因,其自身独立性如何保障——本文件明确不处理,是与本文件平行的另一条待开残余(前次对话已构建候选:审计者自身的 `Ψ_f`/续存不得被受益方单方面掐断,但该候选本身递归、未终结)。
5. **§5 加固提案的形式化**:"支撑/转嫁关系"目前只是文字描述,不是可核查判据——需要给出何时算"支撑"而非"巧合并存"的具体标准。

---

## 7. 禁句表(Must Not Be Overstated)

- ❌ "本文件已解决 D-3"(它把 D-3 精确移动到了反事实基线+归因,没有消除);
- ❌ "自我报告/生理代理指标已被证明无用"(它们被降为辅助证据,不是被否定);
- ❌ "可选空间轨迹判据已可操作化"(V 的真实可达性、反事实基线构造方法均未给出);
- ❌ "distributional payability §2 已加入支撑/转嫁关系条件"(§5 是记录中的 proposal,尚未应用到该 seed);
- ❌ "本文件解决了第三方独立性问题"(明确留作平行残余,见 §6.4)。

---

## 8. 与既有文件关系

- 待检测问题来源:`_SRT_DISTRIBUTIONAL_PAYABILITY_CANDIDATE_SEED.md §4.3`。
- 主证据取材:`_SRT_OBJECTHOOD_AS_RESELECTABILITY_META_STANDARD.md §5`、`_SRT_DIRECTION2_MORAL_GENEALOGY_SEED.md §6`(RCI V/R/A)、`_SRT_DIRECTION2_WEDGE1_SIM_RESULTS.md`(前后对照设计原型)。
- D-3 出处:`_SRT_CLOSURE_BOUNDARY_CONVERGENCE_RECORD.md`。
- **不改写** `_SRT_T_DIR_CANONICAL.md`、`_SRT_PSI_F_CANONICAL.md`、`_SRT_DISTRIBUTIONAL_PAYABILITY_CANDIDATE_SEED.md`。

---

## 9. 文件目标(防误用)

本文件把 D-3 检测问题的证据对象,从"主观感受/生理代理指标"重定位为"可选空间轨迹(真实路径数、可恢复性、申诉/再准入、与受益方稳定的支撑关系)",并诚实标出这个重定位没有解决什么(反事实基线构造、归因、第三方独立性)。它是一份独立的、可被攻击的候选笔记,不是对 `_SRT_DISTRIBUTIONAL_PAYABILITY_CANDIDATE_SEED.md` 的修订——修订留待本文件稳定后再做。任何"候选/proposal"措辞在被作者按 `Governance/SRT_EDIT_PROTOCOL.md` 采纳前,不得当 SRT 定论引用。
