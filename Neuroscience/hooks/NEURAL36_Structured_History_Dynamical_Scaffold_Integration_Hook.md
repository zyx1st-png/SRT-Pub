---
id: HOOK-NEURO-NEURAL36-STRUCTURED-HISTORY-DYNAMICAL-SCAFFOLD
patch_id: PATCH-NEURO-NEURAL36-STRUCTURED-HISTORY-DYNAMICAL-SCAFFOLD-ABSORPTION-PRESSURE
type: integration_hook
layer: operations
epistemic_layer: os
claim_mode: evidence
canonical: false
domain: neuroscience_learning_dynamics_history_reselectability
status: active
integration_status: pending
landing_ledger:
  - target: "Core/SRT_OPEN_TENSIONS.md"
    state: pending
    blocked_by: "Bowler narrows sufficiency conditions for generative health but does not itself authorize an owner rewrite; land only in an author-approved generative-reselectability hardening pass."
  - target: "Neuroscience/SRT_Neural_Mechanisms_CompactCore.md"
    state: pending
    blocked_by: "Current neuroscience synthesis target remains frozen; NEURAL36 is a bounded pressure patch, not authorization to reopen or expand the CompactCore."
---

# NEURAL36 — Structured-history dynamical scaffold integration hook

## 1. Target documents

```text
Core/SRT_OPEN_TENSIONS.md
Neuroscience/SRT_Neural_Mechanisms_CompactCore.md
```

Neither target is edited in this material pass.

---

## 2. Insert only when the relevant owner is explicitly reopened

### `Core/SRT_OPEN_TENSIONS.md`

Future placement should sit next to the formed / structurally stable / generatively healthy separation.

The bounded addition should be a **negative-control sentence**, not a new definition:

> Structured history can generate a stable, low-dimensional and generalizing dynamical scaffold without yet demonstrating consequence-sensitive revision of the system's own comparison, boundary or candidate-generation conditions; transfer/generalization is therefore not sufficient evidence for generative health.

### `Neuroscience/SRT_Neural_Mechanisms_CompactCore.md`

If neuroscience synthesis is later reauthorized, place Bowler as an implementation absorber in the history / transition-field discussion:

> Ordinary recurrent learning can already realize `history -> recurrent organization -> attractor geometry -> changed future strategy accessibility`; local excitation/inhibition and low-dimensional scaffolding therefore belong to the implementation comparison set rather than being identified with SRT primitives.

---

## 3. Cycle-2 cross-read

The current author source:

```text
01_Source_Intuition/SRT_AUTHOR_REENTRY_CYCLE2_DEPENDENCY_STRENGTHENING_2026-09-07.md
```

is **not an integration target** for this hook. It is an author-source control surface and must not be rewritten by an external-material pass.

Use NEURAL36 only as future pressure when the strengthened claim is audited:

```text
if "sedimented vertical structure"
reduces to
history-shaped multidimensional recurrent gating / attractor geometry over pre-given units

-> generic-dynamics absorption remains live
```

The paper does not settle whether the pre-One / selection-position-genesis version survives.

---

## 4. P4 reuse path

If a future author-approved generative-reselectability experiment reopens, retrieve together:

```text
Neuroscience/patches/SRT_Neuro_NEURAL36_Structured_History_Dynamical_Scaffold_Absorption_Pressure_v0_1.md
Neuroscience/patches/SRT_Neuro_NEURAL25_Memory_as_Historical_Selection_Bias_v0_1.md
Core/SRT_OPEN_TENSIONS.md
```

The preferred contrast is not another unrewarded transfer probe. It is a matched-present-state reversal / context-recombination test with consequence return and a strong recurrent-learning rival.

---

## 5. Do not include

Do not land any of the following:

```text
MEC = L2
eigenspectrum = Selection-position
inhibition = SRT backgrounding
low dimensionality = health
generalization = generative reselectability
Bowler supports / proves pre-One ontology
```

---

## 6. Closure condition

This hook remains `pending` until either:

1. an author-approved generative-health / Cycle-2 hardening pass explicitly opens one of the target owners; or
2. the patch is later judged fully redundant and the hook is withdrawn with a recorded reason.
